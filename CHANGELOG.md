# سجل تحديثات المشروع | Aqar — Changelog

> هذا الملف يُحدَّث **مع كل تغيير** في المشروع. الأحدث في الأعلى. التواريخ ميلادية (Asia/Riyadh).
> This file is updated on **every** change. Newest first.

الأنواع | Types: `ميزة` feature · `إصلاح` fix · `بيانات` data/migration · `توثيق` docs · `بنية` infra · `قرار` decision

---

## 🗄️ حالة الهجرات — لم تعد تُكتب هنا

**كانت في هذا الموضع قائمة يدوية بالهجرات المطبَّقة، وقد أُزيلت في ٥ أغسطس ٢٠٢٦.**

**لأنها كذبت مرّتين.** قالت «✅ هجرة `0029` (بوابة المستأجر) — طُبّقت» وهي غير مطبَّقة، **فبقيت البوابة معطّلة شهوراً**. وحين وُضع سجلّ حقيقي (`0068`) كشف أن **`0051` غائبة أيضاً**، ومعها مركز الفوترة ومركز الاشتراكات.

**قائمة تُكتب بالذاكرة ليست سجلّاً — هي نيّة.**

### الجواب الآن في مكانين، كلاهما يقرأ من القاعدة

| | |
|---|---|
| **`/platform/health`** | قسم «الهجرات» أعلى الصفحة: **مطبَّق ٦٨ من ٦٨**، وأي ناقصة **بالاسم** |
| `app.schema_migration` | الجدول نفسه — كل هجرة تُسجّل نفسها في سطرها الأخير |

**افتح `/platform/health` بعد كل تطبيق هجرة، وقبل إضافة أي مكتب جديد.**

### ⏳ يبقى معلّقاً (ليس هجرات)
- **جدولة cron يوميّة** — كانت `*/5 * * * *` وترفضها خطة Vercel Hobby فتفشل كل عملية نشر. صارت يوميّة. **عند الترقية إلى Pro استرجع `*/5`** لتصريف الإشعارات كل ٥ دقائق.

---

## 2026-08-21

### 🧪 بنية — أوّل سلسلة اختبارات تُخاطب المنتج عبر HTTP

**أربع مراجعات كود متتالية انتهت إلى الجملة نفسها:** الإصلاح في `server action` أو مسار HTTP، **ولا وسيلة في المشروع لاختبار أيٍّ منهما**. السلاسل إمّا SQL على Postgres مُضمَّن، أو دوالّ خالصة `.mts` — **فكل إصلاحٍ في طبقة التطبيق شُحن بلا تحقّق**.

```bash
npm run build && npm run test:e2e
```

**تُشغّل `next start` وتسأل المسارات كما يسألها Moyasar وVercel Cron:**

| **مهمّتا cron** | بلا ترويسة · بترويسة خاطئة · **وبالسرّ الصحيح بلا `Bearer`** |
|---|---|
| **ويبهوك الدفع** | ليست JSON · بلا سرّ · بسرٍّ خاطئ · **ثم يقبل الصحيح ويرفض حمولةً بلا `payment_intent`** |
| **`/api/version`** | شكل الجواب، **وأنه لا يُخزَّن مؤقتاً** — فجوابٌ مخزَّن يُبلّغ عن نشرةٍ قديمة إلى الأبد |

**والسرّان يُمرَّران في بيئة العملية الابنة** — وNext يُقدّمها على `.env.local`، **فالخادم المُختبَر يفرض سرّ الاختبار** مهما كان على جهاز المطوّر.

**وتُشغَّل في وظيفة البناء نفسها في CI** لأنها تحتاج ما أنتجته تلك الوظيفة للتوّ، **لا بناءً ثانياً**.

#### ⛔ وأين تقف عمداً

**عند آخر رفضٍ يقع قبل أيّ نداء لـSupabase.** وهذا **ليس قصوراً في الطموح بل النطاق الأمين الوحيد** ما لم يوجد مشروع Supabase للاختبار:

> **`next start` يقرأ `.env.local`، فطلبٌ يبلغ القاعدة يبلغ القاعدة الحقيقية** — **وسلسلةٌ تمسّ بيانات الإنتاج أسوأ من غياب السلاسل.**

**والمسارات الموجبة قرارُ المالك.** وفي [`tests/e2e/README.md`](tests/e2e/README.md) ما يُضاف فور وجود المشروع: **الويبهوك يردّ 500 عند فشل التطبيق** · **التجديد يعدّ `stranded` لا `charged`** · دورة الدعوة كاملة · رفع صورة الصيانة وربطها.

#### وثلاثة عوائق حُلّت في الطريق

**`npx` يُنشئ عبر صدفة على ويندوز** فتُوصَل الوسائط بدل أن تُهرَّب، **وnode يرفض إنشاء `.cmd` بلا صدفة** — فصار `node` يُشغّل ملفّ `next` مباشرةً، **وهو الملفّ نفسه على كل نظام**.

**والخروج كان يُسقط العملية بينما libuv يُغلق أنابيب الابن** — فتقول ١٢/١٢ **ثم تنهار**. **وسلسلةٌ تفعل ذلك تُعلّم الجميع تجاهل رمز خروجها.**

> **وتأكيدٌ منها كان خاطئاً لا الكود:** `/api/version` يعيد `id` فارغاً محلياً **بالتصميم** — المنصّة وحدها تضبطه، **وشارة الانحراف تصمت حين يغيب** بدل أن تُنذر كاذبةً. **فصار يُفحص العقد لا القيمة.**

- **التحقق:** `http-guards.mjs` **١٢/١٢** · `tsc` نظيف.

### 🧾 إصلاح — `0082`: نيّة دفعٍ لا تُغلَق، وآخر وحدة تُغادر `?error=`

**النتيجتان الباقيتان من مراجعة الاشتراكات.**

#### 🟡 ٥ — النيّة اليتيمة، وثالث يتيمٍ من العائلة نفسها في أسبوع

`startSubscriptionCheckout` يسجّل النيّة **ثم** يطلب فاتورة من Moyasar. **وحين يفشل الطلب الثاني** — البوّابة معطّلة، المفتاح خاطئ — يُقال للمكتب «حاول لاحقاً»، **وتبقى النيّة `initiated` إلى الأبد**.

> **ولا أحد يستطيع إغلاقها:** `subscription_payment` يمنح **SELECT فقط**، و`mark_subscription_payment_failed` لـ`service_role`. **فيتراكم في جدول المال صفوفٌ لمدفوعاتٍ لم تُطلَب قط، ويقرؤها `/platform/billing` عملاءَ عالقين في منتصف الدفع.**

**و`abandoned` حالةٌ جديدة، لا إعادة استعمال لـ`failed`** — وهذا جوهر الإصلاح:

| **`failed`** | بطاقةٌ رُفضت — **حقيقةٌ عن العميل**، وعليها يُبنى جدول المطالبة |
|---|---|
| **`abandoned`** | **حقيقةٌ عنّا:** لم نُفلح في السؤال أصلاً |

**ودمجُهما يضع عطلنا في تاريخ مدفوعات المكتب.** والدالّة **ترفض ما ليس `initiated`**: المدفوع مالٌ تحرّك، والمرفوض أساسُ مطالبة، والمردود تاريخٌ مُقفَل — **ولا يمسّها مسار تنظيف**، وويبهوكٌ يصل بعد لحظة يجب أن يجد ما يتوقّعه.

> **وهي ثالث يتيمٍ من العائلة نفسها في أسبوع** — صورة الصيانة (`0080`)، ودفعة الاستيراد، وهذه. **والقاعدة واحدة: من يُنشئ صفّاً قبل خطوةٍ قد تفشل يملك تنظيفه، ومن أنشأه يجب أن يُسمح له بالتراجع.**

#### 🟡 ٤ — آخر وحدة في المنتج تُغادر `?error=`

**خمسة أفعال كانت تُعيد التوجيه برسالةٍ في الرابط** — النمط الذي قامت الحملة كلّها على إزالته، **وهذه آخر وحدة لم تصلها**.

| **رقم الحوالة** | يعود مع الرفض — **يُنسخ من تطبيق البنك، وإعادة كتابته تعني فتح تطبيقٍ آخر لاستخراجه** |
|---|---|
| **البطاقة والتجديد التلقائي** | مكوّنا عميل، **والرفض بجانب الزرّ لا فوق الصفحة** — و«متاح لمدير المنشأة فقط» كان أبعد ما يكون عمّن ضغط |
| **«إزالة البطاقة»** | صار يؤكّد، **لأنه يوقف التجديد التلقائي أيضاً** — والمكتب يُخبَر قبل، لا حين يسقط اشتراكه |

**والنجاح يبقى تحويلاً حيث يكون وجهةً حقيقية:** صفحة Moyasar هي **مكان الدفع**، لا رسالةً عنه.

**وحُذف بانرا `?error=` و`?ok=`** — لم يبق لهما كاتب. **وبقي `checkout=return` وحده**، لأن Moyasar يُعيد المكتب به فعلاً.

- **التحقق:** `platform.mjs` **١٤٥/١٤٥** · `tsc` والبناء نظيفان · `build:schema` **٨٢ من ٨٢**.
- **⏳ يتطلّب تطبيقك:** `0082` — **مطبَّقة**.

### 💳 إصلاح — دفعةٌ تُخصم ولا تُفعَّل، وويبهوك يبتلع إخفاقه

**مراجعة الاشتراكات والفوترة — مسار المال، وهو الوحيد الذي يمسّ خطؤه جيبنا لا جيب المكتب.** خمس نتائج؛ **هذه أخطر ثلاث**، والباقيتان في التقرير أدناه.

#### 🔴 ١ — التجديد التلقائي كان يخصم البطاقة ثم يبتلع فشل التفعيل

```
if (result.ok && result.status === "paid") {
  await admin.rpc("apply_subscription_payment", …);   // ← الخطأ يُهمَل
  charged++;                                          // ← ويُعدّ نجاحاً
}
```

> **البطاقة خُصمت عند Moyasar فعلاً — المال تحرّك.** ثم تفشل `apply_subscription_payment` لأيّ سبب، **فيُرمى الخطأ ويُعدّ الصفّ مشحوناً وتُسجَّل التشغيلة ناجحة**.
>
> **و`claim_due_renewals` تكون قد دفعت `next_charge_at` للأمام عند الحجز** — فلا إعادة محاولة غداً. **المكتب دفع، واشتراكه لم يُمدَّد، ولا شيء يرفع راية.**

**فصار الخصم غير المُطبَّق يُعدّ وحده — `stranded` لا `failed`** — لأن بطاقةً مرفوضة **نتيجةٌ عادية**، وهذه **منشأةٌ دفعت ولم تنل ما دفعت مقابله**. وتُسجَّل التشغيلة **فاشلة بمعرّفات النيّات**، ويردّ المسار **500** لتراه لوحة الصحّة حمراء.

**وإخفاق تسجيل المطالبة يُعدّ كذلك:** عدم تسجيله يعني أن **جدول المطالبة لم يتقدّم** — فلا المكتب مخصوم منه ولا مُطالَب.

> ⚠️ **والـ500 لا يُسبّب خصماً مضاعفاً:** الحجز دفع `next_charge_at` للأمام، **فأيّ إعادة تشغيل لا تجد ما تطالبه**.

#### 🔴 ٢ — الويبهوك كان يبتلع ثلاثة أخطاء ويردّ `ok`

`mark_subscription_payment_failed` و`save_payment_method` **بلا فحصٍ إطلاقاً**. **والردّ بـ`ok` يوقف إعادة محاولات Moyasar** — فيضيع الحدث نهائياً.

**صار يردّ `500` عند كل إخفاق، وذلك هو الصواب لا العطل:** كل كتابة هناك **idempotent** — `apply` تمرّ مرور الكرام على نيّةٍ مدفوعة أصلاً. **فالإعادة آمنة، والردّ بـ`ok` على كتابةٍ لم تقع هو ما يمنع الإعادة من المجيء.**

#### 🟠 ٣ — المنشأة كانت تُؤخذ من الحمولة

`save_payment_method(metadata.org_id, …)` — **وتلك الدالّة ليست حفظ بطاقة فقط:**

```sql
update org_payment_method set status='removed' …   -- تُلغي بطاقة تلك المنشأة
insert into org_payment_method …                    -- وتضع الجديدة
update org_subscription set auto_renew = true …     -- وتُشغّل التجديد التلقائي
```

**والمالك الموثوق على بعد قراءةٍ واحدة من صفّ النيّة.** والمسار كان مغلقاً — `metadata` يُبنى من الجلسة — **لكن كتابةً بهذه الصلاحية لا تأخذ هدفها من الرسالة التي طلبتها.** *(ثالث مرّة يتكرّر هذا المبدأ في أسبوع: مسار الصورة، ومسار الحذف، وهذا.)*

#### ولم أستطع اختبار أيٍّ منها

**ثلاثتها في مسارات HTTP تتحدّث إلى Moyasar** — لا يبلغها مُشغّل Postgres ولا اختبار دالّة خالصة.

> **وهذه ثاني مرّة في يومين تُقال:** البند **«اختبارات E2E»** لم يعد بنداً مؤجَّلاً في قائمة، **بل الثغرة الوحيدة التي تتكرّر في كل مراجعة.**

- **التحقق:** `tsc` والبناء نظيفان. **ولا اختبار آليّ — وذلك جزء من النتيجة، لا هامشها.**

### 🔍 مراجعة — العقود والفواتير والاستيراد: ٨ نتائج، أقدمها منذ شهرين

**أوّل فحصٍ يقع على كودٍ لم يُكتب هذا الأسبوع.** الفحصان السابقان كانا على `0079`–`0081` — **وهذه وحدات عمرها شهران لم تمرّ بمراجعةٍ قط**، وقيمة الفحص أعلى حيث لم ينظر أحد.

#### 🔴 ١ — تعديل المسودة كان يقطع رابط الاسم التجاري

**الاسم التجاري حقيقةٌ واحدة في عمودين.** `createContract` يختاره من كتالوج المستأجر ويحفظ `trade_name` و`trade_name_id` معاً؛ **وتعديل المسودة يكتب الاسم نصّاً حرّاً ولا يمسّ الرابط إطلاقاً**.

> **فيصير العقد يقول «مطعم أ» ورابطه ما زال يشير إلى «مطعم ب».** والحقل نفسه له تجربتان: **قائمة عند الإنشاء، وصندوق حرّ عند التعديل**.

**واسمٌ يُكتب باليد لا كتالوج خلفه، فالرابط يُقطع حين يتغيّر الاسم — وحين يتغيّر وحده:** النموذج يرسل كل الحقول في كل حفظ، **وقطع الرابط لأن المكتب صحّح الإيجار إتلافٌ لبيانات لم يمسّها**. فيُقرأ المخزون ويُقارَن.

**وقيلت للمكتب تحت الحقل نفسه:** «تعديله هنا يفكّ ارتباطه بالاسم المسجَّل». **من يعدّل يستحقّ أن يعرف، لا أن يكتشفه من صفحةٍ كفّت عن المطابقة.**

#### 🔴 ٢ — الاستيراد كان يخلّف دفعةً يتيمة

`import_batch` يُدرَج **قبل صفوفه بالضرورة**، فأيّ فشل بعده كان يترك دفعة **فارغة بلا تحقّق، تظهر في قائمة المكتب كأنها عملٌ جارٍ** — ولا شيء في المنتج يزيلها. صارت تُحذف عند الفشل، **وفشل الحذف نفسه يُكتب في السجلّ**.

> **وهي عائلة الملف اليتيم التي عالجناها في `0080` — لكنها هنا منذ شهرين.**

#### 🟠 ٣ — وسيطٌ ميّت يناقض قاعدةً مكتوبة

`renewContract` كان يمرّر `p_number` من حقل `contract_number` **لا وجود له في المنتج كلّه** — فكان `null` دائماً. **ووجوده يناقض ما تقوله `0045` صراحةً:** الرقم يُسنَد من تسلسل بلا فجوات **كي لا تختاره يد**.

#### 🟡 ٤–٨ — إعادة هيكلة، في التزامٍ منفصل

| **٥ · رسائل PostgREST الإنجليزية** | كانت تصل المكتب في **٧ مواضع**: شاشة عربية، تسمّي أعمدةً لم يسمع بها. **وذلك ليس رسالة خطأ بل غيابها** |
|---|---|
| **٦ · `ContractState` و`ImportState`** | نسختان فقيرتان من `FormState` بلا `field` ولا `values` — **وهما سبب ٤** |
| **٤ · أطول نموذج في المنتج** | كان يُفرَّغ عند كل رفض، **والتعليق فوقه يقول «كل فعلٍ أدناه يعيد `FormState`»** |
| **٧ · سبب التراجع** | كان الثابت `"user_revert"` — **فكل تراجع يبدو كالآخر في سجلّ التدقيق** |
| **٨ · الحدّان** | مكتوبان مرّتين: في الشرط وفي نصّ الرسالة |

**والعلاج:** `refusalAr` في `lib/rpc-errors` — **جدول الوحدة أوّلاً، ثم أخطاء البنية القائمة، ثم رسالة عامّة والسبب الحقيقي في السجلّ**. وكل وحدة تحتفظ بجدولها لأن `CONTRACT_NOT_ACTIVE` لا يعني شيئاً لشاشة الفاتورة؛ **المشترك هو الترتيب، وأن غير المترجَم لا يُعرض أبداً**.

> **و`defaultValue` لم يُضف لكل حقل:** الحقول المتحكَّم بها تحفظ نفسها بحالتها. **أُضيف لغير المتحكَّم بها وحدها** — وهي التي كان React يمسحها.
>
> ⚠️ **وتغيّرت تجربة قائمة:** من كان يضغط «التراجع» ويؤكّد **صار يكتب سبباً أوّلاً**. تغيّرٌ مقصود، وهو البند ٧ نفسه.

#### وما لم أستطع فعله — يُقال ولا يُخفى

**لا اختبار آليّ للنتيجة ١.** منطقها في **server action**، **ولا مُشغّل في المشروع لهذه الطبقة** — الاختبارات إمّا SQL على Postgres مُضمّن، أو دوالّ خالصة `.mts`.

**وفكّرتُ في استخراجها إلى وحدةٍ خالصة لأختبرها كما فعل `plural-ar`، ورفضتُ:** تلك القاعدة كانت في **أربعة مواضع وشُحنت خاطئة مرّتين**؛ هذه في **موضع واحد**، **وثلاثة أسطر لا تستحقّ ملفّين**. **والمكان الصحيح لمثلها اختبارات E2E** — لا وحدةٌ مخترعة لتُختبَر.

- **التحقق:** `verify` **٥٣/٥٣** · `identity` **٤٩/٤٩** · `tsc` والبناء نظيفان.

### 🔧 إصلاح — مكنسة الصور كانت تُخفي فشلها، وهو ما يحذّر منه الملف نفسه

**فحصٌ ثانٍ بعد التسليم، على `0080` و`0081` وما حولهما.** عيبان، ولا سلوك ظاهر تبدّل بهما.

#### 1️⃣ فشلٌ يُقرأ نجاحاً

`purgeErasedPhotos` **حين تعجز عن سؤال القاعدة أصلاً** كانت تكتب سطراً في السجلّ **وتعيد صفراً وصفراً**. والمستدعي يقرأ `failed = 0` **فيسجّل التشغيلة ناجحة تماماً**.

> **وهذا بعينه ما يحذّر منه تعليقٌ في أعلى الملف نفسه:** «مهمّة مجدولة توقّفت عن العمل **تبدو تماماً كمهمّة لا عمل لها**». فصار **العجز عن السؤال** شبيهاً بـ**«لا شيء مستحق»** — وصفحة الصحّة لا تفرّق.

صار الخطأ **يعود مع النتيجة** ويُسجَّل فشلاً.

#### 2️⃣ عدٌّ يَعِد بما لم يتمّ

**نتيجة `mark_photo_purged` كانت مُهمَلة.** فلو فشل تفريغ العمود **بعد** حذف الملف، **تُحسب الصورة محذوفة وتعود غداً كأن شيئاً لم يكن.** صارت تُحسب فاشلة.

**ونُقل القسم بعد `0c` وسُمّي `0d`** بدل «`0b-bis`» — ترقيمٌ يحشر بينه لاتينيةً **يعترف بأنه لم يجد مكانه**.

#### وثلاثة فحوص، ونمطٌ واحد

| المسار يُؤخذ من العميل | خالف قاعدةً مكتوبةً في `/api/org/logo` |
|---|---|
| رسالة الهجرة يقرؤها مستأجر | نُسخت من قارئٍ آخر |
| المكنسة تُخفي فشلها | خالفت تعليقاً في **الملف نفسه** |

> **العيوب لا تأتي من جهلٍ بالقاعدة، بل من عدم إعادة قراءتها عند كتابة الجار.** ولا يمسكها `tsc` ولا اختبار — **يمسكها أن يُقرأ الملف مرّةً أخرى بعد الكتابة**.

- **التحقق:** `maintenance-notify.mjs` **٣٣/٣٣** · `tsc` والبناء نظيفان.

### ⚡ أداء — `0081`: صفحة مكتبٍ واحد تكفّ عن قراءة المنصّة كلّها

**ملاحظة مراجعة كودٍ رقم ٤، معلّقة منذ مراجعةٍ سابقة.** `platform_tenant_360(org)` هي صفحة **مكتبٍ واحد** عند المشغّل، **وكانت تمسح المنصّة كلّها مرّتين**:

| `platform_identity_activity()` | **كل من سجّل دخولاً على المنصّة قط**، يُوصَل بعشرة أعضاء |
|---|---|
| `platform_org_activity()` | **المسح نفسه، مجمَّعاً لكل مكتب**، ثم يُرمى كل شيء إلا صفّاً واحداً |

> **والملاحظة كانت عن الثاني وحده — والأوّل يقع في السطر المجاور ولم يُذكر.**

**وهي صحيحة، وغير مرئية اليوم — ولذلك بقيت:** مع حفنة مكاتب الهدر مللي ثوانٍ. **لكنها تنمو بعدد المكاتب على المنصّة، لا بحجم المكتب المفتوح** — وهي **أرخص أشكال التكلفة إصلاحاً قبل وجود البيانات، وأوجعها بعده**.

**ولا سلوك تغيّر:** الأعمدة والقيم والترتيب كما هي. **توأمان مُقيَّدان بالمنشأة، والنسختان بلا وسيط باقيتان كما هما** — `platform_kpis` (`0049`) يجمع عبر المنصّة كلّها **ويريد كل مكتب فعلاً**. **قارئٌ ثانٍ بسؤالٍ أضيق، لا بديل.**

> **وأُعيدت كتابة `platform_tenant_360` كاملة** لأن plpgsql لا يُرقّع سطرين من جسم دالّة — **وقيل ذلك في تعليق الهجرة صراحةً**، فهذه بعينها المناورة التي أعادت بها `0074` خطأً مُصلَحاً. **الهجرات هي المصدر، و`schema_all.sql` لقطةٌ مولَّدة.**

- **التحقق:** `platform.mjs` **١٤٥/١٤٥** (أُضيف ٣: حارس المشغّل على التوأمين، والتراجع الرشيق بلا `auth.users`) · `tsc` والبناء نظيفان · `build:schema` **٨١ من ٨١**.
- **⏳ يتطلّب تطبيقك:** `0081` — **مطبَّقة**.

### 🔒 ميزة — `0080`: المحو يبلغ الصورة، واليتيم لم يعد يُخلَق

**بالأمس شُحنت `0079` باعتراف مكتوب:** محو `0061` كودٌ في القاعدة، **والقاعدة لا تحذف ملفاً من التخزين** — فصورة مطبخ أحدهم **تعيش بعد محو صاحبها** ما لم يتذكّر مديرٌ زرّاً. **والاعتراف خيرٌ من وعدٍ كاذب، وليس خيراً من الوفاء.**

**وعيبان لا واحد، وعلاجهما مختلف:**

#### 1️⃣ صورة من مُحيت بياناته — ترشيحٌ ثم تأكيد

| `claim_erased_photos` | تُرشّح ما صاحبه `erased_at` — لـ`service_role` وحده |
|---|---|
| **التصريف اليوميّ** | يحذف من التخزين (الـAPI في Node، لا في SQL) |
| `mark_photo_purged` | **تُفرّغ العمود بعد أن يزول الملف فعلاً**، وتكتب سطر تدقيق |

> **واحدةً واحدة، وكلٌّ تُؤكَّد وحدها.** دفعةٌ تنجح نصفها تترك ما عجزت عنه **مُرشَّحاً ليُعاد غداً** — **وصفٌّ مُفرَّغ لملفٍّ ما زال في الدلو محوٌ يكذب على نفسه**، وذلك أسوأ من محوٍ يتأخّر يوماً.
>
> **والعمود لا يُفرَّغ أوّلاً** لأنه المؤشّر الوحيد إلى الملف: تفريغه قبل الحذف **يُضيّع عنوان ما نريد حذفه**.

#### 2️⃣ اليتيم لم يكن مشكلة مسحٍ أصلاً

**رسالة «الصورة رُفعت ولم تُربط بالطلب» كانت تعني ملفاً لا يشير إليه شيء** — **ولا يستطيع المستأجر حذفه، لأننا منعناه.**

**فالعلاج سياسة لا مكنسة.** الحذف بقي للمدير في `0079` لأن **الصورة المرتبطة دليل**، ومن له أقوى دافع لسحبها يجب ألّا يقدر. **وذلك التعليل لا يبلغ ملفاً لا يشير إليه أيّ طلب:** هو دليلٌ على لا شيء، **والمتضرّر الوحيد من زواله من رفعه خطأً**.

> **فصار الفعل ينظّف وراءه عند فشل الربط — ولا يُخلَق يتيمٌ بعد اليوم.** وملاحقة قذًى ما كان ليقع أضعفُ من منعه.

#### وشاشة المحو تقول ما نملكه

> «و**صور طلبات الصيانة** تُحذف تلقائياً خلال ٢٤ ساعة. ولحذفها فوراً: افتح الطلب واضغط «حذف الصورة».»

**تُقال هناك لأنه موضع الوعد** — حيث يَعِد المكتب صاحب البيانات، لا في صفحةٍ يقرؤها بعد أسبوع. **وتأخيرٌ معلَن خيرٌ من وعدٍ ضمنيّ.**

⚠️ **و«٢٤ ساعة» مبنيّة على التصريف اليوميّ ٠٦:٠٠** — فإن صار كل ٥ دقائق (خطة Pro) صارت الجملة أكرم مما تَعِد، وذلك اتجاه الخطأ المقبول.

- **التحقق:** `maintenance-notify.mjs` **٣٣/٣٣** (أُضيف ٥: لا تُرشَّح صورة حيّ، والترشيح بمساره، والتأكيد يُفرّغ، ولا يُرشَّح مرّتين، والتدقيق) · `tsc` والبناء نظيفان · `build:schema` **٨٠ من ٨٠**.
- **⏳ يتطلّب تطبيقك:** `0080` — **مطبَّقة**.

### 🔧 إصلاح — ثلاثة وجدها فحص جودة الكود على صورة الصيانة

**فحصٌ بعد التسليم على ما كُتب في `0079` وما حوله.** لا سلوك مقصود تغيّر؛ **ثلاثة عيوب لم يكن أيٌّ منها ليظهر في `tsc` ولا في البناء ولا في الاختبارات.**

#### 1️⃣ المسار كان يُؤخذ من حقلٍ مخفيّ يملكه العميل

`deleteMaintenancePhoto` يقرأ `photo_path` من النموذج **ثم يحذف به من التخزين**.

> **وليس ثغرةَ صلاحيات:** سياسة الدلو تحصر المدير في مجلّد منشأته. **لكن «محصور في ملفات مكتبك» ليس «ملفّ هذا الطلب»** — ولا شيء يُكسب بسؤال المتصفّح عن جوابٍ موجودٍ في الصفّ.
>
> **والأسوأ أنه خالف قاعدةً مكتوبةً في المشروع بخطّ اليد نفسه:** `/api/org/logo` يقول صراحةً *«المسار لا يُؤخذ من الطلب أبداً»* — **ثم خُولف في الميزة التالية مباشرة.**

صار يُقرأ من الصفّ عبر RLS، **وسقط الحقل المخفيّ من الواجهة.**

#### 2️⃣ رسالةٌ نُسخت من قارئٍ آخر

**«لم تُنشأ مساحة الصور بعد — طبّق الهجرة `0079`»** — يقرؤها **مستأجر**. أُخذت من فعل الشعار حيث القارئ **مدير منشأة**.

> **ولا فعل للمستأجر بها، ولا الشاشة شاشته ليُصلحها.** صارت كل أسباب الفشل جملةً واحدة له، **والسبب الحقيقي يذهب إلى السجلّ**. وأُضيف سطر سجلٍّ لفشل قراءة المجلّد، **وكان يعود صامتاً بخلاف أخويه**.

#### 3️⃣ رسائل لا تلتئم بما تسقط فيه

كانت جُملاً تامّة تُقرأ بعد «لكن»، فتخرج **«لكن رُفعت الصورة…»**. صارت شذرات، **ومكتوبٌ في توثيق الدالّة أن ما يعود شذرةٌ لا جملة** — لأن من يضيف رسالة بعد سنة **لن يرى موضع التركيب**.

**والمسار كان يُحسب مرّتين، فصار مرّة.**

**وما لم يُغيَّر عمداً:** `attachPhoto` يرفع ويعيد قيمة — مخالفة ظاهرية لفصل الأمر عن الاستعلام، **لكنه نمط المشروع كلّه** (كل `action` يعيد `FormState`)، **ومطابقة الجيران تسبق**. و`"maintenance-photos"` مكرّر في ثلاثة ملفات كما `"org-assets"` في اثنين — **ثابتٌ يعبر حدود التطبيق تجريدٌ بمستعملٍ واحد.**

- **التحقق:** `maintenance-notify.mjs` **٢٨/٢٨** · `tsc` والبناء نظيفان.

### 📷 ميزة — `0079`: صورةٌ واحدة تغني عن زيارة

**`photo_path` موجود منذ `0072`، وكل طلب قُدّم منذئذ وضع فيه `null`** — لا دلو يحمل ملفاً، ولا حقل يختاره. **و«تسريب في المطبخ» عشر مشكلات مختلفة**، يجيب عنها المكتب بإرسال من ينظر — **زيارةٌ كانت صورةٌ واحدة تكفي عنها**.

**ودلو الشعار (`0066`) لا يصلح:** سياساته تسأل `is_member_of` و`is_org_admin`، **والرافع هنا مستأجر — ليس أيّاً منهما، وبحقّ.**

| دلو `maintenance-photos` | خاص · **٥ ميغابايت** · `jpeg · png · webp` |
|---|---|
| **الحدّ** | صورة الجوّال بين اثنين وخمسة، **وحدٌّ يرفض الحالة العادية حدٌّ يحوّل «أبلغ عن العطل» إلى «دعك منه»** |
| **HEIC غائبة عمداً** | iOS يحوّل عند الرفع عملياً، **ورفض الدلو للنادر أفضل من ملفٍّ لا يعرضه متصفّح** |

#### وترتيبٌ عكستُه بعد أن صمّمته

**كان الرفع قبل الإرسال.** ثم تبيّن أن القاعدة **ترفض الطلب لأسبابٍ تعرفها وحدها** — لا عقد نشط، أو تجاوز الخمسة يومياً — **ورفعٌ سبق الرفض يترك ملفاً لا يملك المستأجر حذفه**، لأن الحذف فعلٌ مكتبيّ. **ويتيمٌ لا يُسمح لأحد بإزالته أسوأ من دورةٍ إضافية.**

> **فصار: أرسِل ← ارفع ← اربط.** والمسار `<org_id>/<party_id>/<request_id>.<ext>` — **حتميّ ولا يتيم فيه**. وإن فشل الرفع **فالطلب قائم، والرسالة تقول ذلك** بدل أن تدّعي ضياعه.

#### ولا سياسة تعديل أصلاً، لأحد

**صورة عطلٍ دليل، ودليلٌ يُستبدل بعد الإبلاغ ليس دليلاً** — **ومن بيده هذا الباب هو صاحب أقوى دافع لاستبدالها**. والربط مرّة واحدة، و`PHOTO_ALREADY_SET` بعدها.

**والقراءة تمرّ عبر مسارنا كما يمرّ الشعار:** سياسة الدلو **سياجٌ خارجيّ** لأنها تعرف الطرف ولا تعرف الطلب، **والمسار هو الدقيق** — يقرأ الطلب عبر RLS، **فيُطبَّق حصر الموظّف في عقاراتٍ بعينها**، وهو ما لا يستطيعه الدلو.

**والمستأجر لا يقرأ صفّ `party` الخاص به**، فلا تستطيع الواجهة بناء المسار. **`maintenance_photo_folder` تُعطي البادئة التي ستقبلها السياسة، ولا شيء غيرها عن الطرف.**

**ويرى المستأجر أن صورته مرفقة** — لا زينةً، **بل ليعرف أنها وصلت**: صورةٌ لا يراها مرسلها **صورةٌ ستُرسل ثانيةً بواتساب**.

> ⚠️ **وحدٌّ مكتوب لا موعود:** محو `0061` **كودٌ في القاعدة لا يمسّ التخزين**. فزرّ **«حذف الصورة»** للمدير **هو الطريق الوحيد لخروج صورة بيت أحدهم من النظام** — وذلك مكتوبٌ في تعليق العمود وفي الدليل. **والأسوأ أن نَعِد بمحوٍ لا يقع.**

- **التحقق:** `maintenance-notify.mjs` **٢٨/٢٨** (أُضيف ٧: البادئة، ورفضها لغير صاحبها، والربط، ومنع الاستبدال، ومنع الغريب، ورؤية المستأجر للعلم دون المسار) · `tsc` والبناء نظيفان · `build:schema` **٧٩ من ٧٩**.
- **⏳ يتطلّب تطبيقك:** `0079` — **مطبَّقة**.

### 🔔 ميزة — `0078`: المستأجر يعرف ماذا جرى لطلبه

**`0072` أعطت المستأجر طريقاً ليُبلّغ، وأعطت المكتب طريقاً ليعالج.** وبينهما **كان المستأجر لا يسمع شيئاً** — لا حين يبدأ العمل، ولا حين يُغلق الطلب، ولا حين يُرفض. **فيفتح البوابة كل يوم ليتفقّد، أو يتّصل بالمكتب — وهما بالضبط ما بُنيت الوحدة لتوفيره.**

**وكل ما يلزم للإرسال كان موجوداً:** طابور بالتكرار والتراجع (`0038`)، ومُصرِّف (`0059`)، ومزوّد يعمل منذ ٨ أغسطس. **الناقص أن الإشعار كان دائماً غرضاً مكتبياً:** `app.notification` تُقرأ عبر `has_property_access`، **والمستأجر ليس عضواً — فلا صفٌّ فيها كان مقروءاً له قطّ.**

| ما أضافته | |
|---|---|
| **`recipient_party_id`** | **NULL = ما يعنيه كل صفّ اليوم: للمكتب.** وغير NULL = موجَّه لشخص، **والمكتب لا يراه** |
| **`link_path`** | المُصرِّف كان يرسل الجميع إلى `/app/notifications` — **صفحة موظّفين يصطدم بها المستأجر** |
| **مُشغِّل على تغيّر الحالة** | `in_progress` · `resolved` · `cancelled`. **والعودة إلى `open` صامتة** — تصحيحٌ إداريّ لا خبر |

**ومُشغِّل لا سطرٌ داخل فعل المكتب:** الحالة تُحدَّث بـ`UPDATE` عبر RLS، **فلا دالّة واحدة تُعدَّل، وأيّ مسار مستقبليّ كان سينساه**.

> **وملاحظة الإغلاق لا تُرسَل** (قرار ٢٠ أغسطس). المكتب يكتبها منذ `0072` **وهو يعلم أن المستأجر لا يراها** — فقد تحمل ما لم يُكتب ليُقرأ. **ونشرها اليوم يغيّر معنى حقلٍ مُلئ بثقة.** والاختبار يُثبت ذلك بنصٍّ يحوي «٣٠٠».

#### وثلاثة أشياء ظهرت في الطريق

**1️⃣ عيبٌ كان سيُشحن صامتاً.** `enqueue_email_deliveries_for` **تكنس كل صفّ غير مقروء في المنشأة** — وكل إشعار مستأجر صفٌّ غير مقروء في المنشأة. **فكان المكتب سيتلقّى نسخة من كل رسالة كُتبت لمستأجر، موقّعة باسمه وموجَّهة إليه.** وعدّاد «غير المقروء» كان **يعدّها عملاً مكتبياً ينتظر** — وهو الاعتراض نفسه الذي رفعته `0070` على عمق طابورٍ مُنتفخ بصفوفٍ لن تُرسَل.

**2️⃣ وكدت أُكرّر خطأ `0074`.** هممتُ بإعادة كتابة الغلافين، **و`0059` جعلتهما يُفوّضان إلى التوأمين `_for`** — فإعادة كتابة غلافٍ ليحمل الاستعلام ثانيةً **تُلغي ذلك التفويض بصمت**. أُصلح التوأمان وحدهما.

**3️⃣ وسياسة القراءة لم تعمل أوّل مرّة.** المستأجر يسأل «هل هذا الصفّ لي؟» عبر `EXISTS` على `app.party` — **وسياسة `party` تحجب سجلّه عنه، فيُجاب «لا»**. فصار الجواب `party_is_mine` بـSECURITY DEFINER، **كما فعلت `0029` بـ`tenant_is_mine`** بالضبط.

> ⚠️ **وقيدٌ لا يُصلحه هذا العمل:** الطابور يُصرَّف **يوميّاً ٠٦:٠٠** على خطة Hobby، **فقد يصل الإشعار بعد ٢٤ ساعة**. يحلّه الترقّي إلى Pro وحده.

- **التحقق:** `maintenance-notify.mjs` **٢١/٢١** (سلسلة جديدة، منفذ 54365) · `verify` **٥٣** · `phase3` **١١٩** · `email-backlog` **١٠** · `tsc` والبناء نظيفان · `build:schema` **٧٨ من ٧٨**.
- **⏳ يتطلّب تطبيقك:** `0078` — **مطبَّقة**.

---

## 2026-08-20

### 📬 دعوة أُرسلت ولم تصل — ما كشفه التحقيق، وأربعة تغييرات خرجت منه

**قال المكتب: أرسلتُ الدعوة ولم يستلمها المستأجر.** والشاشة تقول «أُرسلت». **وكلاهما صادق.**

**والتحقيق انتهى إلى أن المنصّة بريئة:** الرسالة مبنيّة صحيحة، والعنوان صحيح، والرابط صحيح، **والتوثيق سليم بالكامل** — `send.6n1.io` SPF عبر amazonses، وDKIM قائم، وDMARC عند `p=none`. وResend سلّمها، وGmail قبِلها. **ثم أُعيدت إلى عنوانٍ آخر فوصلت في ثوانٍ.** فالعطل داخل صندوقٍ واحد: فلتر، أو إعادة توجيه، أو قاعدة لا يذكر أحد أنه كتبها.

> **ولم يكن ذلك درساً في البريد بل في المنصّة:** أربع ثغرات ما كانت لتظهر لولا رسالةٌ ضاعت.

#### 1️⃣ `0077` — «أُرسلت» تُخبرنا أين نسأل حين لا تصل

**Resend يُعيد معرّف الرسالة، وكنّا نرميه.** فلم يكن ثمّة خيطٌ يربط صفّ الدعوة بسجلّ المزوّد حين يُسأل: **أين ذهبت؟**

| | |
|---|---|
| **`sent_message_id`** | عمودٌ واحد يحفظ ما قبِله المزوّد |
| **«لم تصل الرسالة؟»** | تفصيل **مطويّ** في بطاقة البوابة — يهمّ يوم الشكوى، وضجيجٌ فيما سواه |
| **سطر التدقيق** | يحمل المعرّف كذلك: صفّ الدعوة يحفظ **آخر إرسال فقط** |

**وصُحّحت جملةٌ كانت تدّعي أكثر مما نعرف:** «وصلت الرسالة إلى البريد» صارت **«سلّمنا الرسالة لمزوّد البريد وقبِلها»**. **القبول ليس الوصول** — وإخبار المكتب بما لا نعرفه هو ما يحوّل «لم يصلني شيء» إلى خلافٍ لا يفصله أحد.

**ودُلّت الدالّة ذات الوسائط الثلاثة بأربع** بدل أن تبقى إلى جانبها: تحميلان لا يفرّق بينهما إلا وسيطٌ أخير له قيمة افتراضية **استدعاءٌ ملتبس**.

#### 2️⃣ الدعوة تصل باسم المكتب، ويمكن الردّ عليها

**كانت تصل من «مكاتب العقار» على نطاق لا يدلّ على أحد، وبلا عنوان ردّ.** والمستأجر لم يسمع بالمنصّة قط — **سمع بمكتبٍ يؤجّره**.

- **اسم المُرسِل:** «اسم المكتب **عبر عقار**».
- **Reply-To:** بريد المكتب من `contact_email`.

> **والعنوان نفسه لم يتغيّر ولا يمكن أن يتغيّر:** لا يُرسل إلا من نطاق موثَّق. غُيّر **من تقول الرسالة إنها منه**، لا من أين خرجت.
>
> **وReply-To يُترك فارغاً إن لم يسجّل المكتب بريداً:** عنوان ردٍّ لا يصل أحداً **أسوأ من غيابه**، لأنه يدعو إلى ردٍّ يضيع بصمت.

**واسم المكتب صار يُقرأ من القاعدة** بدل حقلٍ مخفيّ في النموذج: هذا ما يراه غريبٌ في بريده وما يصله ردّه، **ولا ينبغي أن يكون ما أرسلته صفحة**. فسقط الحقل المخفيّ، **وسقط معه استعلام المنشأة من الصفحة** إذ لم يبق له قارئ.

**ولم يُضف `List-Unsubscribe`:** هذه رسالة **معاملات لا نشرة**، وإضافته تمنح المستأجر زرّاً **لإلغاء الاشتراك في دعوته هو**.

#### 3️⃣ مسارٌ ثانٍ حين يبتلع الصندوقُ الدعوة

**ثبت بالتجربة أن رسالةً سُلّمت وصُودق عليها ولم يرها صاحبها.** وحين يحدث ذلك، لم يكن بيد المكتب إلا أن يُرسل مرّة أخرى **إلى الحائط نفسه**.

**فصار الرابط يُعرض مرّة واحدة لحظة إصداره**، مع زرّ نسخ وسطرٍ يقول إنه لن يظهر ثانية. **لا يُخزَّن معروضاً، و`portal_invitation_state` لا تُعيده أبداً** — ومن فاته النسخ يُصدر دعوة جديدة.

> **وعرضه آمنٌ اليوم وما كان كذلك قبل `0074`:** الرابط وحده لم يعد يكفي أحداً — **القبول يشترط أن يطابق بريدُ الحساب البريدَ المدعوّ**. فمن يلتقطه لا ينتفع به.

**ويعود الرابط مع الفشل أيضاً، لا مع النجاح وحده:** تلك هي الحال التي تحتاجه أكثر — **الدعوة قائمة ولم يحملها شيء**، ودعوةُ المكتب إلى الضغط ثانيةً تُدوّر رمزاً لم يصل هو الآخر. **والنسخ قد تمنعه المتصفّحات، فالرابط يبقى قابلاً للتحديد بالكامل.**

#### 4️⃣ `0076` — البوابة تقول للمستأجر كيف يدفع، لا كم عليه فقط

*(طُبّقت في ١٩ أغسطس ولم تُوثَّق حينها.)*

**كانت الشاشة تنتهي عند «٩٬٠٠٠ ر.س، متأخرة ٣ أيام»**، فيتّصل المستأجر ليسأل عن آيبانٍ سجّله المكتب عنده أصلاً — **وبيانات المكتب البنكية على `app.organization`، وRLS تُخفي ذلك الجدول عن المستأجر بحقّ**.

**فأضافت `tenant_portal_payment_info` ثلاثة حقول لا أكثر:** اسم الحساب والبنك والآيبان. **لا سجلّ تجاري ولا رقم ضريبي ولا عنوان ولا ترخيص** — مستأجرٌ يدفع إيجاره لا يحتاج شيئاً منها، **وقراءة البوابة قراءةٌ من خارج المكتب**.

> **والدالّة لا تُرجع صفّاً إن لم يُسجَّل آيبان:** بطاقةٌ عنوانها «كيف أدفع» فوق ثلاث شُرَط **أسوأ من غيابها** — تُخبر المستأجر أن المكتب نسي، **بصوت المكتب**.

**والبطاقة تحت مبلغ الدفعة مباشرة** لأنها السؤال الذي يليه، **وفيها رقم العقد مرجعاً للحوالة**: أرخص ربطٍ بين حوالةٍ ومكتبٍ يستقبل عشرات الحوالات. **وسطرها الأخير يقول الحقيقة صراحةً: التحويل ليس سنداً**، والمكتب يُصدره حين يصل المال.

- **التحقق:** `invitations.mjs` **٢٥/٢٥** (أُضيف: حفظ المعرّف، ووصوله سطر التدقيق) · `tsc` والبناء نظيفان.
- **⏳ يتطلّب تطبيقك:** `0076` و`0077` — **مطبَّقتان**.

---

## 2026-08-19

### ⚙️ تغيير — قناة إثبات هوية البوابة تُختار من قائمة، لا تُكتب بالنصّ

الإعداد يظهر في لوحة الإدارة منذ `0074`، **لكن كصندوق نصّ حرّ**: قيمته **واحدة من ثلاث** والقاعدة ترفض ما عداها — **وصندوقٌ حرٌّ لقيمة محصورة تذكرةُ دعمٍ تنتظر أن تُفتح**.

```
◉ البريد الإلكتروني
○ الجوال — لا يوجد مزوّد رسائل نصّية مُعدّ            [معطّل]
○ أيّهما (بريد أو جوال) — يتطلّب مزوّد رسائل نصّية    [معطّل]
```

> **وخيارا الجوال معطَّلان ومعروضان معاً:** المشغّل يستحقّ أن يرى **أن المفتاح موجود وما الذي ينتظره**. وخيارٌ مخفيّ يعني أن أحداً **سيسأل «أين الجوال؟»** بدل أن يقرأ الجواب.

**ولم يُخترَع متغيّر بيئة لفحص المزوّد.** لا مزوّد رسائل في المشروع أصلاً ([ADR-0001](docs/adr/0001-sms-provider.md) معلّق)، **واختراع اسمٍ لفحص شيءٍ غير موجود تجريدٌ بلا مستعمل**. السبب مكتوب في الخيار نفسه، **ومن يتعاقد على المزوّد يرفع المنع من ذلك الموضع**.

**والقيمة السارية لا تُعطَّل أبداً مهما كانت:** لو بُدّلت يوماً من القاعدة مباشرة، تبقى الصفحة قادرة على عرضها والرجوع عنها — **فلا تُحبَس اللوحة خارج إعدادٍ قائم**.

### 📱 تغيير — شاشة المستأجر تُعاد كتابتها للجوّال الذي تُقرأ عليه

**كانت تفتح على ثلاثة تبويبات من جداول المكتب** — العقود والدفعات والصيانة — كلٌّ منها شبكة بخمسة أعمدة تنزلق أفقياً.

**والمستأجر يصل بسؤالٍ من اثنين:** «كم عليّ ومتى» أو «شيءٌ تعطّل». **وما عداهما مرجعٌ يُفتح مرّة.**

| فصارت تُجيبهما فوق الطيّة | |
|---|---|
| **بطاقة الدفعة القادمة** | بمبلغها وتاريخها، **وحمراء إن تأخّرت** |
| **زرّ طلب الصيانة** | لا تبويب يُبحَث عنه |

**والتبويبات أُزيلت:** ثلاثة تبويبات على شاشة ضيّقة **تُخفي ثلثي صفحةٍ قصيرة**. صارت أقساماً تتوالى ورابطين سريعين.

**والجداول صارت صفوفاً تتراكم** — جدولٌ بخمسة أعمدة على الجوّال **جدولٌ لا يقرأه أحد**.

> **و«متأخرة ٣ أيام» تسبق التاريخ**، لأن التاريخ وحده **يُحسب ذهنياً** والعبارة **تُقرأ**.
>
> **ونموذج الصيانة داخل `details`**: مفتوحٌ لمن لا طلبات له — فهو حينها كل ما في القسم — **ومطويٌّ لمن له طلبات**، فلا يزاحم متابعتها.
>
> **وحين لا يوجد مستحق تقول البطاقة ذلك بالأخضر صراحةً** — لا صفراً رمادياً يترك القارئ يتساءل: **أهو صفرٌ، أم رقمٌ لم يُحمَّل؟**

### 🔴 إصلاح — زرّ دعوةٍ كان يقتل الرابط القائم ولا يُرسل شيئاً

**أمسكه الفحص البصري على الإنتاج، ولم يره البناء ولا `tsc`:** كلاهما أخضر، والزرّ يُصيَّر صحيحاً، **وأثره هدمٌ صامت**.

**سجلٌّ بلا بريد إلكتروني كان يعرض «إعادة الإرسال».** و`resend_portal_invitation` **تُبطل الرابط الحيّ أوّلاً** ثم تُصدر جديداً — فيكتشف الفعل بعدها أن لا عنوان لإرساله إليه ويتوقّف. **فيبقى المكتب أسوأ حالاً من قبل الضغطة: الرابط القديم ميّت، ولا رسالة خرجت.**

**فصار الفحص قبل أن يُمَسّ شيء:** الفعل يقرأ بريد الطرف **أوّلاً** ويرفض، **والبطاقة لا تعرض الزرّ أصلاً** — بل تقول أين يُضاف البريد ولماذا يلزم: الدعوة تُرسَل إليه، ولا تُقبل إلا منه.

**وأُضيف `id="portal"`** إلى قسم البوابة: رابط «البوابة» في بطاقة المستأجر كان يشير إلى **مرساةٍ غير موجودة**، فيهبط القارئ في أعلى صفحة طويلة **ويبحث عن القسم الذي أُرسل إليه**.

---

## 2026-08-18

### ✉️ ميزة — المنصّة ترسل دعوة البوابة بنفسها، وتعرض أين وصلت

**كان المكتب ينسخ رابطاً من صندوق ويرسله بالواتساب، ثم لا يعرف شيئاً بعدها.** الآن زرٌّ يُرسل بريداً فعلياً — والقناة تعمل منذ ١١ أغسطس — **وبطاقةٌ تقول الحالة**.

| ولكل حالة **جملة تقول الخطوة التالية**، لا اسمها فقط |
|---|
| «فُتح الرابط ولم يكتمل الربط — **قد يكون سجّل الدخول ببريد آخر**» تعني شيئاً · و«فُتح» وحدها لا تعني |

**وزرّ الدعوة خرج من بطاقة القائمة** إلى صفحة المستأجر: **الإرسال صار يحتاج عنواناً ويُخرج رسالة**، فمكانه حيث يظهر بريد المستأجر.

> **وإن أنشأت القاعدة الدعوة ثم أخفق المزوّد، يُقال ذلك صراحةً** — «أُنشئت ولم تُرسَل» — **ولا تُسجَّل «أُرسلت»**. رسالة فشلٍ عامّة كانت ستدفع المكتب للضغط ثانيةً، **فيدوّر رابطاً لم يُرسل أصلاً**.

**وقالب البريد يقول للمستأجر إنّ الدعوة لا تُقبل من بريد آخر.** **القارئ الذي يعرف القاعدة هو الذي يلاحظ حين تُخرق** — وهي الرسالة الوحيدة عندنا التي تحمل رابطاً، بخلاف رمز الدخول الذي **لا رابط فيه عمداً**.

**وفكّ الارتباط بواجهةٍ الآن**، بسببٍ مطلوب يُكتب في سجل التدقيق: بدونه **ملفٌّ ارتبط بحسابٍ خاطئ لا يُسترجع أبداً**.

### 🗓️ بيانات — دورة حياة الدعوة (`0075`)

```
pending → sent → opened → accepted        (وإلى expired بالتاريخ)
             ↘ revoked  (سحبها المكتب)
             ↘ superseded  (تغيّرت جهة الاتصال)
```

| القرار | السبب |
|---|---|
| **دعوة حيّة واحدة لكل ملف** (فهرس فريد جزئي) | رمزان حيّان **بابٌ أوسع بلا مقابل** — و«أيّ رابطٍ أرسلتَ لي؟» مكالمة دعم لا يحتاجها أحد |
| **تغيّر البريد أو الجوال يُبطل ما في الطريق** | الدعوة موجَّهة إلى عنوانٍ بعينه، **وقد يكون التصحيح نفسه سببه أن العنوان كان خطأ** |
| **`sent_to` منفصل عن `invitation.email`** | السجل يُعدَّل بعد الإرسال، **و«إلى أين أرسلنا فعلاً» يجب ألّا يتغيّر معه** |
| **`mark_invitation_opened` لا تُرجع شيئاً** | لو أجابت **لصارت أداةً لسبر الرموز**: يُعرَف الحيُّ من الميت بلا محاولة قبول |
| **حالة الدعوة لا تحمل الرمز ولا بصمته** | تجيب «أين وصل هذا»، **والرابط يُعرض مرّة واحدة حين يُنشأ** |

> **وقبل الفهرس ترميم:** الإنتاج كان يحمل ملفاً **بدعوتين حيّتين**، لأن لا شيء كان يمنع ضغطةً ثانية. الأحدث يبقى والأقدم يُتقاعد **`superseded` لا `revoked`** — **لم يسحبها أحد، استُبدلت**.

### 🔻 إصلاح — انحدارٌ أدخلتُه في `0074` وأمسكه الاختبار

أعدتُ كتابة `operator_set_setting` **نقلاً عن `schema_all.sql`** — وهي لقطة **أقدم من `0055`** — فأعدتُ قبول **تجربةٍ بصفر يوم**، وهي تُنشئ مكتباً **مقفلاً لحظة إنشائه**.

**أمسكه `platform.mjs`.** والحدّ الأدنى `1` كما قرّرته `0055`.

> **والدرس مكتوب بجانب الشرط في الملف:** `schema_all.sql` **لقطة مُولَّدة، والهجرات هي المصدر**. وأعدتُ كتابة دالّة كاملة لأضيف شرطاً واحداً، وكان يكفي أن أقرأ آخر هجرة مسّتها.

---

## 2026-08-17

### 🔐 أمان — من يصير مستأجراً: إغلاق مسار ربط مفتوح (`0074`)

**`app.link_party_identity` كانت ممنوحة لكل حساب مسجَّل منذ `0013`**، وتقبل **معرّف طرفٍ يختاره المتصل** مع **أيّ رمز دعوة حيّ في المكتب**:

| لا تفحص | الأثر |
|---|---|
| **نوع الدعوة** | رمز دعوة **فريق** يصلح |
| **أن الدعوة تخصّ ذلك الطرف** | يربط نفسه **بأيّ مالك أو مستأجر** في المكتب |
| **أن الطرف غير مرتبط** | **ينتزع ملفاً مرتبطاً بحسابٍ آخر** |

**وكان ذلك يسرّب قراءةً حتى ١٤ أغسطس، ثم صار يكتب:** بوابة المستأجر تقدّم طلبات صيانة **باسم صاحب الملف**.

**حُذفت لا قُيّدت** — لم يستدعها المنتج قطّ. **ومستدعيها الوحيد سطرٌ في الاختبارات كان يوثّق الثغرة ويسمّيها «المسار المصرَّح به»**: يُنشئ دعوة فريق ثم يربط بها طرفاً مستأجراً ويؤكّد النجاح. استُبدل بأربعة عشر اختباراً.

**والقبول لا يقع الآن إلا بأربعة:** دعوة بوابة حيّة · تحمل الطرف نفسه · طرفٌ غير مرتبط · **وعنوان حسابٍ يطابق عنوان الدعوة**.

> **وأُبقي `owner_portal` مع `tenant_portal`** — بوابة المالك تستدعي الدالّة نفسها، **وحصرها بالمستأجر كان سيكسر ربط الملّاك بصمت**.
>
> **والقراءة من `request.jwt.claims` لا من `app.identity`**: تسجيلٌ ببريدٍ فقط **لا يُنشئ سجلّ هوية أصلاً** (الجدول يشترط جوالاً سعودياً)، والمطالبة في الرمز **هي ما أثبته مزوّد الدخول فعلاً**.

**والقناة صارت إعداداً لا شيفرة:** `portal_identity_channel` بقيمه الثلاث (بريد · جوال · أيّهما) يُبدَّل من لوحة الإدارة — **فيوم يعمل مزوّد الرسائل لا ننتظر إصداراً**.

---

## 2026-08-14

### 🔧 ميزة — طلبات الصيانة (`0072`): أوّل ما يكتبه المستأجر عندنا

من الماكيت إلى وحدةٍ كاملة: **بوابة المستأجر تُقدّم**، **وشاشة المكتب تفرز وتُعيّن وتُغلق**، وبند الشريط الجانبي خرج من **«قريباً»** إلى قسم فعّال **بعدّاد المفتوحة**.

> ⚠️ **وهذه أوّل كتابة يملكها مستأجر في النظام.** فالمدخل **دالّة واحدة** تُثبت العقد النشط والوصف والحدّ اليومي — **وليست سياسة `insert`**: السياسة بابٌ **قد تُوسّعه سياسةٌ لاحقة بالخطأ**.

| القرار | السبب |
|---|---|
| **قائمة ومفتّش، لا صفحة لكل طلب** | الطلب **كائن عملية ينتهي بحلّه** — والاختيار في العنوان ليُرسَل رابطه لزميل |
| **الترتيب بالأهمية ثم بالأقدم** | **الطابور يُقرأ من أعلاه تحت الضغط** |
| **الإغلاق يشترط ملاحظة** | إغلاقٌ بلا جواب يجعل الطلب **غير قابل للسؤال عنه بعد ثلاثة أشهر** |
| **العاجل وحده يُشعِر** | **إشعارٌ لكل شيء هو إشعارٌ لا شيء** |
| **حدّ خمسة طلبات يومياً** | بدونه يصير النموذج **قناة تكرار**، فيسقط الطابور من كونه مقروءاً |
| **من يتحمّل التكلفة يُسجَّل ولا يُرحَّل** | ربطُه بكشف المالك **يمسّ عمولة الإدارة** — قرارٌ محاسبي مؤجَّل بذاته |

**ونموذج المستأجر أربعة حقول عمداً:** كل حقل زائد سببٌ لعدم الإبلاغ، **وتسرّبٌ لا يُبلَّغ أغلى على المكتب من تسرّبٍ صُنِّف خطأ**. **وجوابه يبقى على الشاشة** ولا يختفي كإشعار عابر — هو يحتاج أن يعرف أن الطلب **وصل**.

> **و`0073` هجرة صغيرة** أضافت `tenant_portal_units`: النموذج كان **يعرف اسم الوحدة ولا يعرف معرّفها**، وRLS تُخفي جدول الوحدات عن المستأجر **بحقّ**. دالّةٌ مستقلّة، **لا تعديلٌ في دالّةٍ تستدعيها ثلاث شاشات**.

### 🚀 ميزة — قائمة الإعداد الأولي على لوحة المؤشرات

خمس خطوات بشريط تقدّم، **وحالتها مُشتقّة من البيانات لا مخزّنة**: أربعة عدّادات تسأل الوحدات والعقود والأعضاء.

> **قائمةٌ لها جدولها الخاص تكذب أوّل مرّة** يُضاف عقار بالاستيراد أو يُحذف العقد الوحيد. **والاشتقاق لا يستطيع أن يكذب.**

**وزرٌّ واحد للخطوة التالية وحدها** — خمسة أزرار معاً **قائمة خيارات لا خطوة تالية**. **وتختفي عند الاكتمال**، لأن لافتةً دائمة تهنّئ مكتباً يعمل **تُقرأ عنصراً في الصفحة لا مهمّة**.

---

## 2026-08-13

### 🛡️ إصلاح — كتابةٌ ترفضها الصلاحيات كانت تُبلَّغ نجاحاً

**مراجعة شيفرة الشهر كشفت أخطر شكل من أشكال الكذب في واجهة:** رسالة نجاح على فعل لم يقع.

**والسبب أن سياسة الحماية (RLS) لا تُصدر خطأً حين ترفض — تُطابق صفر صفوف.** فالفعل الذي يفحص `error` وحده يمضي كأن شيئاً لم يكن:

| الحالة | ما كان يراه المستخدم |
|---|---|
| **عضو محصور بعقارات معيّنة** يعدّل وحدة خارج نطاقه | الدرج يُغلق ويقول **«حُفظت الوحدة»** |
| **غير المدير** يغيّر دور عضو في الفريق | **«حُفظ الدور»** — والدور كما هو |

**والقاعدة كانت مكتوبة في المشروع ومطبَّقة في صفحة واحدة فقط** (الإعدادات). فصارت مشتركة في `lib/rpc-errors`: كل `UPDATE` يطلب `.select("id")`، **وصفر صفوف = رفض**. طُبِّق على العقارات والوحدات والفريق والمرافق والمستأجرين والملّاك.

> **والجملة تذكر السببين معاً** — «خارج نطاق صلاحيتك، أو لم يعد موجوداً» — لأننا **لا نملك من هنا ما يميّزهما**، وتخمين أحدهما أسوأ من ذكرهما.

**وقراران داخل نطاق الفريق:**

- **تغيير نطاق العضو يتوقّف عند الرفض قبل إعادة كتابة صفوف النطاق** — وإلا بقي العضو **بأذونات لا تطابق رايته**، وهو أسوأ من الرفض نفسه.
- **إلغاء الدعوة يقول سببه الأرجح:** «قُبلت بالفعل» — لأن صفر الصفوف هناك **سباقٌ مشروع** لا رفض صلاحية.

**وأزرار التأكيد صارت تعرف أنها تعمل.** `ConfirmButton` يقرأ حالة نموذجه بنفسه (`useFormStatus`) ويُعطَّل أثناء التنفيذ — فلا تُرسَل ضغطةٌ ثانية على **إنهاء عقد** أو **تراجع استيراد** أو **حذف عقار**. القاعدة كانت ترفض الثانية، **لكن المكتب يقرأ رفضها على أنه فشل الأولى**.

### 📐 إصلاح — الإشغال يُعرَّف مرّة واحدة

كان محسوباً في موضعين **يختلفان عند عقار بلا وحدات**: البطاقة تقول **٠٪** والتقرير يقول **شرطة**. صار في `lib/occupancy` بتعريف واحد: **المقام كل الوحدات** — لأن وحدة الصيانة وحدةٌ لا تُحصَّل عليها — **وصفر وحدات يعني «لا نسبة» لا «صفر بالمئة»**.

> **وثالثة ظهرت في الطريق:** تقرير وحدات العقار — الذي يُرسل للمالك — كان يعرض العددين **ولا يعرض النسبة**. صار يعرضها بنفس القاعدة.

وانتقلت `HOLDING_TYPE_AR` إلى `lib/labels` مع بقية خرائط التسميات، بعد أن كانت داخل مكوّن.

### 🗂️ تغيير — قائمة العقارات بطاقات، والبيانات الناقصة تُرى بعلامة

**العقارات كانت آخر قائمة ما زالت جدولاً** بينما الملّاك والمستأجرون والعقود بطاقات — فصارت بنفس النظام.

**وعمودان منها كانا عملاً ذهنياً:** «الوحدات» و«شاغرة» رقمان **يُطرح أحدهما من الآخر في كل صفّ**. صارا **شريط إشغال** واحداً — والمكتب يمسح عشرين عقاراً بحثاً عن **الشريط القصير**، لا عن رقم.

**ورقم الصك أُسقط من القائمة، وعُمِّمت الفكرة على غيره.** لا أحد يقرأ رقم صكّ من قائمة؛ إنما ينظر ليعرف **هل يوجد**. فصار السطر أربع علامات: **صك · مالك · كود · مساحات الوحدات**.

> **ولم يُعتمد اللون وحده:** كل علامة تحمل نصّها لقارئ الشاشة (`مسجّل` / `غير مسجّل`)، **وتلميحاً يقول ما الناقص بالضبط** — «٣ وحدات بلا مساحة». **علامة حمراء بلا سبب تُقلق ولا تُرشد.**

**وتبويب «بلا وحدات» حلّ محلّ عمود كان يعتذر.** كان في كل صفّ نصٌّ رمادي «لا يُحذف»؛ والآن العقار الناقص **تبويبٌ يُفتح**، وهو وحده الذي يحمل خيار الحذف — **بنفس حارس 0067**، فلا خيار يفشل دائماً.

ويظهر **الإيجار التعاقدي السنوي** على البطاقة حين يوجد عقد نشط، **بنفس تعريف تقرير المحفظة** حرفياً: مجموع العقود النشطة لا المحصَّل. وبطاقة «غير مكتملة» في أعلى الصفحة صارت **«بلا مساحة»** — الاسم القديم كان يدعو القارئ أن يخمّن أيّ حقل يعدّه.

> **استعلامان للصفحة كلها، لا استعلامان لكل بطاقة.** الوحدات والعقود النشطة تُقرأ مرّة واحدة لمعرّفات الصفحة المعروضة، ثم تُوزَّع في الذاكرة.

### 📄 ميزة — تقريرا العقارات والوحدات، وتصدير الوحدات إلى Excel

**طُلب «زرّ يطبع كل العقارات، بصيغة Excel أو PDF»، ولم تكن الصيغتان خياراً من قائمة واحدة** — هما جوابان لسؤالين:

| | **Excel** | **التقرير المطبوع** |
|---|---|---|
| **لمن** | الموظّف داخل المكتب | **المالك أو مدير المحفظة** خارجه |
| **ما يفعله به** | يفرز ويجمع | يقرأ ويحفظ ويسلّم |

**ولم يُطبع جدول العقارات كما هو.** المالك يعرف أسماء عقاراته؛ سؤاله هو الإشغال وما تعاقدت عليه المحفظة — فصار **لكل عقار سطر**: الوحدات · مؤجّرة · شاغرة · الإشغال · الإيجار التعاقدي السنوي، وتحته **صفّ إجماليات**.

**وتقرير وحدات العقار** كشفُ حالةٍ وحدةً وحدة، ومعه المستأجر الحالي ونهاية العقد من العقد النشط.

> **ولا معلومات مالك في التقريرين** (قرار المالك صراحةً)، **وتقرير المحفظة لا يذكر مستأجراً** كذلك — **الورقة تسافر أبعد من الشاشة التي طُبعت منها.**

**وقراران في الحساب مكتوبان في ذيل كل تقرير**، لأن رقماً بلا تعريفه يُقرأ خطأً:

- **الإشغال = المؤجّرة ÷ إجمالي الوحدات** — لا ÷ (مؤجّرة + شاغرة). وحدات الصيانة وخارج الخدمة **وحداتٌ لا تُحصَّل**، وإخراجها من المقام يُجمّل الرقم.
- **«الإيجار التعاقدي السنوي» مجموع العقود النشطة**، لا المحصَّل فعلاً.

**وتصدير الوحدات سطرٌ واحد في `LIST_SPECS`** بمرشّح `property`، فمرّ على مسار التصدير القائم بنفس RLS وبنفس سقف الصفوف — **لا مسار جديد ولا صلاحية جديدة**. والطباعة بنمط `PrintButton` + `print-sheet` الذي تعمل به العقود والسندات: **لا مكتبة PDF على الخادم، ولا خطوط عربية مُضمَّنة، ولا مشكلة تشكيل حروف** — مقابل صفر فائدة على ورقة يطبعها المستخدم بنفسه.

### 💬 تغيير — الرسائل انتقلت إلى موضع الفعل في **كل** صفحات النظام

القاعدة التي اعتُمدت أمس في الإعدادات سرت اليوم على التسع الباقية، **خمس دفعات، كلٌّ منها بناء ودفع مستقل**: الفريق · المرافق · الملّاك والمستأجرون والعقارات · الفواتير والاستيراد والخصوصية · **العقود**.

**والنتيجة الواحدة:** لم يعد في النظام رسالةٌ تُحمَل في عنوان الصفحة. كان `?error=` يُعيد تحميل الصفحة، **فيمحو ما كُتب قبل أن يقرأه أحد**.

| الشكل | متى |
|---|---|
| **تحت الحقل، والمُدخَل باقٍ** | نموذج له حقول — العقود، الملّاك، المستأجرون، الإعدادات |
| **إشعار عابر جوار الزر** | زرّ في صفّ لا حقل له ولا شيء يُفقد — الفريق، المرافق، الاستيراد |
| **جواب يبقى على الشاشة** | حذف بيانات PDPL: عدد الفواتير المحتفَظ بها **هو ما يبلّغه المكتب لصاحب البيانات**، فلا يليق به الاختفاء بعد ثوانٍ |

**والعقود آخرها وأثقلها** — ثمانية أفعال في المسار الذي يحرّك المال. أشدّها **تعديل المسودة**: عشرون حقلاً، كان رفضُ تاريخٍ واحد يرتدّ بالتسعة عشر الباقية إلى قيمها المخزّنة.

> **وإصدار الفاتورة وإنشاء عقد التجديد يبقيان إعادةَ توجيه عند النجاح**، وهذا ليس استثناءً من القاعدة: كلاهما **وجهة تُقرأ**، لا رسالة تُقال.

### 🛡️ إصلاح — حارسان ناقصان كشفهما التحويل

قراءة كل زرّ سطراً سطراً أظهرت فعلين لا رجعة فيهما بلا سؤال:

- **التراجع عن دفعة استيراد** — يحذف كل ما أنشأته الدفعة.
- **الإنهاء المبكر للعقد** — يلغي كل استحقاق مستقبلي غير مدفوع ويُحرّر الوحدة، والعقد لا يعود «نشطاً» بتعديل.

**كلاهما يسأل الآن قبل أن يفعل.** وفي المقابل رُفض حذف ثلاثة تأكيدات كانت قائمة في صفحات المرافق: **نقلُ الرسالة تغييرٌ في الواجهة، وإزالةُ حارس ليست كذلك.**

### ➖ قرار — ثلاثة مواضع تبقى على النمط القديم، عن قصد

| | |
|---|---|
| `portal/join` | خطأ **رمز الدعوة** خطأ صفحةٍ لا خطأ حقل — لا يوجد نموذج يُصحَّح |
| `subscription` | الرسالة تأتي **من بوابة الدفع** عند العودة، لا من نموذج عندنا |
| الأرشفة المشتركة (`archiveRecord`) | فعل واحد تستعمله عدّة صفحات — **وهو وحده سبب بقاء شريطَي المالك والمستأجر** |

---

## 2026-08-12

### ✉️ تغيير — رمز البريد يتقدّم تطبيق المصادقة

كان الأقوى يفوز، فتطبيق المصادقة يُطلب متى كان الاثنان مُفعَّلين. **وقرار المالك عكسها:** أغلب المستخدمين مكاتب عقارية، والبريد مفتوح على الشاشة نفسها، **والرمز الذي يُنسخ ويُلصق هو الخطوة التي تُنجَز فعلاً**. والتطبيق يبقى **رابطاً** في أسفل الشاشة، لا إلزاماً — ورابطُه مشروط بأن يكون العاملان صالحين، فبدونه يعلق من لا يستطيع فتح بريده الآن.

> **وملاحظة أمنية:** الوسيط لم يكن يفرض الأسبقية القديمة أصلاً — يقبل **أيّ** إثبات على الجلسة، وصفحة التحقّق وحدها كانت تختار. فالتغيير يجعل المعروض **مطابقاً** لما يقبله النظام، لا أوسع منه.

### 💬 تغيير — الرسائل تحت حقولها، والمُدخَل لا يضيع (صفحة الإعدادات كاملة)

**السبب من الميدان:** كتب مكتبٌ بريداً مسجَّلاً لحساب آخر، فظهر الرفض في **أعلى صفحة طويلة** وهو يعمل في أسفلها — فبدا أن «زر التعديل لا يعمل».

**واقتُرحت نافذة منبثقة، ولم تكن الجواب:** النافذة تحجب الحقل الذي يريد إصلاحه، وتُعلّم الإغلاق الانعكاسي **فتُضعف نافذة الحذف الحقيقية**. والقاعدة التي اعتُمدت بدلها ثلاث مراتب: **خطأ الحقل تحته** · **النجاح إشعار عابر** · **والنافذة للأفعال التي لا رجعة فيها وحدها**، حيث الحجب هو الغرض.

**وسببان مستقلّان لعطل واحد، ولم يظهر الثاني إلا بالفحص البصري على الإنتاج:**

| | |
|---|---|
| `redirect` يُعيد تحميل الصفحة | أُزيل — الأفعال تُرجع حالتها |
| **React 19 يُصفّر النموذج** بعد انتهاء الـ`action` | لم يُكتشف إلا بعد أن ظهرت الرسالة صحيحة **والحقل تحتها فارغ**. فصار كل رفض يُعيد ما كُتب، والحقول تتّخذه قيمتها الافتراضية |

> **ولا `tsc` ولا البناء يريان شيئاً من هذا** — كلاهما أخضر في الحالتين. الفحص البصري وحده أمسكه.

**والصفحة الآن بلا شريط علويّ إطلاقاً**، ولا `?error=` في عنوانها. وأُخرج المشترك بعد أن صار له مستعملان: `lib/form-state.ts` و`components/form-field.tsx`. وتبقى **تسع صفحات** على النمط القديم، تُنقل واحدة مع كل زيارة لسبب آخر — لا حملة واحدة بلا اختبارات واجهة تحميها.

### 🔄 ميزة — تنبيه «صدر تحديث» قبل أن يبتلع النشرُ ما يكتبه المكتب

صادفَنا **مرّتين** أثناء الفحص: ينزل نشر جديد والمستخدم يملأ نموذجاً، فيكتشف Next أن البناء تغيّر ويُعيد التحميل بنفسه — وأحياناً يسبق ذلك **«Application error»**. صحيحٌ في ذاته، ويقع في منتصف الكتابة.

`NEXT_PUBLIC_BUILD_ID` يُخبز في الحزمة وقت البناء، و`/api/version` يقرأ المتغيّر نفسه **وقت التشغيل** — فيجيب من النشر الجديد بمعرّف يخالف ما تحمله الصفحة المفتوحة. **ويصمت متى عجز عن الجزم:** معرّف فارغ على أيّ طرف = لا شريط، لأن تنبيهاً كاذباً يُعلّم تجاهل التنبيه الصادق.

---

## 2026-08-11

### ✅ إغلاق — البريد يعمل لأول مرّة، والقاعدة `٧١ من ٧١`

**متغيّرات البيئة الثلاثة أُصلحت وتحقّق منها المِجَسّ نفسه**، ثم وصل رمز التفعيل فعلاً. وهذا أوّل بريد تُرسله المنصّة **منذ الإطلاق**.

**و`/platform/billing` فُحص ونتيجته نظيفة:** لا دفعة عالقة، ولا مكتب دفع ولم يُفعَّل اشتراكه. فترة عطل مفتاح الخدمة مرّت **قبل دخول مدفوعات حقيقية**.

### 🔀 دمج — وإعادة ترقيم: رقمان `0070` لهجرتين مختلفتين

عمل جهازان بالتوازي (هذا الجهاز، وحاوية claude.ai) فأنتج كلٌّ منهما هجرة `0070`: **حدّ عُمر الطابور** هنا، و**طريق العودة** هناك.

**ولماذا لم يكن إزعاجاً فقط:** `record_migration` تكتب `on conflict (version) do update set name = excluded.name`. فتطبيق الثانية كان **يكتب فوق سطر الأولى بصمت** — يبقى أثر الهجرتين في القاعدة، ولا يذكر السجلّ إلا واحدة، **وتقول `/platform/health` إن كل شيء بخير**. وهو الكذب بعينه الذي بُني السجلّ لإنهائه يوم كانت `0029` غائبة.

**والقاعدة التي حسمت الاختيار: ما طُبِّق على الإنتاج لا يُعاد ترقيمه.** فبقي `0070` حدَّ عُمر الطابور، وصار الاسترداد **`0071`** — ومعه **كل إشارة إليه**: سطر التسجيل، وترويسة الملفّ، وأربعة عشر تعليقاً في الشيفرة والاختبارات والدليل. **فرقمٌ في تعليق يكذب هو طريق الالتباس القادم.**

> والتعارض الفعليّ كان في ملفّين **مولَّدين** فقط (`schema_all.sql` و`lib/migrations.ts`) فأُعيد توليدهما؛ أمّا الشيفرة فاندمجت وحدها.

### 🔴 إصلاح — منفذان متصادمان في الاختبارات، وحارس يرفض تكرارهما

**نفس السبب، في مكان آخر:** `email-backlog.mjs` و`mfa-recovery.mjs` حجزتا المنفذ `54362` نفسه — لأن كلاًّ منهما اختار **«الرقم التالي الفارغ» في القائمة التي يراها**.

**والتصادم لا يبدو تصادماً:** المجموعات تعمل واحدة تلو الأخرى، **فالثانية تُقلع على قاعدة تركتها الأولى وتسقط بتوكيدات لا علاقة لها بالمنفذ**. ساعة تشخيص مقابل سطرين يرفضان مقدَّماً — فصار `run-all` يقرأ رقم المنفذ من كل مجموعة **ويرفض التشغيل** إن تكرّر، ذاكراً اسمَي الملفّين.

**ولا حارس بعدُ لأرقام الهجرات.** فقبل ترقيم هجرة جديدة:

```bash
git fetch && git log --all --name-only | grep migrations
```

**النتيجة:** ١٨ مجموعة خضراء · `tsc` نظيف · البناء نجح.

---

## 2026-08-09

### 🔑 ميزة — طريق العودة: رموز استرداد، ورمز بريد للطوارئ (`0071`)

**المشكلة:** فريق الاختبار عَلِق خارج حسابه. وقراءة الكود أعطت السبب كاملاً: `middleware` يحوّل كل جلسة لم تتحقّق إلى `/auth/mfa`، وتلك الصفحة لا تقبل إلا العامل نفسه، **وإلغاء العامل يشترط إثبات العامل**. أي أنّ **جوالاً ضائعاً كان يعني حساباً ميّتاً** — لا استرداد، ولا إعادة تعيين، ولا شيء في المنتج يقول ذلك.

**مخرجان، وليسا متساويين — والفرق هو جوهر التصميم:**

| | **رموز الاسترداد** | **رمز البريد للطوارئ** |
|---|---|---|
| ما هو | ١٠ رموز من ٥٠ بت، تظهر مرّة، تُخزَّن مهشّرة، كلٌّ لمرّة | رمز ٦ أرقام إلى بريد الحساب |
| قوّته | **مساوٍ للعامل** الذي يحلّ محلّه | **أضعف** — من يفتح بريدك يعيد تعيين كلمة مرورك أصلاً |
| ما يفتحه | **النظام كاملاً** | **صفحة الأمان فقط** (جلسة مقيّدة) |

الجلسة المقيّدة (`method = 'email_fallback'`) هي الفكرة التي تمنع الطريق الأضعف من أن يصير — بصمت — مستوى أمان الحساب الحقيقي، فتتحوّل كل تطبيقات المصادقة في المنتج إلى زينة. تفتح ما يكفي لإزالة العامل المفقود أو تسجيل غيره، ولا شيء بعده. وبمجرّد تسجيل عامل جديد يُفتح النظام كاملاً.

**ما لا يستطيعه أحد** (٣١ اختباراً في `mfa-recovery.mjs`، وكلها تمرّ):
- صرف الرمز مرّتين، أو صرف قائمة شخص آخر.
- **طباعة رموز جديدة من جلسة مقيّدة** أو من كلمة مرور وحدها — طلبٌ يحتاج إثباتاً حقيقياً (`app.mfa_proved`).
- **تخفيض** جلسة أثبتت العامل عبر إرسال رمز بريد بعدها. الترقية تسير في اتجاه واحد.
- استبدال رمز خطوة‑بخطوتين برمز استرداد — لكلٍّ `purpose` منفصل.
- التمييز بين «رمز خاطئ» و«رمز مستهلك»: جواب واحد، فالتمييز يؤكّد للمهاجم أنّ تخمينه أصاب.

**والملفّات:** `supabase/migrations/0071_mfa_recovery.sql` · `middleware.ts` · `app/auth/mfa/*` · `app/app/security/*` · `lib/{otp,mfa,mfa-server}.ts` · `components/{recovery-codes,mfa-recovery-options}.tsx` · دليل المستخدم `19`.

**ويبقى معلّقاً** من دراسة الحبس: إعادة التعيين الإدارية من لوحة المكتب، وسياسة «إلزام التحقّق» بمنطق «سجّل قبل المتابعة»، ومخرج المنصّة بمهلة تبريد لمالك المكتب الوحيد.

---

## 2026-08-08

### ميزة — حدّ عُمر داخل طابور البريد (هجرة `0070`): الطابور العائد لا يُغرق العميل بالماضي

`claim_email_deliveries` (هجرة `0038`) كانت تأخذ **كل صفّ معلّق، الأقدم أولاً، بلا أي مفهوم للعُمر**. وهذا صحيح حين يُصرَّف الطابور كل دقائق، **وخاطئ في اللحظة التي يتوقّف فيها**: فأول تشغيل ناجح بعد إصلاح متغيّرات البيئة كان سيبدأ بمراسلة المكاتب عن **أقساط سُدّدت من شهور** وعقود **انتهت وجُدّدت**. صحيحة يوم كُتبت، خاطئة يوم تصل.

| القرار | لماذا |
|---|---|
| **الحدّ سبعة أيام** | التذكير المتأخّر يستحقّ الإرسال؛ المتأخّر **أسبوعاً** لا يستحقّ — فالمكتب إمّا سدّد أو طالب بنفسه |
| **ثابت لا مُعامل** | لا مُستدعٍ ثانٍ يمرّر رقماً مختلفاً، **ومِقبضٌ لا يُدار مِقبضٌ يبلى** |
| **تُشطَب لا تُتخطَّى** | لو رُشِّحت فقط لبقيت `pending` **إلى الأبد**، ولظلّ عدّاد «بريد بانتظار الإرسال» يعدّ رسائل **لن تُرسَل أبداً**. ورقمٌ لا يُبنى عليه عمل أسوأ من لا رقم |
| **الإشعار نفسه سليم** | يُلغى **التسليم بالبريد** فقط؛ الإشعار باقٍ داخل التطبيق والمكتب يراه |

**وأثر جانبي مقصود:** تطبيق `0070` **يُصفّي المتراكم الحالي تلقائياً** عند أول تشغيل — فلا حاجة إلى SQL يدويّ.

**الاختبارات:** `email-backlog.mjs` — ١٠ فحوص، حدودها: يومٌ يمرّ، **وستّة أيام تمرّ أيضاً**، وثمانية تُشطب.

### 🔴 اكتشاف — نظامان معطّلان في الإنتاج منذ أسابيع، ولا شيء كان يقول ذلك

كشفهما أوّل ضغطة على «أرسل الرمز»، في سجلّ Vercel:

```
[rate-limit] mfa-otp-send:ip check failed, allowing through: Invalid API key
[mfa] could not send code: email provider not configured (RESEND_API_KEY / EMAIL_FROM)
```

| المعطّل | ما كان متوقّفاً معه بصمت |
|---|---|
| **`SUPABASE_SERVICE_ROLE_KEY` باطل** | كل مسار `service_role`: تصريف البريد، تجديد الاشتراكات، **إشعارات بوابة الدفع**. و**حدّ محاولات الدخول مُعطَّل تماماً** — لأنه «يفشل مفتوحاً» بالتصميم، فكان يمرّر كل شيء ويكتب سطراً في سجلّ لا يقرأه أحد |
| **`RESEND_API_KEY`/`EMAIL_FROM` غير مضبوطين** | **لم تخرج رسالة بريد واحدة من المنصّة قط.** والإشعارات تتراكم في `email_outbox` بلا شكوى، لأن المُصرِّف الوحيد يمرّ على cron المعطّل أصلاً |

**ولماذا لم يظهرا:** كلاهما **يفشل بلطف** — واحد يمرّر، والآخر يُدخِل في طابور. والفشل اللطيف بلا مقياس هو **فشل غير مرئيّ**.

### ميزة — «الخدمات المرتبطة» في `/platform/health`: نسأل بدل أن نفترض

كان في الصفحة بطاقة اسمها «خارج قياسنا» تقول بصراحة إن هذه الأنظمة **غير مرصودة**، وتصف نفسها بأنها «قائمة المهام لتركيب مجسّات حقيقية». رُكِّبت الآن.

| المِجَسّ | كيف يفحص |
|---|---|
| **مفتاح الخدمة** | **باستعماله فعلاً** — يستدعي `rate_limit_hit` المحصورة بـ`service_role`، وهي بعينها النداء الذي كان يفشل. لا فحص وجودٍ يكذب على نفسه |
| **مزوّد البريد** | يسأل Resend عن الحساب (`GET /domains`) — **بلا إرسال رسالة**، فمِجَسٌّ يُرسِل بريداً مِجَسٌّ لا يُشغَّل مرّتين. ويميّز **المفتاح الباطل** من **المفتاح الصالح بلا نطاق مُوثَّق**، وهذا الثاني يرفض الإرسال لغير صاحب الحساب |
| **سرّ cron** | وجوده فقط، **ومكتوبٌ أن هذا كل ما يمكن قوله** — صحّته تظهر في جدول المهام أعلاه |

ولكل معطَّل **ثلاثة أسطر**: نصّ خطأ المزوّد حرفيّاً، وما يتوقّف بسببه، **وأين تُصلحه بالضبط**. ولا يُطبع أي سرّ — الصفحة للمشغّل وحده، والقيم لا تغادر الخادم.

**الملف:** `lib/integration-health.ts` · القسم في `app/platform/health/page.tsx`.

### ميزة — التحقّق بخطوتين برمز يصل على البريد (هجرة `0069`)

**السبب:** التحقّق بخطوتين كان **تطبيق مصادقة فقط**. وهو الأقوى، لكنه يطلب من مكتب عقاري أن يُنزّل تطبيقاً ويفهمه **قبل** أن يدخل حسابه — وهذه كلفة حُكم عليها بأنها أعلى من قيمتها. الرمز على البريد لا يطلب من المستخدم شيئاً جديداً.

**وما لا يفعله، مكتوباً في الواجهة نفسها:** يوقف **كلمة المرور المسروقة**، ولا يوقف **البريد المسروق** — فمن يقرأ البريد يستطيع أصلاً إعادة تعيين كلمة المرور. **تطبيق المصادقة باقٍ كما هو** لمن أراد إغلاق البابين، وهو **المقدَّم** حين يكون الاثنان مفعَّلين.

**القرارات التي تشكّل التصميم:**

| القرار | لماذا |
|---|---|
| **جداول عندنا، لا عوامل Supabase** | GoTrue يعرف TOTP والهاتف، **ولا يعرف عامل بريد**، فمستوى التوثيق (AAL) لا يستطيع وصفه أصلاً. فالحالة عندنا والبوّابة عندنا (`middleware` يسأل `app.mfa_state`). |
| **مُفهرَس بالقناة من أول سطر** | `channel in ('email','sms')`. فإضافة الرسائل النصية لاحقاً **وجهة ومُرسِل**، لا تصميم ثانٍ. |
| **الرمز لا يدخل القاعدة أبداً** | التطبيق يولّده ويُرسِل **بصمته** فقط. لا نسخة احتياطية ولا `SELECT` يكشف رمزاً حيّاً. |
| **مربوط بالجلسة، لا بالشخص** | رمز أُرسل لمتصفّح **لا يفتح متصفّحاً آخر**. `session_id` يُؤخذ من الرمز المميّز المُوثَّق نفسه، فلا يستطيع طالبٌ ادّعاء جلسة غيره. |
| **يُرسَل فوراً، خارج الطابور** | إشعارات البريد تخرج مع cron **٦ صباحاً**. ورمز دخول يصل صباح الغد ليس رمز دخول. |
| **رسالة بلا رابط، عمداً** | «اضغط لتأكيد دخولك» هو شكل رسالة التصيّد بالضبط. القارئ ينسخ ستّة أرقام إلى صفحة **فتحها هو**. |
| **الوجهة مُجمَّدة عند التفعيل** | لا تُقرأ حيّة من `identity.email`، وإلا **لحوّل تغييرُ البريد العاملَ الثاني بصمت** — وتغيير البريد هو بالضبط ما وُضع العامل الثاني ليعترضه. |
| **الإطفاء يحتاج إثباتاً** | `mfa_disable` يرفض جلسة لم تُدخل الرمز — وإلا فسارق كلمة المرور يُطفئ الحماية الموضوعة لأجل سرقتها. |

**الحدود:** صالح **١٠ دقائق** · **مرّة واحدة** · **٥ محاولات** ثم يُبطَل · **إعادة الإرسال تقتل السابق** (وإلا لوسّع كل «أرسل مجدداً» مجموعةَ الرموز التي تفتح الحساب) · وحدّ إرسال **٥ لكل حساب و١٠ لكل عنوان IP** كل ربع ساعة.

**الملفات:** `supabase/migrations/0069_email_mfa.sql` · `lib/otp.ts` · `lib/mfa-server.ts` · `lib/mfa.ts` · `lib/email/templates.ts` · `components/email-otp-form.tsx` · `components/email-mfa-enroll.tsx` · `app/app/security/*` · `app/auth/mfa/*` · `middleware.ts` · تنظيف يومي في `app/api/cron/drain-notifications`.

**الاختبارات:** `supabase/tests/local/email-mfa.mjs` — **٣٧ فحصاً**، أهمّها ليست «الرمز الصحيح يعمل» بل الطرق التي يتوقّف بها عن كونه عاملاً ثانياً: رمز يفتح جلسة أخرى، حساب يُفعّل عامل غيره، إعادة إرسال تُبقي القديم حيّاً، تخمين بلا نهاية، وإطفاء بلا إثبات.

**الدليل:** [١٩ — أمان الحساب](docs/user-guide/19-account-security.md) أُعيد كتابته بجدول يقارن الطريقين صراحةً.

---

## 2026-08-06

### 🔴 إصلاح — ثلاث ثغرات في CI: الشبكة التي نعتمد عليها كانت مثقوبة

كُشفت عند مراجعة الهيكل استعداداً لقرارات النقل والجوال. **الثلاث تجعل CI تقول «أخضر» وهي لم تفحص:**

**١) الاختبارات تركض جزئياً.**
```json
"verify": "node verify.mjs && node phase3.mjs"
```
**مجموعتان من خمس عشرة** — ١٦٠ تأكيداً من ٧٠٩. **و٥٤٩ لم تُشغَّل على GitHub قطّ**، ومنها كل الحرّاس التي كُتبت هذا الأسبوع.

**والسبب البنيوي قائمة يدوية**، فحلّ محلّها `run-all.mjs` **يكتشف المجموعات من المجلّد**: المجموعة الجديدة تدخل تلقائياً، ولا قائمة تُنسى. ويُشغّلها **واحدة تلو الأخرى** — كلٌّ منها تُقلع PostgreSQL على منفذ ثابت فتتصادم لو تزامنت — **ويسمّي الفاشلة** بدل ابتلاعها في علامة حمراء واحدة.

**٢) Node 20 في CI، والمجموعات `.mts` تحتاج 22.6+.** أربع مجموعات تعتمد تجريد الأنواع الأصلي بلا خطوة بناء، **وعلى 20 لا تُحلَّل أصلاً**. رُفع إلى 24.

**٣) `lib/migrations.ts` بلا فحص انحراف.** `build-schema.mjs` يولّد **ملفّين** وCI يفحص واحداً — **والملفّ غير المفحوص هو ما تقارن به `/platform/health`**، فنسخة قديمة منه تجعل فحص الاكتمال **يكذب بصمت**. وهو العطل نفسه الذي وُجد السجلّ ليُنهيه. صار مشمولاً.

> **والتحقّق:** ١٥ مجموعة، **٧٠٩ تأكيداً، كلّها خضراء** بالمُشغّل الجديد.

### تحسين — فصل `analysis/` إلى مستودع بحث مستقلّ

**١٥٤ ملفّاً و٧ ميغابايت** من تحليل المنافسين — **أكبر مجلّد في المستودع، أكبر من `app/` نفسه**، و**صفر استيراد** منه في الكود. كان يسافر مع كل استنساخ وكل checkout في CI بلا فائدة، ولا يُحدَّث مع المنتج (تاريخه ٢٠–٢١ يوليو).

**بحث، لا منتج.** نُقل إلى `aqar-analysis` بمستودعه ووثيقته، ومعه تنبيه أنه **خاصّ لا يُنشر**: فيه ٦٣ لقطة شاشة من مواقع شركات أخرى.

> **وما بقي:** الملفّات خرجت من شجرة العمل ومن الفهرس، **لكن الـ٧ ميغابايت باقية في تاريخ git**. تنظيفها يحتاج إعادة كتابة تاريخ `main` المدفوع — **ولا أنصح به** مقابل ٧ ميغابايت.

### إصلاح — `README` كان يقول «٣١ هجرة» و«الدخول بالجوال»

الرقم كُتب مرّة وبقي بينما صارت **٦٨**، والدخول **بالبريد وكلمة المرور** منذ Sprint E. **وأُزيل الرقم بدل تحديثه** — رقم يُكتب باليد سيَقدُم مرّة أخرى — وحلّ محلّه مؤشّر إلى المجلّد وإلى `/platform/health`. وحُدّثت قائمة «ما يعمل الآن» بما شُحن منذ يوليو.

---

## 2026-08-05

### 🔴 اكتشاف — `0051_platform_billing` كانت غائبة أيضاً، وجدها السجلّ في دقيقته الأولى

طُبّقت `0068` وفُتحت `/platform/health`، فقالت **«مطبَّق ٦٧ من ٦٨»** وسمّت الناقصة: **`0051_platform_billing`**.

**ولم يُؤخذ ذلك على عهدة المِجَسّ وحده.** فُتحت `/platform/billing` فقالت «طبّق الهجرة 0051 لتفعيل مركز الفوترة» — **فحصٌ كُتب قبل السجلّ بشهور ويقول الشيء نفسه**. مصدران مستقلّان، لا إنذار كاذب.

**ما كان معطّلاً:** مركز الفوترة · مركز الاشتراكات · تعديل الخطط والأسعار. **ولا أثر على المكاتب** — فوترة العملاء من `0036`/`0039`/`0040` وهي مطبَّقة. المعطّل كان **أدوات مشغّل المنصة**: لا تستطيع تعديل خطة مكتب ولا مراجعة مدفوعاته.

طُبّقت `0051` وأُعيد تشغيل `0068` (قابلة لإعادة التشغيل بالكامل)، فصارت **٦٨ من ٦٨** بشارة خضراء — **ومركز الفوترة يعمل**، وهو الدليل الذي لا يقوله السجلّ عن نفسه.

> **هجرتان غائبتان في يومين، من ٦٨.** `0029` كلّفت شهوراً واكتُشفت بالمصادفة، و`0051` اكتُشفت في ثانية. **ولولا السجلّ لبقيت مختبئة حتى يوم تحتاج تعديل خطة مكتب — غالباً أمام عميل.**

### إصلاح — دليل المستخدم كان يقول إن الدخول برقم الجوال

فصلا **[بوابة المالك](docs/user-guide/15-owner-portal.md)** و**[بوابة المستأجر](docs/user-guide/16-tenant-portal.md)** يقولان «سجّل الدخول برقم جوالك (رمز تحقق)» — **والدخول بالبريد وكلمة المرور منذ Sprint E**؛ مسار الجوال موجود في الكود لكنه خامل ولا يظهر في الواجهة. **مستأجر يتبع هذا الدليل يقف أمام شاشة لا تطابقه.** صُحّح الفصلان والفهرس.

وأُضيف إليهما ما تأكّد عملياً عند إصدار أول رابط حقيقي: **الرابط مفتاح لا إشعار** — من يفتحه يربط السجلّ بحسابه، فيُرسل إلى الجوال المسجَّل وحده؛ **والقاعدة تحفظ بصمته لا نصّه**، فلا يستطيع أحد استخراجه ثانيةً، لا المكتب ولا الدعم. وصُحّح أيضاً موضع الزرّ: **في بطاقة المستأجر**، لا في عمود.

### توثيق
- **أُزيلت قائمة «الهجرات المطبَّقة» من أعلى هذا الملف.** كذبت مرّتين، وحلّ محلّها مؤشّر إلى `/platform/health`. **قائمة تُكتب بالذاكرة ليست سجلّاً.**

---

## 2026-08-04

### ميزة — سجلّ الهجرات وفحص اكتمال القاعدة (`0068`)

**السبب:** `0029` لم تكن مطبَّقة، **وبوابة المستأجر بقيت معطّلة شهوراً**. ولم ينتبه أحد لأن `my_tenant_links` تُستدعى في الصفحة الرئيسية **ويُهمَل خطؤها** — فالهجرة الغائبة بدت كمستأجر بلا بوابة. والسجلّ الوحيد لما طُبّق كان **قائمة في `CHANGELOG` تُحدَّث من الذاكرة**، وقالت إنها مطبَّقة.

**الآن: كل هجرة تُسجّل نفسها** في سطرها الأخير — لا أحد يحتاج أن يتذكّر.

| | |
|---|---|
| `app.schema_migration` | ما تملكه القاعدة فعلاً |
| `lib/migrations.ts` | ما يتوقّعه الكود — **مولّد** من مجلّد الهجرات |
| `/platform/health` | يقارنهما ويعرض **الفرق بالأسماء لا بالعدد** |

**قرارات:**

**١) 🔍 البذر استُنتج من المخطّط لا من قائمة.** أن أبدأ بثقة «الـ٦٧ مطبَّقة» هو تكرار للخطأ نفسه بأثر رجعي. **فلكل هجرة مِجَسّ** يبحث عن الأثر الذي تركته — والهجرة التي يفشل مِجَسّها **لا تُسجَّل**، بل تظهر ناقصة. وهذه هي الطريقة التي كُشفت بها `0029` أصلاً.

**٢) وأربع هجرات لا يمكن التحقّق منها، فقيلت كما هي.** `0014` هدمتها `0032`، و`0046`/`0055`/`0056` **تُعيد إصدار دوالّ قائمة ولا تُنشئ شيئاً جديداً** — فلا مِجَسّ يميّز «مطبَّقة» من «غير مطبَّقة». تُسجَّل `verified = false`. **«لم نستطع فحص هذه» معلومة، لا نقص.** والصفحة تقولها.

**٣) الفحص بالاسم لا بالتوقيع.** `to_regprocedure` يحتاج قائمة الوسائط بالضبط، **ومِجَسّ يفشل لأن أحدهم أضاف وسيطاً افتراضياً كان سيُبلّغ عن هجرة مفقودة وهي موجودة** — وهو الشيء الوحيد الذي يجب ألّا يفعله هذا الجدول.

**٤) والسجلّ لا يُقرأ عبر PostgREST.** قائمة الهجرات الناقصة **خريطة للحرّاس الغائبين**؛ فالجدول بلا سياسة RLS، وقراءته عبر دالّة مقصورة على **مشغّلي المنصة**. و`record_migration` مُنِعت بالاسم عن `anon` و`authenticated` (قاعدة `0053`) وإلّا زوّر أي مستخدم صفّاً.

**٥) وقسم الهجرات فوق المؤشّرات لا تحتها** — مؤشّر على قاعدة ناقصة يقيس الشيء الخطأ.

### توثيق
- `SCHEMA.md` — قسم سجلّ الهجرات، **مع تنبيه أن جدول خريطة الملفات متوقّف عند `0045`** والمصدر الحيّ هو المجلّد.
- الاختبارات: **٧٠٩ خضراء** في ١٥ مجموعة (`migration-ledger` ١٧ جديدة — **تُثبت صحّة الـ٦٧ مِجَسّاً** على قاعدة مبنيّة من كل الهجرات).

### إصلاح — أربعة عيوب كشفتها التجربة الحيّة على بيانات المكتب

فُتحت شاشات اليوم على المنصّة الحيّة بعد تطبيق `0066` و`0067`. **الحسابات والحرّاس صحيحة كما شُحنت** — الخلل كله في اللغة والترتيب، وهو ما لا يكشفه اختبار.

**١) 🔴 «١٢ دفعات» — والعربية تقول «١٢ دفعة».**

عقد شهري بـ١٠٠٬٠٠٠ ر.س أظهر «ستُنشأ **12 دفعات**». والقاعدة العربية أربع صيغ لا اثنتان:

| العدد | الصيغة |
|---|---|
| ١ | عقد واحد |
| ٢ | عقدان *(مثنّى، كلمة أخرى)* |
| ٣–١٠ | ٣ عقود |
| **١١+** | **١٢ عقداً** *(مفرد منصوب)* |

**والفخّ هو الأخير**، لأنه يبدو تراجعاً لمن كتب الجمع. **وهذا ثاني ظهور لنفس الغلط** — سبقه «1 بنداً» في الخط الزمني — فنُقلت القاعدة إلى `lib/plural-ar.ts` وحدها، وحلّت محلّ **أربع نسخ يدوية** متفرّقة، **ولها الآن ٢٣ اختباراً**.

> **وتفصيلة نحوية بُنيت في التصميم:** المثنّى المخزَّن **مرفوع** («عقدان»)، وبعد حرف الجرّ أو الفعل المتعدّي يصير «عقدين» — ولا نصّ واحد يصلح للحالتين. **فالصياغة مسؤولية موضع الاستدعاء**: «عليه عقدان»، «له عقاران» — لا «مرتبط بـ» ولا «يملك». وأُعيدت صياغة الرسائل الثلاث على ذلك.

**٢) سطر رفض الحذف كان يفصل الاسم عن البيانات** في صفحة المستأجر، فيُقرأ كشارة حالة عن المستأجر لا كتعليل لزرّ. نُقل **أسفل الحقائق** — حيث تضعه صفحة المالك أصلاً؛ **والتناقض بين الصفحتين كان هو العيب**.

**٣) مربّع «عرض المؤجّرة» ينكسر سطرين** في لوحة عرضها ٤٠٠px، فتتدلّى «أيضاً» وحدها تحت مربّع لم تعد تلامسه. صار سطرين مستقلّين، والعبارة أقصر.

**٤) «Choose File / No file chosen» بالإنجليزية** وسط شاشة عربية بالكامل — وهي من المتصفّح لا منّا، ولا تُترجَم. `components/file-picker.tsx` يُخفي الحقل الأصلي (يبقى في النموذج ويحمل الملف) ويقوده زرّ عربي **يعرض اسم الملف المختار** — لأن منتقياً بلا أثر بعد الضغط مشكلة أسوأ من منتقٍ إنجليزي.

### ✅ وما تأكّد سليماً على بيانات حقيقية
- **الملخّص المالي يتحدّث حيّاً**: تجاري ١٠٠٬٠٠٠ ← ضريبة ١٥٬٠٠٠٫٠٠ وإجمالي ١١٥٬٠٠٠٫٠٠؛ وشهري ← «عدا الأخيرة ٩٬٥٨٣٫٣٨» تظهر تلقائياً.
- **«٤٤ وحدة متاحة من ٥٢»** على مخزون المكتب الفعلي.
- **حرّاس الحذف**: «لا يُحذف» على العقارَين، و«عليه عقد واحد» للمستأجر، و«له عقاران» للمالك — **والمالك الذاتي بلا زرّ حذف إطلاقاً**.
- الاختبارات: **٦٩٢ خضراء** في ١٤ مجموعة (`plural-ar` ٢٣ جديدة).

> **ولم يُتحقَّق بصرياً من تنبيه العدّاد القديم** — لا يوجد في بيانات المكتب عقار يحمل رقماً في الحقلين المُزالين، فالتنبيه لا يظهر أصلاً.

### ميزة — الحذف بقاعدة «امنع مع شرح»، وثلاث دوالّ حذف كانت غائبة (`0067`)

**العيب:** `deleteProperty` كان **يحذف عقاراً بوحداته وعقوده النشطة بلا كلمة**. العقود تبقى حيّة تشير إلى عقار اختفى من كل شاشة — لا خطأ، ولا تحذير، ولا سبيل للمكتب أن ينتبه حتى يتوقّف رقم عن الاتّساق. **ولم تكن هناك `deleteUnit` ولا `deleteTenant` ولا `deleteOwner` أصلاً.**

**الحارس في القاعدة لا في الكود** — مُشغّل واحد على خمسة جداول، يستيقظ فقط عند انتقال `deleted_at` من فارغ إلى تاريخ، فيمرّ كل تعديل عادي وكل إلغاء أرشفة كما كان:

| يُرفض حذف | إذا |
|---|---|
| عقار | فيه وحدة أو عقد |
| وحدة · مستأجر | عليه عقد |
| مالك | يملك عقاراً — **والمالك الذاتي يُرفض دائماً** |
| عقد | حالته **نشط** (مساره «الإنهاء المبكر») |

**والرسالة مُهيكلة عمداً** (`HAS_DEPENDENTS:units=3;contracts=2`) لا نثراً إنجليزياً، لأن المكتب يقرأ **«مرتبط بـ٣ وحدات وعقدين»** — وصيغة المثنّى في العربية كلمة أخرى لا حرف زائد.

**قرارات:**

**١) `SECURITY DEFINER` على المُشغّل.** العدّ يجب أن يكون **الحقيقة** لا رؤية المُنفِّذ: العضو المقيَّد بعقارات معيّنة كان سيعدّ صفر عقود على عقار تُخفي RLS عقوده عنه، **فيمرّر الحارس بالضبط ما وُجد ليمنعه**.

**٢) المالك الذاتي بلا زرّ حذف إطلاقاً — لا زرّاً معطّلاً.** هو المكتب نفسه، و`issue_invoice` يقرأ هويّته الضريبية لكل عقار مملوك.

**٣) والاستيراد استثناء واحد مشروع.** `import_revert` يمشي على دفعة كاملة بلا ترتيب، فيصل إلى العقار قبل وحداته. لولا استثناؤه **لصار التراجع عن استيراد خاطئ مستحيلاً** — وهي أكثر لحظة يحتاج فيها المكتب التراجع. يُعلن عن نفسه براية داخل المعاملة، بنفس شكل `app.allow_org_purge` من `0061`.

**٤) والواجهة لا تعرض زرّاً يفشل دائماً.** العقار غير الفارغ يعرض «لا يُحذف»، والمستأجر ذو العقود يعرض السبب ورابطاً إليها. **الحارس شبكة أمان لا واجهة.**

**والاختبارات تشمل ما يجب ألّا يُمسك أيضاً:** التراجع عن الاستيراد، وإلغاء الأرشفة، وإعادة التسمية، **وأن حذف بيانات صاحب البيانات (PDPL) لا يؤرشف مستأجراً ولا مالكاً** — مُتحقَّقاً من نصّ الدالّة نفسها، لأن استدعاءها كمشرف يرفض قبل أن يصل إلى شيء.

### توثيق
- **[٢٤) الحذف](docs/user-guide/24-deleting.md)** — فصل جديد: ما يُحذف، وما يُرفض ولماذا، وترتيب إزالة عقار كاملاً.
- الاختبارات: **٦٦٩ خضراء** في ١٣ مجموعة (`archive-guards` ٢٤ جديدة).

### إصلاح — رقم العدّاد كان يُكتب في مكانين، ولا شيء يقول أيّهما الصحيح (بلا هجرة)

نموذج **إضافة عقار** كان فيه حقلان نصّيان — «عدّاد الماء» و«عدّاد الكهرباء» — من `0044`، بينما **وحدة المرافق (`0063`) تدير العدادات سجلّات لها قراءات وفواتير ومن يتحمّلها**. فالمكتب يكتب الرقم مرّتين، ويكتشف الاختلاف يوم يهمّ فقط.

**أُزيل الحقلان من النموذج ومن الكتابة.** ومكانهما سطر يقول أين ذهبا: «العدّادات تُضاف بعد حفظ العقار من تبويب العدادات».

**والعمودان بقيا في القاعدة — لا هجرة ولا حذف.** المكتب كتب فيهما أرقاماً لسبب، وإسقاطهما فقدان بيانات لا تنظيف. وبدلاً من ذلك، **تبويب العدادات يعرضها حين يكون العقار بلا عدّاد فعلي**:

> ⚠️ أرقام عدّادات مسجّلة في حقل قديم: ماء `12345`. **هذه نصوص فقط — بلا قراءات ولا فواتير.** أضِفها عدّادات فعلية ليصبح لها سجلّ.

ويختفي التنبيه بمجرّد وجود عدّاد واحد. **نبّه ولا تُجبر، مطبَّقاً على بيانات المكتب نفسها** لا على إدخاله فقط.

### توثيق
- [٧) إدارة العقارات](docs/user-guide/07-properties.md) و[٢٠) العدادات](docs/user-guide/20-utilities.md) — أين صارت العدّادات، وماذا يحدث لما كُتب سابقاً.

### ميزة — الملخّص المالي في نموذج العقد، وقائمة الوحدات المتاحة (بلا هجرة)

طلبت المواصفة **مربّع اختيار للضريبة** وضعُه الافتراضي «بلا ضريبة». **لم يُنفَّذ، وهذا مقصود** — فتحتُ `app.activate_contract` أوّلاً:

```sql
if c.contract_kind = 'commercial' then
  v_rate := 0.15; v_type := 'commercial_rent';
else
  v_rate := 0;    v_type := 'residential_rent';
end if;
```

**الاشتقاق موجود منذ `0019` ويصيب.** والمربّع كان سيستبدل اشتقاقاً صحيحاً باختيار يدوي — وبوضعه الافتراضي المطلوب، **كل عقد تجاري يصدر بلا ضريبة حتى يتذكّر الموظّف**. الناقص لم يكن قراراً، بل **رؤية نتيجته**.

**فبُني الملخّص المالي** أسفل حقول المبالغ: الإيجار الأساسي، الضريبة (١٥٪ أو «معفى — إيجار سكني»)، الإجمالي، ثم عدد الدفعات وقيمة الواحدة.

**وهو مرآة لـ`activate_contract` لا رأي ثانٍ فيه** — نُقلت عنه تفصيلتان تعمّداً:
- **الباقي يُحمَّل على الدفعة الأخيرة** كما تفعل القاعدة. القسمة المتساوية على الشاشة كانت ستعرض رقماً لن تحمله أي فاتورة، **والملخّص يقول ذلك صراحة حين يحدث**.
- **الضريبة تُقرَّب لكل دفعة ثم تُجمع**، لا تُؤخذ على الإجمالي السنوي. الفرق هللة لكل فترة، والمكتب يطابق على الفواتير.

**والحساب بأعداد صحيحة لا بـ`× 0.15`** — الفاصلة العائمة تقع على الجانب الخطأ من نصف الهللة كثيراً بما يكفي لتختلف الشاشة عن القاعدة.

**والاختبار لا يقارن الحساب بنفسه:** ٩ حالات **تُفعِّل عقوداً حقيقية على قاعدة حقيقية** وتطالب بتطابق مبالغ الاستحقاقات وضرائبها — ومنها حالة `10` هللات (نصف هللة بالضبط: حدّ التقريب)، وحالات بواقٍ من القسمة.

### إصلاح — نموذج العقد كان يعرض كل الوحدات، بما فيها المؤجّرة

لا تصفية بالحالة ولا تجميع. الآن يعرض **الشاغرة والمحجوزة** فقط، مع عدّاد «١٢ متاحة من ٥٠»، ومفتاح يكشف الباقي **بحالته مكتوبة بجانبه** (وقابلة للبحث: اكتب «مؤجرة»).

**والمحجوزة متاحة عمداً** — الوحدة تُحجز لأن عقدها قيد الكتابة.

> **وفخّ تفاديته:** `Combobox` يقرأ نصّ الخيار المختار من القائمة المعطاة له. فلو أخفت التصفية الوحدة المختارة، **لظهر الحقل فارغاً بينما القيمة المخفيّة باقية** — نموذج يبدو ناقصاً ويُرسَل كاملاً. الوحدة المختارة تبقى في القائمة دائماً.

### توثيق
- [٨) المستأجرون والعقود](docs/user-guide/08-tenants-contracts.md) — قسمان جديدان: قائمة الوحدات المتاحة، والملخّص المالي (ولماذا لا يوجد مربّع للضريبة). وصُحّح سطر قديم يقول إن رقم العقد يُكتب يدوياً.
- الاختبارات: **٦٤٥ خضراء** في ١٢ مجموعة (`contract-finance` ٦٨ جديدة).

### ميزة — صفحة إعدادات المنشأة: هويّة المكتب النظامية أخيراً لها مكان (`0066`)

`app.issue_invoice` ينسخ **اسم المنشأة وسجلّها التجاري ورقمها الضريبي** على كل فاتورة لحظة إصدارها، وأربعة مستندات مطبوعة تعرضها (العقد، سند القبض، كشف حساب المالك، سند التوريد). و**لم يكن في المنتج كلّه ما يكتب هذين العمودين** — لا شاشة ولا دالّة. كل مكتب كان يطبع مستنداته بهويّة ضريبية فارغة، بلا خطأ ولا تنبيه.

**صفحة `/app/settings`** تملأ ذلك، وفيها ثلاثة أقسام للمنشأة وقسم للحساب الشخصي:

- **الهويّة النظامية** — الاسم، السجل التجاري، الرقم الضريبي، **ترخيص فال**، هاتف المكتب وبريده.
- **العنوان الوطني** بأجزائه الستّة (مبنى، شارع، حي، مدينة، رمز بريدي، رقم إضافي) — لا سطراً واحداً، لأن السطر الواحد لا يُقسَّم لاحقاً وهذا هو الشكل الذي تطلبه المستندات الرسمية. وتحته **معاينة** لصيغة الطباعة.
- **الحساب البنكي للتحصيل** (حساب المكتب لا المالك) — **والآيبان يُفحص حسابياً بـ mod-97 لا شكلياً**: قيود القاعدة تُثبت أنه `SA` + ٢٢ رقماً، وهو ما يمرّ منه رقمان مقلوبان بسلام. `lib/iban.ts` يمسك ذلك، ومعه ١٥ اختباراً.
- **حسابي** — الاسم وجوال التواصل والبريد وكلمة المرور.

**قرارات تستحق التسجيل:**

**١) تغيير كلمة المرور يطلب الحالية** رغم أن الجلسة قائمة. Supabase يقبل التغيير من أي جلسة، وهذا حاسوب مفتوح واحد يفصل المالك عن فقدان حسابه. والجلسة تُثبت أن الجهاز جهازه، لا أن الجالس أمامه هو.

**٢) التعديل الصامت.** العضو غير المدير يمرّ من سياسة القراءة ويسقط في سياسة الكتابة، فيُنفَّذ `UPDATE` **بلا خطأ وبصفر صفوف** — نجاح ظاهري لتغيير لم يحدث. لذلك تطلب الدالّة الصفوف المتأثّرة (`.select("id")`) وترفع الرسالة بنفسها. اختباران يثبّتان السلوك حتى لا يُبسَّط لاحقاً.

**٣) الشعار في مساحة خاصّة لا عامّة.** دلو `org-assets` خاص، ومجلّد لكل منشأة، وسياسات `storage.objects` تُثبت العضوية من **مسار الملف نفسه**. ويُبَثّ عبر `/api/org/logo` من نطاقنا — فلا رابط عام ولا موقّت، ولا حاجة لتوسيع `img-src` في سياسة الأمان. **ويُفحص محتوى الملف (magic bytes) لا امتداده ولا ترويسته**، لأن الترويسة يكتبها الطالب.

**٤) التدقيق بمُشغّل لا بالمُستدعي.** أي تعديل على صفّ المنشأة يكتب `org.profile_update` في سجلّ العمليات: **أسماء الحقول** التي تغيّرت، **والقيمة السابقة للهويّة الضريبية وحدها** — فهي ما يُسأل عنه لو اختلفت فاتورة قديمة عن جديدة، وما عداها لا داعي لنسخه مرّتين. وبالمُشغّل لا بالكود، ليشمل كل مسار يُكتب لاحقاً.

**٥) القيود على العمودين القديمين `NOT VALID`.** هذا الملف لا يرى ما في القاعدة الحيّة، فكل كتابة جديدة تُفحص وصفّ قديم غير مطابق يُترك — بدل هجرة تفشل عند التطبيق.

### إصلاح — بريد المستخدم كان يبقى قديماً في `app.identity` إلى الأبد (`0066`)

المُشغّل الذي ينشئ ملفّ الهوية يعمل عند **الإنشاء** فقط (`0017`/`0037`)، ولا شيء كان يراقب التحديث. فمن غيّر بريده عبر Supabase بقي `app.identity.email` عنده على القديم — **بلا عطل ظاهر**، لأن الأثر الوحيد أن الإشعارات تذهب إلى عنوان مهجور. أُضيف `on_auth_user_updated` ليُتابع البريد والجوال. والاسم يبقى ملكنا: يُحرَّر من التطبيق لا من طبقة المصادقة.

### توثيق
- **[٢٣) إعدادات المنشأة وحسابك](docs/user-guide/23-settings.md)** — فصل جديد، مربوط من الفصول ٠٢ و١٣ و١٩.
- الاختبارات: **٥٧٧ خضراء** في ١١ مجموعة (`org-profile` ٣٧ · `iban` ١٥ جديدة).

---

## 2026-08-03

### إصلاح — ستّة عيوب كشفتها التجربة الحيّة على بيانات حقيقية

بعد نشر الـ360 فُتحت الشاشات على المنصّة الحيّة ببيانات المكتب الفعلية. **ولا واحد من هذه الستّة كانت الاختبارات لتكشفه** — كلّها تحتاج بيانات حقيقية أو عيناً على الشاشة.

**١) بطاقة المبلغ تلتفّ سطرين.** «١١٥٬٠٠٠٫٠٠ ر.س» أطول من ربع بطاقة، فانكسرت القيمة سطرين ودفعت عنوانها ورابطها خارج محاذاة البطاقات الثلاث. صارت العملة **في العنوان** — وهو ما تفعله بطاقات لوحة المؤشرات أصلاً («المتأخرات (ر.س)»).

**٢) 🎯 الخط الزمني يبدأ بحدث لم يقع.** «ينتهي العقد ٢٠٢٧» تصدّر ما حدث فعلاً، **فقرأت الصفحة وكأن العقد انتهى**. الآن مجموعتان: **«قادم»** بنقطة مجوّفة، ثم **«ما حدث»** بنقطة ممتلئة — تمييز لا يعتمد على اللون وحده.

**٣) «١ بنداً»** صيغة خاطئة (تصلح لـ١١ فأكثر)، وبقية التطبيق تكتب «N بند».

**٤) زرّ تعديل بعلامة `+`.** إعادة استخدام `FormDrawer` جلبت علامتها الثابتة، **فصار الزرّ يقول للمستخدم إنه سيُنشئ شيئاً**. صارت الأيقونة خاصية، والتعديل يحمل **قلماً**.

**٥) بطاقة تُنقَر ولا شيء يقول ذلك.** في صفحة العقار صارت «عقود نشطة» وحدها رابطاً بين ثلاث بطاقات تطابقها شكلاً — **وبطاقة لا تُميَّز عن جاراتها أسوأ من أربع ساكنة**. الأربع الآن تحمل «عرض ←»، وأُضيف `?property=` و`?status=` لقائمة الوحدات لتكون لها وجهة حقيقية.

**٦) رابط عميق يقود إلى طريق مسدود.** «شاغرة» كانت تعرض **«لا توجد وحدات بعد»** والمكتب عنده ٥٢ وحدة — **رسالة تقرأ كفقدان بيانات**، بلا طريق للعودة. الآن شريحة تسمّي التصفية مع «إزالة التصفية»، والحالة الفارغة تفرّق بين «لا مطابق» و«لا يوجد».

> **والسادس نتيجة الخامس**: أضفتُ رابطاً عميقاً فصنعتُ طريقاً مسدوداً. لم يظهر إلا بالنقر عليه فعلاً.

### تحسين — صفحات المستأجر والمالك والعقار تذكر الحقائق والتعديل خلف زرّ

الـ360 كان **طبقةً فوق نموذج تحرير، لا صفحة 360**. في صفحة المستأجر وحدها كان النموذج **٧٣٢px من أصل ٢١٤٧** — شاشتان من الحقول تفصل الملخّص عن الملاحظات.

**الآن الترويسة تقول ما هو السجلّ**، والتعديل خلف `FormDrawer` — **نفس الدُرج الذي يستعمله التطبيق لكل نماذجه، لا نمط جديد**.

| | قبل | بعد |
|---|---|---|
| صفحة المستأجر | ٢١٤٧px · نموذج ٧٣٢px | **١٧٥٠px · بطاقة حقائق ٣١٥px** |

**والمالك** كان يفتح على **نموذجَي إعدادات مفتوحين** (الأتعاب · الضريبة والسجل) — أي أن الصفحة تبدأ بالتهيئة لا بالمالك. انتقلا إلى دُرج واحد، **مقصوصَين من الأصل برمجياً لا معادَي الكتابة** حتى لا تنزلق أسماء الحقول.

**وشارة «بلا رقم ضريبي» على المالك الخارجي ليست زينة:** بدونه **لا تُصدَر فاتورة ضريبية باسمه** — فالصفحة تقولها بدل أن تُكتشف عند أول إصدار.

**والعقار** جمع سطر المدينة وسطر الصك وشارة الكود في **شبكة حقائق واحدة**، وتغيير المالك انتقل من `<details>` إلى الدُرج القياسي **محتفظاً بزرّه المستقل لأنه قرار ذو أثر**.

استُخرج `Fact`/`FactGrid` إلى مكوّن مشترك **لمّا صار الاستعمال ثالثاً**، وحُذف `Kpi` المحلي بعد أن فقد مستدعيه. وطُبّق §8 على ما لُمس: **١٤ خاصية اتجاه فيزيائية** صارت منطقية.

### ميزة — أرشيتايب الـ360 للكيانات الدائمة (هجرة `0065`) (الطبقة الصفراء ٤/٧)

صفحات **المستأجر والمالك والعقار** صارت تتبع §6.1: ترويسة · **بطاقات ملخّص تربط خارجاً** · أقسام · **ملاحظات داخلية** · **خط زمني**.

> والعقود والفواتير والسندات **مستندات لا كيانات دائمة** — فتصحيحها يكون بمستند (ملحق، إشعار دائن) لا بملاحظة. لم تُمسّ.

**🎯 والبطاقات تربط خارجاً — وهذا هو جوهر النمط لا زينته.** صفحة 360 تعيد بناء جدول العقود تصير **نسخة ثانية تنحرف عن الأصل**. فالبطاقة تعرض الرقم وتُسلّم: عقود المستأجر ← قائمة العقود مُصفّاة به، وسنداته ← قائمة السندات مُصفّاة بطرفه. **ولذلك أُضيفت مرشّحات `?tenant=` و`?party=` و`?property=` فعلياً** — بطاقة تربط لقائمة غير مُصفّاة كذبة.

**⚠️ وأغلقتُ ثغرة أحدثتها في تصدير CSV:** المرشّحات الجديدة كانت ستجعل الشاشة تعرض مستأجراً واحداً **بينما يُنزّل الملفّ كل المستأجرين** — وهو بالضبط الانحراف الذي حاججتُ ضدّه في التصدير نفسه. صار `ListSpec` يملك المرشّحات ويطبّقها التصدير أيضاً.

**والخط الزمني مُشتقّ بالكامل** من طوابع زمنية موجودة (بداية العقد، تاريخ السند، وقت الملاحظة). **لا جدول أحداث** — لأنه سيكون سجلاً ثانياً لنفس الوقائع، حرّاً في أن يخالف الأول.

## الملاحظات الداخلية — إلحاقية فعلاً لا وعداً

جدول واحد `entity_note` للثلاثة جميعاً، لا نسخة لكل وحدة. **ولا زرّ تعديل ولا حذف** — القاعدة ترفضهما، فزرٌّ يعرضهما كذبة تقولها الواجهة.

**والمؤلّف يُؤخذ من الجلسة لا من الطلب:** مشغّل يتجاهل أي `created_by` مُرسَل. واختبار يثبت أن إرسال هوية شخص آخر لا يغيّر شيئاً.

**🔒 وثلاث طبقات — اكتشفتُ الثانية أثناء الاختبار:**

| الطبقة | ماذا تفعل |
|---|---|
| ١ | **لا صلاحية** `UPDATE`/`DELETE` أصلاً → «permission denied» |
| ٢ | **ولا سياسة RLS** لهما → حتى لو مُنحت الصلاحية، **العبارة تمسّ صفر صفوف بصمت** |
| ٣ | **المشغّل** → يرفض صراحةً |

كتبتُ الاختبار أوّلاً متوقّعاً المشغّل، ففشل برسالة «permission denied» — ثم منحتُ الصلاحية فمرّ التعديل **بلا خطأ وبلا أثر**، وهناك ظهرت الطبقة الثانية. فصار الاختبار **يمنح الصلاحية والسياسة معاً** ليصل إلى المشغّل ويثبته — وإلا لبقي حارساً غير مُختبَر.

## 🇸🇦 والملاحظات داخل نطاق PDPL

الملاحظة نصّ حرّ كتبه المكتب عن شخص، **فقد تحمل بيانات شخصية**. لو بقيت مقروءة بعد حذف بيانات صاحبها **لصار الحذف ادّعاءً لا واقعاً**.

فأُعيد إصدار `erase_party` ليطمس الملاحظات ويُبلّغ بعددها. **والصفّ يبقى ومحتواه يُطمَس** — حتى لا يفقد الخط الزمني شكله. وملاحظة عن عقار لا تُطمَس بحذف طرف: اختبار يثبت ذلك.

> **وأعدتُ إصدارها بقصّ نصّها من `0061` برمجياً لا بإعادة كتابتها** — نسخ ٦٠ سطراً من دالّة أمنية يدوياً هو حيث يسقط سطر بصمت فيتوقّف الحذف عن حذف شيء.

**والمشغّل ليس مطلقاً كـ`audit_log`**: حذف المنشأة يجب أن يمرّ بالتتالي، والطمس يجب أن يعمل. فالمنع على **الأدوار** (`authenticated`/`anon`) لا على الجميع — واختباران يثبتان أن مسار حذف المنشأة ما زال يمرّ ويأخذ ملاحظاتها معه.

**٢٧ اختباراً جديداً** · والمجموع **٥٢٥**.

### ميزة — ظلال البطاقات مصبوغة بالتيل (الطبقة الصفراء ٣/٧)

ثلاثة رموز في `tailwind.config.ts`: **`shadow-card`** · **`shadow-card-hover`** · **`shadow-nav`** — كلّها بلون العلامة `rgba(15,118,110,…)` لا بالأسود. الظلّ الأسود **يبدو متّسخاً** على هذه الأسطح، والظلّ الحامل للون العلامة جزء كبير ممّا يجعل الواجهة تبدو عائلة واحدة.

> **وبشفافية أقلّ قليلاً من قيم الكحلي المستوردة** — التيل هو اللون الأفتح، فيثقل عند نفس الشفافية.

**طُبِّقت على الأسطح التي هي بطاقات فعلاً:** مكوّن `Card` المشترك · بطاقات العقود والملّاك والمستأجرين والوحدات (وهي التي تملك زوج «بطاقة/تحويم» الذي يصفه §3.3) · وبطاقات الصفحة العامة. **والشريط السفلي تخلّى عن قيمته المكتوبة يدوياً لصالح الرمز المشترك.**

**🌙 وفي الوضع الداكن: `dark:shadow-none`.** الظلّ الملوّن على سطح شبه أسود **لا يُقرأ كظلّ بل كتوهّج**، والفصل هناك يقوم به الحدّ لا الظلّ.

> **ما لم يُكنس عمداً:** ٥١ `shadow-sm` أخرى على ألواح وأشرطة ليست بطاقات. تحويلها جميعاً تغيير بصري لا إعادة صبغ — تُحوَّل مع لمس ملفّاتها، كقاعدة §8.

**والتحقّق كان بقراءة القيمة المحسوبة من المتصفّح لا بالنظر:**
- **فاتح:** `rgba(15, 118, 110, 0.1) 0px 4px 24px` على ٦ بطاقات ✅
- **داكن:** الظلّ **شفّاف تماماً** والخلفية `rgb(15,23,42)` ✅

**واكتشفتُ في أثنائها أن الوضع الداكن في المشروع يعمل بـ `prefers-color-scheme` لا بصنف `.dark`** (لأن `darkMode` غير مضبوط في الإعداد) — فأول قياس أجريتُه كان بالآلية الخاطئة وأعطى نتيجة مضلّلة حتى أعدتُ التحميل.

### ميزة — شريط تنقّل سفلي للجوال (الطبقة الصفراء ٢/٧)

**رُفع هذا البند في الترتيب بعد تأكيد المالك أن موظّفي المكاتب يعملون من الجوال أساساً.**

شريط ثابت أسفل الشاشة على الجوال وحده (`md:hidden`) بأربعة تبويبات: **الرئيسية · العقود · السندات · المزيد**.

**والاختيار بالتكرار لا بالتصنيف** (§1.2): اللوحة التي يبدأ منها، والعقود التي تحمل المستأجر والإيجار، والسندات التي يُصدرها **وهو واقف أمام الدافع**. وما عداها خلف «المزيد».

**🎯 و«المزيد» يفتح نفس دُرج الشريط الجانبي** — قائمة واحدة لا نسخة ثانية تنحرف عنها. **ولذلك نُقل زرّ القائمة من أعلى الشاشة إلى الشريط السفلي**: فاتحان لنفس الدُرج هما بالضبط الازدواج الذي يعترض عليه §5.3، والأسفل في متناول الإبهام.

**وعدّاد الإشعارات ظهر على تبويب «المزيد»** — الإشعارات خلفه، **فبدون العدّاد هناك لا يعرف مستخدم الجوال أن هناك ما يُقرأ إطلاقاً**.

**وتفاصيل تجعله يبدو أصلياً:** ارتفاع لمس `56px` · **حشوة `env(safe-area-inset-bottom)`** ليعلو فوق شريط الآيفون السفلي (ومعها `viewport-fit=cover` **وإلا لظلّت القيمة صفراً**) · ظلّ علوي **مصبوغ بالتيل لا بالأسود** · والتبويب النشط **بنقطة إلى جانب اللون، لا باللون وحده**.

**⚠️ وخطأ تفاعلي أحدثه التغيير نفسه وأُصلح:** رسائل التنبيه (`toast`) تُعرض عند `bottom-4` بـ `z-60`، **فصارت تغطّي الشريط الجديد وتحجب التبويبات** طوال ظهورها. رُفعت فوقه على الجوال وبقيت كما كانت على سطح المكتب.

**وطُبِّق §8 على ما لُمس:** الشريط الجانبي وغلاف المحتوى تحوّلا إلى خصائص منطقية (`start-0` · `border-e` · `ps-64`). والمكوّن الجديد **نظيف من أول مرّة** بفحص `check:rtl`.

> **ما لم أتحقّق منه:** لم أفتح الشاشة المُسجَّل دخولها بصرياً — تحتاج حساباً لا أملكه ولا أتعامل مع بياناته. **تحقّقتُ بدلاً من ذلك** أن كل صنف منطقي جديد **وُلِّد فعلاً في CSS المبني** (`start-0` · `border-e` · `ps-64` · `min-h-[56px]` · `safe-area-inset-bottom`) — إذ Tailwind **لا يُخطئ بل يُسقط الصنف بصمت** لو لم يدعمه، ومن هنا يأتي الانكسار.

### ميزة — تصدير القوائم إلى Excel وفرز مُسمّى (الطبقة الصفراء ١/٧)

زرّ **«تصدير»** وقائمة **ترتيب** في شريط أدوات ستّ قوائم: العقارات · المستأجرون · الملّاك · العقود · الفواتير · السندات.

**والتصدير للمجموعة المُصفّاة التي تراها على الشاشة**، لا للجدول كلّه ولا للصفحة الظاهرة وحدها — بحدّ ٥٬٠٠٠ صفّ حتى لا يتحوّل الطلب إلى قراءة بلا سقف.

**🎯 وثلاث تفاصيل عن Excel تحديداً، لأنه ما يفتح به المكتب الملفّ فعلاً:**

| | لولاها |
|---|---|
| **علامة ترتيب البايتات (BOM)** | Excel يقرأ البايتات بترميز النظام المحلي **فتصير كل الأسماء العربية طلاسم** |
| **حراسة حقن الصيغ** | الخلية التي تبدأ بـ `=` أو `+` أو `-` أو `@` **ينفّذها Excel كصيغة** — واسم المستأجر إدخال مستخدم، فهذا مسار حقيقي من «كتب أحدهم اسماً» إلى «نُفِّذت صيغة على جهاز المحاسب» |
| **المبالغ بلا فواصل آلاف** | `12,000.00` تحوي فاصلة فتُقتبَس، **فيقرأها Excel نصّاً لا رقماً — والعمود لا يُجمَع**، وهو أصل سبب التصدير |

**والفرز قائمة مُسمّاة لا نقراً على الأعمدة** — العمود لا يُخبر أحداً أن الفرز ممكن أصلاً. **ويُطبَّق بمجرّد الاختيار**: المكاتب تعمل من الجوال، والبديل يكلّف نقرة ثالثة في كل تغيير. وبلا JavaScript يعمل زرّ «بحث» كما هو.

**🔒 ولا مفتاح خدمة في مسار التصدير:** يمرّ بنفس عميل الجلسة ونفس RLS، والمنشأة من كوكي `active-org`. **والمورد من قائمة بيضاء** — اسم غير معروف لا يصل إلى استعلام أبداً. والفرز كذلك: مفتاح لا يطابق قائمة القائمة يسقط للافتراضي، **فلا يُحقن عمود من المستخدم**.

**وسجلّ واحد يملك الثلاثة** ([`lib/list-specs.ts`](aqar/lib/list-specs.ts)): كيف يُطابَق البحث، وما الفرز المتاح، وكيف يبدو الصفّ في Excel. لو بنت الصفحة مرشّحاً وبنى التصدير آخر **لانحرف الملفّ عن الشاشة بصمت** — وهي نفس العلّة التي يحذّر منها نظام التصميم لصفحات الـ360 التي تعيد بناء الجدول.

**٢٠ اختباراً جديداً للـCSV** (`csv.mts`) بلا قاعدة بيانات ولا خطوة بناء. **والافتراضات الستّة لم تتغيّر**: أوّل فرز في كل قائمة هو ترتيبها السابق حرفياً.

**وإصلاح جانبي:** البحث في صفحة العقود كان **يُسقط `?unit=`** فيضيع اختيار الوحدة المُمرَّر من صفحة الوحدات.

### قرار — الطبقة الخضراء من نظام التصميم صارت **سارية**

§1 المبادئ · §2 تصنيف الكيانات · §7 الوصولية وأرضية التباين · §8 الخصائص المنطقية · **§11 قائمة فحص كل شاشة جديدة** — لم تعد طموحاً في وثيقة مستوردة، بل **ملزِمة على كل عمل جديد**. مُعلَنة في [§9](docs/foundation/08-design-system-red-sea-marine.md) وفي [فهرس وثائق التأسيس](docs/foundation/README.md).

**وبدل أن تبقى نصّاً، صار §8 قابلاً للتشغيل:** `npm run check:rtl` — فاحص [بلا أي اعتمادية](aqar/scripts/check-rtl.mjs) (المشروع يحتفظ بسبع حزم فقط، وESLint كان يعني سلسلة أدوات كاملة لقاعدة واحدة).

**🎯 وما يجعل مخرجاته تُقرأ: يفرّق بين المقصود والمنحرف.** `text-right` بجوار `dir="ltr"` **صحيح** — رقم العدّاد أو الآيبان أو التاريخ مقطع يسار-يمين داخل صفحة عربية، ومحاذاته يميناً هي محاذاته لنهايته هو. لولا هذا الاستثناء لأنتج الفاحص **٢١٤ نتيجة أغلبها كاذب فلا يقرؤها أحد**؛ ومعه **١٣٧ نتيجة حقيقية**.

**وثلاثة أخطاء في الفاحص نفسه أُصلحت قبل الاعتماد:**
- كان يقطع الوسم عند أول `>` — و`[&>th]:text-start` يحوي `>` داخل النص! فكان **يتخطّى بصمت كل صنف بعده**. صار ماسحاً يتتبّع علامات الاقتباس، فظهرت ١٧ حالة كانت مخفيّة.
- كان يقترح `text-right → text-end`. **والمستند `dir="rtl"`، فاليمين هو البداية لا النهاية** — التطبيق الأعمى للاقتراح كان **يقلب تخطيط كل ما «يصلحه»**. صار `text-right → text-start`.
- وللسبب نفسه: `mr → ms` لا `me`.

**وطُبِّق على الملفات المُلامَسة اليوم** (كما نصّ §9: بلا كنس شامل): خمسة مواضع في وحدة المرافق.

**وأرضية التباين §7.2 كشفت خطأين في شاشاتي الجديدة:** «قراءة أساس» — **وهي إحدى حالات الاستهلاك الثلاث** — وملاحظة المستخدم، كانتا بـ `slate-400` أي دون معيار AA. رُفعتا إلى `slate-500`. الشرطة `—` تبقى ٤٠٠ فهي زخرفية وموحّدة في التطبيق كلّه.

> **ما لم يُنفَّذ عمداً:** كنس الـ١٣٧ الباقية. §9 اعتمد §8 «يُطبَّق مع لمس الملفات» لا كترحيل شامل، والفاحص **قائمة عمل لا بوابة بناء** (`exitCode` صفر دائماً).

### إصلاح — سدّ إعادة التوجيه المفتوحة في `updateUnit` + تغطية حارس التوجيه باختبارات

**الثغرة:** `updateUnit` في [`app/app/properties/actions.ts`](aqar/app/app/properties/actions.ts) كان يقرأ حقل `back` المخفي ويمرّره إلى `redirect()` **بلا فحص**. صار يمرّ عبر `safeReturnTo` — **الحارس الموجود في المشروع أصلاً** ([`lib/return-to.ts`](aqar/lib/return-to.ts)) ولم يكن هذا الموضع يستعمله.

**ومسح للنمط في كل المشروع:** لا موضع ثالث. `/login` و`/auth/mfa` و`/platform` تستعمل الحارس منذ البداية.

**وشدّدتُ حارس المرافق نفسه:** `safeBack` كان يفحص البادئة فقط، فيمرّر **محارف تحكّم** (`%0d%0a`) وحيلة `/\`. صار **يبني على `safeReturnTo` ثم يضيّق على `/app/utilities`** — الحارس المشترك أولاً، ثم النطاق.

**🐛 وخطأ حقيقي كشفه الإصلاح:** رابط العودة في المرافق يحمل تصفية وصفحة، أي **معه `?` أصلاً**. وإلحاق `?error=` به كان يجعل الرسالة تُبتلع داخل معامل سابق فلا تظهر أبداً (`?meter=x?error=y`). صار عبر `backWithError` الذي يختار `?` أو `&`. **كل رسائل الخطأ في وحدة المرافق كانت صامتة، وهذا يظهر فقط عند فشل عملية.**

**🧪 و`safeReturnTo` كان بلا اختبار واحد** رغم أنه يحرس كل تحويلات الدخول ولوحة الإدارة. أُضيف [`redirect-guards.mts`](aqar/supabase/tests/local/redirect-guards.mts) بـ **٢٤ اختباراً**: الروابط الخارجية، `//` و`/\`، تهريب الترويسات (CR/LF، تبويب، NUL، DEL)، الطول، ودورة `/login`. **يعمل بلا اعتمادية جديدة** على تجريد الأنواع الأصلي في Node (٢٣+) — بلا بناء وبلا postgres.

**٤٧٨ اختباراً خضراء** · `tsc` ✅ · `build` ✅.

### توثيق — وحدة المرافق U-5: دليل المستخدم ومراجعة الكود (الوحدة اكتملت)

**[فصل ٢٠ من دليل المستخدم](docs/user-guide/20-utilities.md)** — العدادات وفواتير المرافق: الحالات الستّ، قاعدة الاستهلاك وماذا تفعل عند القراءة الأقلّ، قاعدة من يتحمّل، التقارير الأربعة، ومن يرى ماذا. ومُفهرَس في [الدليل](docs/user-guide/README.md).

**ومراجعة `/clean-code-guard` على الوحدة كاملة (١٬٨٥٠ سطراً) وجدت سبعة بنود، أُصلحت كلّها:**

🔒 **الأهمّ — إعادة توجيه مفتوحة.** كل إجراء يقرأ حقل `back` المخفي ثم `redirect(back)` **بلا فحص**. نموذج مُلفَّق كان يستطيع أن يحوّل زرّ «حذف» إلى رحلة لموقع خارجي. صار `back` يمرّ عبر `safeBack` الذي **يرفض أي مسار خارج `/app/utilities`** ويعود للافتراضي.

> ⚠️ **والنمط نفسه قائم في [`app/app/properties/actions.ts`](aqar/app/app/properties/actions.ts) (`updateUnit`)** من قبل هذه الوحدة. **لم أغيّره** لأنه خارج نطاق U-5 — بند مفتوح لقرارك.

**وستّة أخرى:**
- **`setBillPaid` كان إجراءً واحداً بعلامة `unpay`** — فُصل إلى `markBillPaid` و`clearBillPaid`، ولا فرع ولا علامة منطقية في نموذج.
- حقل `paid_at` في نفس الإجراء **لم يكن يُرسله أحد** — حُذف.
- فرع `YYYY-MM-DD` في `firstOfMonth` **بلا مستدعٍ** (`<input type="month">` لا يرسله) — حُذف.
- عمودان (`meter_level`, `unit_id`) يُجلبان في شاشة العدادات **ولا يُستعملان** — حُذفا من الاستعلام.
- رابط العودة في شاشة القراءات كان ينتهي بـ `?` فارغة حين لا تصفية — أُصلح.
- **`monthLabel` في `lib/utilities.ts` كان يصطدم باسم دالّة أخرى بسلوك مختلف** في [`app/platform/page.tsx`](aqar/app/platform/page.tsx) — أُزيلت وأُدمجت في موضعها الوحيد.
- استعلام الاستهلاك كان **بلا حدّ** بينما لأخويه حدّ — أُضيف.

**٤٥٤ اختباراً خضراء بعد الإصلاحات** · `tsc` ✅ · `build` ✅.

### ميزة — وحدة المرافق U-4: التقارير الأربعة (هجرة `0064`)

شاشة `/app/utilities/reports` بالتقارير الأربعة المعتمدة.

**ولماذا لزمت هجرة رغم أن U-2 وU-3 لم تلزمهما:** التقريران الأول والثاني تصفيتان على العرض القائم، أما **الاستهلاك الشهري فتجميع `GROUP BY`** و**«تحتاج انتباهاً» اتحاد أربعة استعلامات نفي عبر أربعة جداول** — وكلاهما لا يمكن التعبير عنه عبر PostgREST مهما بلغت المعاملات. فأُضيف **عرضان** (`utility_monthly_consumption` و`utility_attention`) بـ `security_invoker = true`. **لا جدول جديد ولا عمود ولا تعديل على أي شيء قائم.**

**١) المستحقة** — غير مسدَّدة ولها تاريخ استحقاق لم يحن. **٢) المتأخرة** — مضى تاريخها. والفاتورة بلا تاريخ استحقاق **لا تدخل أياً منهما**، إذ لا شيء تتقدّم عليه أو تتأخّر.

**٣) الاستهلاك الشهري** — جدول اتجاه: العدادات صفوف والأشهر الاثنا عشر أعمدة. **والشهر بلا استهلاك معروف يُعرض «—» لا صفراً**، لأن الصفر يعني «لم يُستهلك شيء» وهذا ليس ما نعرفه. الشهر الفارغ تماماً لا قراءة له.

**٤) عدادات تحتاج انتباهاً** — أربعة مصادر، كل سطر فيها **فعل مطلوب** مع رابط يفتح مكانه مباشرة:
- عدّاد نشط بلا قراءة منذ ٦٠ يوماً أو لم يُقرأ قط (**والمفكوك لا يُطالَب بقراءة**)
- فاتورة **لا قراءة عليها ولا قراءة مسجَّلة في شهرها** — والفاتورة التي تحمل قراءتيها مسنودة فلا تُوسَم، وإلا لدُفنت الحالات الحقيقية في الضجيج
- وحدة مؤجَّرة بلا عدّاد
- قراءة موسومة «تحتاج مراجعة» لم تُحسم

**١٦ اختباراً جديداً** (المجموع ٦٦). **وتصحيح في اختبار العزل:** كتبتُ أولاً أن العضو المقيَّد يرى «صفر» صفوف انتباه، ففشل — لأن لعقاره فعلاً عدّاداً غير مقروء **وحقّه أن يراه**. الادّعاء الصحيح هو العزل لا الفراغ: **كل ما يراه يخصّ عقاره، وصفوف العقار الآخر موجودة وغير مرئية له**. الاختبار الأول كان سيمرّ لسبب خاطئ.

### ميزة — وحدة المرافق U-3: الفواتير ومن يتحمّلها

شاشة `/app/utilities/bills`: تسجيل الفاتورة الشهرية، تصفية (الكل · غير مسدَّدة · متأخّرة · مسدَّدة)، وتعليم السداد وإلغاؤه.

**وكل ما تعرضه الشاشة عن الفاتورة مشتقّ، لا مُدخَل:**

| المعروض | من أين |
|---|---|
| الإجمالي | عمود محسوب = الاستهلاك + الضريبة + الرسوم |
| الاستهلاك | نفس قاعدة القراءات مطبَّقة داخل الصف |
| **مسدَّدة / متأخّرة** | من `paid_at` و`due_date` — **لا عمود حالة** |
| **من يتحمّلها** | من العقد الساري **آخر يوم من شهر الفاتورة** |

فالنموذج يطلب ما طبعه المزوّد فقط: الشهر، القراءتين (اختياريتان)، المبالغ، تاريخ الاستحقاق. **ولا يُدخل أحد من يتحمّل الفاتورة** — حقل يكتبه إنسان سيخالف العقود يوماً، والمشتقّ منها لا يستطيع.

**و«تعليم مسدَّدة» كتابة واحدة**: تاريخ يُملأ أو يُفرَّغ. لا حقل ثانٍ يمكن أن يتأخّر عنه، فيستحيل أن تبقى فاتورة «متأخّرة» بعد سدادها.

**⚠️ تنبيه تغيّر المستأجر:** إذا سرى عقدان على الوحدة في آخر يوم من الشهر، تُعرَض الفاتورة **مع تحذير صريح** أن عقد آخر الشهر هو المعتمد وأن التوزيع النسبي يدوي — لأن تقسيم الفاتورة بين مستأجرَين قرار لا يجوز للنظام أن يتّخذه ضمناً.

**وبلا أثر مالي:** الشاشة تقول ذلك للمستخدم صراحةً — لا استحقاق على المستأجر ولا دخول في كشف المالك.

**ستّة اختبارات جديدة** (المجموع ٥٠): المتأخّرة تُشتقّ، والسداد يمسح التأخّر في نفس الخطوة، وما لم يحلّ أجله ليس متأخّراً، والعقدان المتداخلان يرفعان العلم بينما الفاتورة ذات العقد الواحد لا ترفعه.

### ميزة — وحدة المرافق U-2: الواجهة (العدادات والقراءات)

بند **«المرافق» مستقلّ في الشريط الجانبي** — لا داخل الصيانة، لأن العدادات مرافق لا أعطال.

**شاشة العدادات** (`/app/utilities`): بحث برقم العدّاد، آخر قراءة لكل عدّاد، تغيير الحالة (نشط/مؤرشف/مفكوك)، وحذف ناعم. **ونموذج الإضافة ثلاثة حقول ظاهرة فقط** — النوع، الرقم، وهل يخدم العقار كاملاً أم وحدة — والباقي (رقم الحساب، الاشتراك، المزوّد، تاريخ التركيب) خلف «تفاصيل إضافية»، احتراماً لأن المكتب يُدخل أرقام العدادات فقط.

**شاشة القراءات** (`/app/utilities/readings`): تعرض الاستهلاك من العرض المشتقّ `utility_consumption` مباشرةً، ومعه «قراءة أساس» و«تحتاج مراجعة» كحالتين ظاهرتين لا كفراغ صامت، مع تصفية «تحتاج مراجعة فقط».

**والقراءة الأقلّ من سابقتها تُواجَه في مكانين:**
- **قبل الحفظ** — يوقف النموذج ويسأل «هل استُبدل العدّاد؟» فور إدخال رقم أقلّ من آخر قراءة.
- **بعد الحفظ** — زرّان في الجدول: **«نعم، عدّاد جديد»** (يضع `is_reset`) أو **«تصحيح»** بحقل رقم في مكانه.

وتصحيح الرقم وحده يكفي: **الاستهلاك عرض على القراءات لا عمود مخزَّن**، فلا خطوة إعادة احتساب ولا شيء يمكن أن يبقى خارج التزامن. اختباران جديدان يثبتان ذلك — التصحيح يعيد الاستهلاك، والقراءة التالية تقيس من الرقم المصحَّح.

**في صفحة العقار:** تبويب «العدادات» يفصل الرئيسية عن عدادات الوحدات (فهما يجيبان سؤالين مختلفين)، وعمود «عدّادات» في جدول الوحدات. **ولا صفحة وحدة مستقلّة في النظام اليوم**، فعدادات كل وحدة تُعرَض من هنا ومن تصفية شاشة القراءات.

**إصلاح جانبي:** تفعيل الشريط الجانبي صار **يختار أطول تطابق** بدل `startsWith` — وإلا لأُضيء «العدادات» و«القراءات» معاً في صفحة القراءات.

**بلا أثر مالي، كما في U-1:** لا `charge` ولا `payment` — واختبارا «صفر» ما زالا خضراوين.

### ميزة — وحدة المرافق U-1: العدادات والقراءات والفواتير (هجرة `0063`)

التصميم المعتمد كاملاً في [`docs/foundation/09-utilities-module.md`](docs/foundation/09-utilities-module.md). هذه المرحلة **قاعدة البيانات واختباراتها فقط** — بلا واجهة بعد.

**ثلاثة قرارات تحكم الوحدة كلّها:**

**١) كيان واحد `utility_meter` بحقل `utility_type`** لا جدول لكل مرفق — الكهرباء والماء لهما نفس الحقول تماماً، وجدولان يعنيان ازدواج كل استعلام وتقرير وسياسة RLS ثم جدولاً ثالثاً للغاز. والنوع **نصّ مع `CHECK` لا `enum`**، لأن `ALTER TYPE ... ADD VALUE` لا يعمل داخل معاملة فيكسر التطبيق بضغطة واحدة في SQL Editor.

**٢) من يتحمّل الفاتورة يُشتقّ ولا يُدخَل.** لا عمود `paid_by`: حقل يكتبه إنسان **سيخالف** العقود، وحقل يُشتقّ منها **لا يستطيع**.

**٣) لا أثر مالي.** لا `charge` ولا `payment` ولا كشف مالك — واختباران يثبتان ذلك صراحةً. وهذا ما يجعل شرط «دون تغيير النماذج المالية» صحيحاً حرفياً.

**🎯 قاعدة الاستهلاك — والفرع الرابع هو بيت القصيد.** أربعة فروع: بلا قراءة سابقة ← `NULL` (قراءة أساس) · `is_reset` ← القراءة نفسها هي الاستهلاك · الحالي ≥ السابق ← الفرق · **الحالي < السابق ← `NULL` + علامة مراجعة**. **ونرفض افتراض «الالتفاف»**: العدادات الرقمية من ٥–٦ خانات فالالتفاف يحتاج استهلاكاً غير واقعي، والقراءة الأقل في الغالبية الساحقة خطأ إدخال أو استبدال لم يُسجَّل — وحساب `(السقف − السابق) + الحالي` صامتاً **يحوّل خطأً مطبعياً إلى فاتورة تبدو معقولة**، وهذا أسوأ من فراغ لأن الفراغ يُرى والمعقول لا يُراجَع.

**🎯 قاعدة من يتحمّل — بالعقد الساري في شهر الفاتورة لا اليوم.** فاتورة فبراير تُحمَّل على مستأجر فبراير ولو خرج بعدها؛ الحساب على «اليوم» كان سيحمّل المستأجر الجديد فواتير سلفه. والمرجع **آخر يوم من الشهر** لأن الفاتورة تصدر بنهايته. والعداد الرئيسي أو الوحدة الشاغرة ← **مالك العقار**.

**قيود تمنع الخطأ في القاعدة لا في الكود:** الوحدة يجب أن تتبع عقار العداد — **بمفتاح أجنبي مركّب** لا بمشغّل (وهو التعديل الوحيد على جدول قائم: فهرس فريد إضافي على `unit`، إضافي بحت). و`status='removed'` ⟺ `removed_at` مُعبّأ. ورقم العداد لا يتكرّر في نفس المكتب والنوع. و`billing_month` أوّل الشهر وإلا شُقّ الشهر نصفين في كل تقرير.

**و`meter_level` و`payment_status` و`consumption` كلها مشتقّة** — عمود يخزّن ما يمكن اشتقاقه ينحرف عنه.

**⚠️ `security_invoker = true` على العرضين ليس تفصيلاً:** بدونه يعمل العرض بصلاحيات مالكه فيسلّم قراءات كل مكتب لكل متصل، **متجاوزاً السياسات بالكامل**. واختباران يثبتان أن العضو المقيَّد بعقار لا يرى غيره من خلال العرض.

**الاختبارات: 430 خضراء** — مجموعة `utilities.mjs` جديدة بـ**42** تأكيداً تغطي السيناريوهات الستّة وقاعدتَي الاستهلاك والتحمّل وعزل RLS.

### قرار — تأجيل الإطلاق + ميزة: الدفع بتحويل بنكي أو نقداً (هجرة `0062`)

**أُجّل الإطلاق** بقرار فريق التطوير لعدم جهوزية الدفع الإلكتروني. سبرنت الإطلاق **موقوف لا ملغى** — بنوده المنجزة كلها حيّة، وحُفظت الحالة كاملة للعودة إليها عند تحديد موعد جديد.

**ولأن الدفع الإلكتروني هو سبب التأجيل، صار الدفع اليدوي هو المسار الوحيد للإيراد** — لا ميزة رفاهية.

**الشكل يتبع المال لا الواجهة.** الدفعة اليدوية هي **نفس صفّ `subscription_payment`** الذي تنتجه الدفعة الإلكترونية؛ الفرق الوحيد **من يؤكّدها**: ويبهوك Moyasar يؤكّد البطاقة، ومشغّل بشري يؤكّد الحوالة بعد رؤيتها في كشف الحساب. جدول ثانٍ للدفعات كان سيشقّ تاريخ الاشتراكات نصفين ويكسر كل تقرير وحساب MRR وسجلّ تدقيق يقرأ `subscription_payment` اليوم.

**تسجيل التحويل لا يمنح شيئاً.** يُنشئ **نيّة** فقط — وإلا لفتح أي مكتب خطة مدفوعة بكتابة رقم مرجع.

**طلب مفتوح واحد لكل مكتب** — وإلّا سجّل مكتب مرتبك خمسة طلبات، وترك للمشغّل مهمة تخمين أي حوالة تقابل أيّها.

**التأكيد مُحايد التكرار (idempotent)** — وهذا هنا أهم منه في المسار الإلكتروني: هذا **زرّ يضغطه إنسان** لا ويبهوك، والنقرة المزدوجة كانت ستمدّد الاشتراك شهرين. و**الدفعة المؤكَّدة لا تُرفض** — عكسها استرداد لا رفض.

**الرفض يستلزم سبباً** — رفض بلا سبب يترك المكتب بلا فكرة عمّا يصلحه، ويترك سجلّ التدقيق بلا فكرة عن سبب ردّ المال.

بيانات الحساب البنكي تُنشر من إعدادات المنصّة (`0054`)، ويقرأها مدير المكتب فقط عبر دالة مستقلة — فسطح الإعدادات يبقى للمشغّلين وحدهم.

**الاختبارات: 388 خضراء** (41 + 119 + **142** + 49 + 37) — خمسة عشر جديدة على المسار اليدوي.

### ميزة — سبرنت الإطلاق: التحقّق بخطوتين (MFA)

**بلا هجرة وبلا اعتمادية جديدة.** يعتمد على عوامل TOTP في Supabase Auth، ويُرسم رمز QR محلياً من `totp.uri` بحزمة `qrcode` الموجودة أصلاً لفاتورة ZATCA — بدل حقن SVG جاهز من طرف ثالث في صفحة تحكمها CSP صارمة.

**الحالة الوحيدة التي تهمّ هي مستوى التوثيق (AAL).** «مُفعَّل لكنه لم يُستخدم في هذه الجلسة» هو بالضبط `current=aal1 && next=aal2`، وهو الشرط الوحيد الجدير باسم — فصار `lib/mfa.ts` يحمله في مكان واحد يقرأه الوسيط والمكتب واللوحة.

**الفرض في الوسيط لا في كل صفحة.** إضافة مسار جديد ونسيان التحقّق فيه هي الطريقة المعتادة لغياب هذه الحماية عن شاشة واحدة بالذات.

**⚠️ إلزامي لمشغّلي المنصّة.** حساب المشغّل يرى كل المكاتب، فكلمة المرور وحدها لا تكفي لحمله. والوسيط يرفع مستوى من **يملك** عاملاً؛ أما من **لم يسجّل** عاملاً قط فيغطّيه `app/platform/layout.tsx` بتحويله إلى صفحة الأمان.

**التقييد مطبَّق على الرمز نفسه:** ٦ أرقام = مليون احتمال، وكلمة المرور المسروقة هي ما يوصل المهاجم إلى هذه الشاشة أصلاً — فبلا حدّ يصير العامل الثاني مجرّد تأخير.

**ولا يوجد تجاوز من داخل النظام** إن فُقد الجوال؛ أيّ باب خلفي للطوارئ هو باب للمهاجم. وزرّ «تسجيل الخروج» هو المخرج الوحيد من شاشة التحقّق، حتى لا يعلق مستخدم في صفحة كل ما فيها يرفضه.

دليل مستخدم جديد [19](docs/user-guide/19-account-security.md).

### ميزة — سبرنت الإطلاق: PDPL — التصدير والحذف (هجرة `0061`)

**مبدأ التصميم: صاحبا بيانات لا واحد.** نحن المتحكّم في حساب **المكتب**، فطلب حذفه ننفّذه نحن. أما بيانات **المستأجر والمالك** فالمكتب هو المتحكّم فيها ونحن المعالج — فلا نستجيب لطلب المستأجر، بل **نعطي المكتب الأداة** لينفّذه. لذلك زوجان من الدوال لا واحد.

**ما لا يمسّه الحذف — وهذا هو الجزء المهم.** نظام حماية البيانات **لا يلغي التزام حفظ نظامياً**: الفاتورة الضريبية تلتقط `buyer_name` و`buyer_id` (0023) **لأن ZATCA تُلزم بذلك**. فـ`erase_party` يمسح السجل **التشغيلي** ويُبقي **المالي**. الوعد بأكثر من ذلك في الواجهة كذب، وحذف الفواتير صامتاً يعرّض المكتب لمخالفة لم يخترها. **والعدد المُحتفَظ به يُعرَض للمكتب** ليُبلغ صاحب البيانات بما بقي ولماذا.

**الحذف يُرفض على عقد نشط** — العقد الساري التزام بين طرفين، لا طلب خصوصية يُنفَّذ بضغطة.

**حذف حساب المكتب: مهلة ٣٠ يوماً.** فورياً كان سيعني أن نقرة خاطئة تُفني سجلّات نشاط تجاري.

**⚠️ المصيدة التي حدّدت التصميم كله:** كل مفاتيح `organization` الثلاثين **تتالٍ (cascade)** — بما فيها **`subscription_payment`، أي سجلّ مبيعاتنا نحن**. فحذف صفّ المنشأة كان سيأخذ حساباتنا الضريبية معه. الحلّ: نُرحّل ما يلزمنا حفظه إلى `retained_billing` (بلا مفتاح أجنبي، فالمنشأة تختفي) **ثم** نحذف الصف — فيقوم التتالي بالباقي. وهذا مقصود: قائمة `DELETE` مكتوبة يدوياً كانت ستُغفل أول جدول يُضاف لاحقاً وتترك بيانات شخصية خلفها **تحت وعد بأنها حُذفت**.

**🔐 أضعفتُ ثلاثة حرّاس أمنيين عمداً — وأغلقتُ ما فتحه ذلك.** التتالي يصطدم بحماية آخر مالك وبحارسَي «الإلحاق فقط» (`audit_log` و`subscription_event`). وهي تحمي التاريخ **داخل حساب حيّ**، لا لتجعل الحساب غير قابل للحذف. لكن `set_config()` يستطيع استدعاءها أي واجهة، و`authenticated` يملك **UPDATE على `membership`** — أي أن راية جلسة وحدها كانت ستسمح لمستخدم بتخفيض آخر مالك وترك مكتب بلا مسؤول. الإغلاق: `app.org_purge_in_progress()` يشترط **الراية + أن يكون `current_user` ليس `authenticated`/`anon`** — فداخل دالة `SECURITY DEFINER` المستخدم هو المالك، ومن المتصفّح هو `authenticated`. **أربعة اختبارات تثبت أن رفع الراية من حساب مستخدم لا يخترق أياً من الحرّاس الثلاثة.**

**الواجهة:** `/app/privacy` (تنزيل نسخة JSON · طلب الحذف بتأكيد مكتوب لا مربّع اختيار · إلغاء الطلب) · قسم الحذف في صفحة المستأجر · وحُدّثت **سياسة الخصوصية** لتقول ما يفعله الكود فعلاً بدل وعد عام · ودليل مستخدم جديد [18](docs/user-guide/18-privacy-data.md).

**الاختبارات: 373 خضراء** (41 + 119 + 127 + 49 + **37**). مجموعة `pdpl.mjs` جديدة.

### قرار + توثيق — نظام التصميم المستورد: نطاق التبنّي والتسلسل

ضُمّ [`docs/foundation/08-design-system-red-sea-marine.md`](docs/foundation/08-design-system-red-sea-marine.md) إلى المستودع بعد تدقيقه مقابل الكود الفعلي.

**التسلسل المعتمد: سبرنت الإطلاق ← البند الأخضر (نصف يوم) ← البند الأصفر بعد الإطلاق.** لا شيء في هذا المستند يتقدّم على الإطلاق: بنوده تلميع، وبنود الإطلاق التزامات قانونية وأمنية. الاستثناء الوحيد **الوصولية** — تركب مع سبرنت الإطلاق لأنها ليست تلميعاً.

**القرارات الثلاثة (§10):** الوضع الداكن **يبقى** (1032 استخدام `dark:` — ليس ميزة بل طريقة بناء المنتج) · **التيل يبقى** ونستعير الانضباط لا سُلَّم الألوان · **Tajawal يبقى**.

**المرفوض:** سُلَّم الألوان كاملاً (يصطدم بالعلامة وبـ`neutral`، ويستلزم ترحيل `brand.*` عبر المشروع) · تبديل الخطّ · **أصناف `@layer components`** لأنها نظام تنسيق ثانٍ موازٍ — وهو نفس الاعتراض الذي أبقى shadcn/ui خارج المشروع · `lucide-react` كأول اعتمادية واجهة.

**سبعة تصحيحات على المستند:**
- **ثلاثة عيوب داخلية:** `.btn-primary` كان مطلياً بـ`accent` و`.btn-secondary` بـ`primary` — الاسم يناقض الرمز · حلقة التركيز معرَّفة بلونين (`.input-field` كحلي مقابل `.focus-ring` برتقالي) · تقدير «يوم واحد» للوضع الداكن كان يجيب على السؤال الخطأ: الكلفة ليست متغيّرات `dark:` بل نظام التنسيق الثاني.
- **أربعة ادّعاءات قديمة:** الشريط الجانبي **مجموعات بالفعل** · شاشات القوائم تملك فلاتر في الـURL وهياكل تحميل بالفعل · `Drawer` يملك أغلب دلالات الحوار وينقصه **حبس Tab فقط** · الأيقونات SVG يدوية بلا مكتبة.

**🔴 ثغرة وصولية حقيقية كشفها التدقيق:** `components/upgrade-modal.tsx` نافذة **بلا أي دلالات حوار** — لا `role` ولا `aria-modal` ولا `Escape` ولا إدارة تركيز؛ يمكن للوحة المفاتيح النفاذ خلفها إلى الصفحة. مُدرَجة مع سبرنت الإطلاق.

**تصحيح `neutral`:** دمج لوحة المصدر يعيد طلاء **نصّ الواجهة كلها** من الرمادي إلى الأزرق‑الأسود `#1a1a2e`، لأن Tailwind يدمج `extend.colors` بعمق. صار القرار: إن استُوردت اللوحة يوماً، فاسم الرمز **`ink` لا `neutral`**.

### ميزة — هوية المستأجر: معرّف رئيسي واحد + ممثل المنشأة + كتالوج الأسماء التجارية (هجرتا `0057` و`0058`)

**القاعدة المعتمدة:** الفرد يُعرَّف بـ**واحد** من (هوية وطنية / إقامة / **جواز**)؛ والمؤسسة والشركة تُعرَّفان بـ**الرقم الموحّد (700) إلزامياً**، وتُصبح بيانات الشخص هي **بيانات ممثل المنشأة** (اسم + هوية + جوال) وهي مطلوبة. ولكل الحالتين باب استثناء صريح: **حقل «سبب الإعفاء»** للجهات الحكومية والسفارات والكيانات الأجنبية بلا رقم موحّد — لأن ترك الحقل فارغاً لا يفرّق بين «لا يوجد» و«لم يُدخل».

**`0057` — النموذج.**
- `party.entity_type` انتقل من `tenant` إلى `party`. **لم يكن اختياراً:** `createTenant` يُدرج `party` **قبل** `tenant`، فلا يمكن لقيد أن يقرأ النوع وقت الإدراج، و`CHECK` لا يعبر جدولين. و`tenant.tenant_type`/`tenant_kind` صارا **مرآتين يحفظهما مشغّل** بدل أن تكتبهما الواجهة — وهذا يُنهي انحراف الأعمدة الثلاثة المتوازية.
- `party.primary_id` **عمود محسوب** فوق الأعمدة القائمة (`coalesce(unified_number, national_id, iqama_id, passport_no)`) — بلا مصدرَي حقيقة وبلا ترحيل بيانات. البحث والتحقق من التكرار كلاهما يقرأ هذا العمود.
- `party.identity_complete` عمود محسوب يقود شارة **«بيانات ناقصة»**.
- **الإلزام بمشغّل لا بقيد `CHECK`:** قيد `NOT VALID` يظلّ يُطلق عند **تعديل** صفّ قديم، فيمنع تصحيح رقم جوال على سجلّ سابق. المشغّل يقارن `OLD` بـ`NEW` فينفّذ القرار المتّفق عليه: **يمنع الناقص الجديد، ويُبقي القديم قابلاً للتعديل، ولا يسمح لسجلّ مكتمل بأن يُفرَّغ**.
- **منع التكرار بمشغّل كذلك:** فهرس فريد كان سيُفشل تطبيق الهجرة كلها لو كانت هناك تكرارات قديمة من الاستيراد. التكرار القديم يُحتمل، والجديد يُرفض.
- تطبيع تلقائي: المعرّفات تُخزَّن **أرقاماً فقط** والجواز بحروف كبيرة — فيتطابق البحث سواء كُتب الرقم بمسافات أو بدونها.
- `app.trade_name` (اسم + رقم رخصة البلدية + تاريخ الانتهاء) + `contract.trade_name_id`. **العقد يحتفظ بنسخته النصّية** من الاسم لأنه وثيقة قانونية تُقرأ كما وُقّعت.
- صيغ المعرّفات في **مكان واحد** (`app.id_pattern`) يقرأه المشغّل والمستورد معاً.

**`0058` — المستورد.** كان سيصير باباً خلفياً يلتفّ على كل ما سبق: يُدرج مستأجراً بلا معرّف، فيرفضه حارس `0057`، فتسقط **الدفعة كلها** برسالة Postgres خام. الآن يُرفض الصفّ **أثناء التحقق** برسالة عربية على الحقل المخالف، ويقبل الملف أعمدة النوع والرقم الموحّد والسجل والضريبي وبيانات الممثل، ويكشف **التكرار داخل الملف** كما يكشفه مع المنصة.

**🐞 عطل قديم كشفه الاختبار:** استيراد المستأجرين **لم يكن يعمل أصلاً** — `(n->>'email')::citext` داخل دالة `search_path = app` لا يُحلّ لأن `citext` في `public`، فأول صفّ مستأجر يصل إلى `import_commit` كان يفشل. صار الاسم مؤهّلاً `public.citext`.

**⚠️ تصادم خطير أوقفه الاختبار قبل التطبيق:** أسميت مشغّلي الجديد `party_identity_guard`، وهو **اسم يملكه `0013`** لحارس ربط الهوية بالطرف. `drop trigger if exists` كان سيحذف حارساً أمنياً بصمت. أعيدت التسمية إلى `party_identity_rules` — وسقوط اختبار `verify.mjs` هو ما كشفه.

**الواجهة.** نوع الهوية صار حقلاً صريحاً (وطنية/إقامة/جواز) بدل خانة واحدة تخلط المواطن بالمقيم · حقول الممثل **تختفي للفرد** بعد أن كانت تظهر في كل عقد · الاسم التجاري صار قائمة منسدلة من كتالوج المستأجر · البحث صار يشمل **الهوية والرقم الموحّد والجوال واسم الممثل** بعد أن كان بالاسم فقط · شارة «بيانات ناقصة» على البطاقة وفي صفحة المستأجر · تلميح **«الممثل نفسه مسجّل على N منشأة أخرى»** يجيب على «شخص واحد يملك عدة سجلات تجارية» بلا جدول أشخاص منفصل · `components/tenant-fields.tsx` يجمع حقول الإضافة والتعديل في مصدر واحد.

**الاختبارات:** ملف `identity.mjs` جديد (**49 تأكيداً**). المجموع **321** تأكيداً خضراء: `verify` 41 · `phase3` 104 · `platform` 127 · `identity` 49.

---

## 2026-07-30

### تحسين — استكمال بنود المراجعة: توحيد ٥ و٦ · تسريع ٣ (هجرة `0056`) · تقسيم الاختبارات ٧

**٥) ترجمة أخطاء SQL كانت في أربع نسخ.** `app/platform/actions.ts` كانت تحمل أربع خرائط `Record<string,string>` متطابقة البنية — رمز خطأ جديد يُترجَم في موضع ويظهر خاماً في ثلاثة. صارت `PLATFORM_ERRORS_AR` + `platformErrorAr()` في `lib/platform.ts` تجمع **كل** الرموز الخمسة عشر. والدالة **تُرجع الرسالة الخام** عند عدم التعرّف بدل «حدث خطأ» — الرمز غير المترجَم ثغرة في الخريطة، وإخفاؤه يخفي الثغرة.

**٦) شرائح الفلترة كانت في ثلاث نسخ، ثالثتها منحرفة.** المعرفة المكرَّرة ليست الشكل بل **أن تغيير فلتر يجب أن يحمل بقية فلاتر الصفحة في الرابط** — وكل صفحة أعادت بناء ذلك يدوياً. `components/platform/filter-chips.tsx` تحمله مرّة واحدة. ومكسب جانبي: فلتر نوع العملية في مركز التدقيق صار له شريحة «الكل» مثل بقية الشاشات بدل زرّ «×» مختلف.

**٣) تنبيه «بلوغ السقف» كان أغلى استعلام في اللوحة — وعلى الصفحة الرئيسية.** لكل اشتراك: **ست نداءات `plan_limit` وثلاث `usage_count`**، وكل واحدة `count(*)`. عند ألف مكتب ≈ **تسعة آلاف استعلام تجميع** في كل فتح لصفحة التنبيهات **ولكل فتح للوحة التنفيذية** (شريط التنبيهات يستدعيها). صارت **ثلاث تجميعات مُجمَّعة تُحسب مرّة وتُوصَل** — نفس الإجابة، وكلفة لا تتضاعف بعدد المكاتب.

> **أُبقيت سلوكية واحدة كما هي عمداً وأُعلنت:** المنشآت المحذوفة ناعماً ما تزال معدودة في هذا التنبيه بينما `platform_kpis` تستثنيها — فالاثنان يختلفان. **إعادة الهيكلة ليست موضع تغيير الإجابة**، والبند مُسجَّل.

**٧) `phase3.mjs` كان 1040 سطراً.** فُصلت اختبارات اللوحة إلى **`platform.mjs` مستقلّ يُقلع بقاعدته الخاصة ويبذر ما يفحصه فقط** — بدل الاتّكاء على تهيئة مشتركة قد تجعل اختبار لوحة ينجح لأن اختبار مكتب غير ذي صلة ترك الصفّ المناسب خلفه. **العدد الإجمالي لم يتغيّر: 230 = 103 + 127.**

- `phase3.mjs` **103/103** · `platform.mjs` **127/127** · `verify.mjs` 41/41 · `typecheck` و`build` نظيفان.
- **يبقى مؤجَّلاً بقرارك:** البند ٤ (`platform_org_activity` تُحسب لكل المكاتب لعرض صفحة من ٢٠).

### إصلاح — نتائج مراجعة `clean-code-guard` للوحة الإدارة العليا (هجرة `0055`)

**١) قوائم الخطط كانت مزروعة في الكود بينما الكتالوج صار قابلاً للتحرير.** ثلاثة مواضع تقول `["basic","pro","enterprise"]` بينما `app.plan` تقول غيرها — **وT‑3 هو من أدخل التناقض** حين جعل الكتالوج قابلاً للتعديل بلا هجرة. أضِف خطة «النمو» من مركز الاشتراكات ولن تجدها في **أي** شاشة تعيين. الآن الصفحتان تقرآن الكتالوج وتمرّرانه لـ`SubscriptionDrawer` و`TenantActions`، والأسماء المعروضة صارت **العربية** من `plan.name_ar` بدل الرموز.

**٢) فخّ في `platform_broadcast` — ثم عيب ثانٍ كشفه الاختبار.** `array_agg` فوق **صفر صفوف يُعيد NULL**، وهو نفس الـNULL الذي كان يعني «بلا تقييد» — فـ`{"orgs": []}` أي «لا ترسل لأحد» **كان يبثّ للجميع**. وأثناء كتابة الاختبار ظهر عيب ثانٍ في المفتاح نفسه: `value::text` على نصّ jsonb **يُبقي علامتَي التنصيص**، فأي قائمة **غير فارغة كانت ترفع خطأً**. أي أن المفتاح **لم يكن له مُدخَل صحيح إطلاقاً**: الفارغة تصل للجميع، وغير الفارغة تنهار.

> لم يكن أيٌّ منهما قابلاً للوصول من اللوحة (المُنشئ يرسل الحالة والخطة فقط) — **وهذا بالضبط سبب إصلاحهما الآن**: المفتاح جزء من عقد الدالة وموثَّق، والمستدعي التالي كان سيجد **أسوأ عطل ممكن في أقل عملية قابلية للتراجع في المنتج**.

**٨) تجربة صفر يوم.** حقل رقمي فارغ → `Number("")` = 0، و SQL كان يقبل `trial_days = 0` → **مكتب جديد مقفول لحظة إنشائه**. الأرضية صارت `>= 1` في SQL، **والواجهة ترفض الحقل الفارغ برسالة** بدل تمريره صفراً بصمت — الفراغ ليس صفراً.

- 3 اختبارات جديدة (`phase3.mjs` **229/229**): الجمهور الفارغ صراحةً يصل **لصفر**، والقائمة الصريحة تصل **لها بالضبط**، وتجربة الصفر مرفوضة · `typecheck` و`build` نظيفان.
- **مؤجَّل من المراجعة بقرارك:** توحيد ترجمة الأخطاء وشرائح الفلترة (٥ و٦) · كلفة `limit_reached` و`platform_org_activity` (٣ و٤) · تقسيم `phase3.mjs` (٧).
- الملفات: `supabase/migrations/0055_platform_fixes.sql` · `lib/platform.ts` · `app/platform/tenants/{page,[orgId]/page}.tsx` · `app/platform/actions.ts` · `components/platform/{subscription-drawer,tenant-actions}.tsx`

### ميزة — Sprint T‑5: البثّ + إدارة الميزات + إعدادات المنصة (هجرة `0054`) — **اكتملت لوحة الإدارة العليا**

**الإعدادات — ونصفها لا يجوز أن يكون في قاعدة بيانات.** ما يسمّيه البرومت «إعدادات المنصة» ينقسم قسمين: **مفاتيح المزوّدين** (Resend · Moyasar · `CRON_SECRET` · مفتاح الخدمة) **بيئةٌ لا إعدادات**، ولوحةٌ تقرأها أو تكتبها **تحوّل اختراق حساب مشغّل واحد إلى اختراق حساب الدفع**. تبقى في البيئة، وتُعرض **حالتها فقط** («مضبوط / غير مضبوط») ولا تُقرأ قيمتها أبداً. والقسم الآخر — ما كان يحتاج **هجرة** لتغييره — صار في `platform_setting`: **مدة التجربة · خطة البداية · بريد وجوال الدعم · اسم مُرسِل البثّ**. و`create_organization` صارت تقرأ الأولين بدل ثابتَي `'basic'` و`interval '30 days'` المزروعَين في الكود منذ `0036`. **الاختبار ينشئ مكتباً جديداً بعد تغيير الإعداد ويؤكّد أنه بدأ على الخطة والمدة الجديدتين.**

**الميزات — طبقة عامة فوق `feature_flag` لكل مكتب.** `app.feature_flag` (منذ `0005`) لكل منشأة ولا طبقة عامة له، فـ«فعّلها للجميع» و«فعّلها لـ١٠٪ من المكاتب» **لم يكونا قابلَين للتعبير**. `platform_flag` يضيفها، و`feature_enabled(org, key)` تحسم بترتيب ثابت: **استثناء المكتب ← الخطة المطلوبة ← مفعّلة للجميع ← نسبة الإطلاق**. و**ما لا مفتاح له مغلق دائماً** — الميزة المجهولة لا تُفتح بالخطأ. والنسبة تُحسب من **تجزئة المكتب والمفتاح معاً**، فجواب المكتب **لا يتذبذب بين طلب وآخر**، و**ميزتان بـ١٠٪ لا تختاران نفس الـ١٠٪**. وهذا يفي أيضاً بـ«ميزات لكل خطة» التي أُجِّلت من T‑3 — عبر `required_plan` بترتيب `plan.sort`، بموضع واحد لا موضعين.

**البثّ — الأقل قابلية للتراجع في المنتج كلّه، فخطوتان بقصد.** `p_dry_run` **يحسب الجمهور ولا يكتب شيئاً**؛ ولا يظهر زرّ الإرسال إلا **بعد أن يرى المشغّل العدد**، ويظلّ يحتاج تأكيداً مؤشَّراً. و**تعديل أي حقل بعد المعاينة يُلغيها**: عددٌ حُسِب لـ«المكاتب المتأخرة» يجب ألّا يبقى على الشاشة والجمهور يقول بهدوء «الجميع». والإرسال يعيد استخدام **طابور الإشعارات والمُصرِّف المجدول نفسيهما**، فالبثّ يُسلَّم بالآلة ذاتها ويُتابَع من صفحة الصحة. وكل إرسال محفوظ بجمهوره ومداه ومُدقَّق.

- 26 اختباراً جديداً (`phase3.mjs` **226/226**) — منها: **المعاينة لا تكتب شيئاً**، و**الإرسال يصل بالضبط لمن عدّته المعاينة**، وثبات نسبة الإطلاق، وأسبقية استثناء المكتب في الاتجاهين، وأن `app.setting()` الداخلية **غير قابلة للاستدعاء أصلاً** (قاعدة `0053`).
- الملفات: `supabase/migrations/0054_platform_settings_flags_broadcast.sql` · `app/platform/{broadcast,features,settings}/page.tsx` · `app/platform/actions.ts` · `components/platform/broadcast-composer.tsx` · `components/platform/platform-sidebar.tsx`

> **بهذا تكتمل ١٢ من ١٤ وحدة.** المتبقّي بقرار موثَّق: **مركز الدعم** (لا جداول تذاكر — يتقاطع مع وحدة الصيانة المؤجَّلة) و**تحليلات المنتج** (لا يوجد تتبّع سلوك إطلاقاً، ويحتاج مراجعة PDPL).

### 🔴 أمن — دوال «service_role فقط» كانت مفتوحة لأي مستخدم مسجَّل (هجرة `0053`)

اكتُشف أثناء كتابة اختبارات صحّة T‑4، وهو **أقدم من هذا السبرنت**.

الهجرة `0001` تعلن:
```sql
alter default privileges in schema app grant execute on functions to anon, authenticated, service_role;
```
فكل دالة تُنشَأ في `app` تُمنح **صراحةً** لـ`anon` و`authenticated` لحظة إنشائها. ودوال الـwebhook والـcron كانت تكتفي بـ`revoke ... from public` — وهذا يزيل منح **PUBLIC** الضمني ولا يمسّ المنحَين الصريحَين. فبقيت **قابلة للاستدعاء من أي مستخدم مسجَّل**.

**الأخطر:** `apply_subscription_payment(intent, gateway_id, raw)` تُعلّم الاشتراك **مدفوعاً** وتدفع الفترة للأمام. ومستخدم مسجَّل يستطيع قراءة معرّف نيّة الدفع الخاصة به — **من صفّه هو** — ثم استدعاءها **فيمدّد اشتراكه بلا دفع**. تحقّقتُ على PG17: الاستدعاء **نُفِّذ فعلاً** وأعاد `PAYMENT_INTENT_NOT_FOUND` لمعرّف وهمي بدل أن يُرفَض. ونفس الشكل في دوال المداينة وطابور البريد ومطالبة التجديدات ورمز البطاقة، وفي `usage_count/plan_limit` (تسريب عدّادات منشأة أخرى).

**الدوال المحروسة لم تتأثر إطلاقاً** — فحصها داخلي (`is_platform_operator` / `has_org_access`)، وADR‑0006 يقول ذلك حرفياً: «المنح ليس هو ما يُصرّح». لكن دوال service_role **بلا أي فحص داخلي**؛ المنح كان دفاعها الوحيد ولم يكن موجوداً.

**القاعدة التي تُرسيها الهجرة:** *دالةٌ بلا فحص تصريح داخلي يجب أن تسحب المنح من `anon` و`authenticated` صراحةً — السحب من `PUBLIC` لا يكفي في هذا المخطط.* و**١٠ اختبارات** تُثبّت ذلك الآن (تعليم دفعة مدفوعة/فاشلة · تأجير طابور البريد · إعلان رسالة مُرسَلة · تأجير التجديدات · دفع مكتب إلى المداينة · ربط بطاقة بأي مكتب · قراءة استهلاك منشأة أخرى · تزوير دورة cron).

**بلا تغيير سلوك لأي مستدعٍ شرعي:** كلها تُستدعى إمّا من مسار خادمي يحمل مفتاح service_role، أو من داخل دالة `SECURITY DEFINER` أخرى تعمل بصلاحية المالك ولا تستشير منح المستدعي.

### ميزة — Sprint T‑4: صحة المنصة + التنبيهات + مركز التدقيق (هجرة `0052`)

**فجوة قياس أُغلقت أولاً: المهام المجدولة لم تكن تترك أثراً.** مُصرِّف البريد ومحرّك التجديد يعملان على Vercel Cron ولا يبلّغاننا بشيء — فسؤال «هل صُرّف طابور البريد ليلة أمس؟» **لم يكن له جواب**، و**cron توقّف عن العمل بصمت يبدو تماماً كـcron لا عمل لديه**. الجدول `app.cron_run` يسجّل كل دورة (النتيجة · المدة · الخطأ)، والمساران يكتبان فيه الآن في النجاح والفشل. هذا هو الفرق بين صفحة صحّة **تُبلِّغ** وأخرى **تُخمِّن**.

**صحة المنصة:** طابور البريد (معلّق · فاشل · **متأخر عن موعد إعادته**) · الإشعارات · **المهام المجدولة** بآخر تشغيل ونتيجته ومدته · المدفوعات و**«بانتظار إشعار البوابة»** · الاستيراد.

> **«خارج قياسنا»** قسم صريح: قاعدة البيانات والتخزين ودوال الحافة وVercel ومعدّل الأخطاء وتوفّر بوابة الدفع — **أنظمة خارج قاعدتنا**. لكلٍّ منها مكان الفحص وسببه. **نقطة خضراء لم نرصدها أسوأ من لا نقطة.**

**التنبيهات:** قائمة **واحدة مرتّبة بالأهمية** لا جداراً من بطاقات مبعثرة. `platform_alerts()` تُعيد `kind` ثابتاً فقط — **الصياغة والوجهة في الواجهة، وSQL لا يحمل مساراً**. وما عدده صفر **لا يُعرض أصلاً**، فالقائمة الفارغة تعني فعلاً أن كل ما نراقبه ضمن الحدود. والأشد: **فشل مهمة مجدولة — لأنه يُخفي بقية التنبيهات**؛ الطوابير التي تُصرّفها تتوقف فلا يعود شيء على الصفحة موثوقاً بحداثته. والتنبيهات تظهر أيضاً **شريطاً أعلى اللوحة التنفيذية**: من يفتح تلك الشاشة لا ينبغي أن يحتاج نقرة ثانية ليرى cron فاشلاً.

**مركز التدقيق — وخط العزل مُطبَّق فيه:** صفوف التدقيق تحمل شيئين مختلفين: **بيانات وصفية** للعملية (من · ماذا · لأي مكتب · متى) و**حمولة `detail`** التي قد تحمل أرقام المكتب في عمليات المكاتب. الوصفية **ليست بيانات مكتب** — هي شكل النشاط، من فئة العدد نفسها. الحمولة قد تكون. لذا: **الحمولة تُعرض لعمليات `platform.*` فقط**؛ اللوحة ترى أن مكتباً فعّل ١٢ عقداً الأسبوع الماضي **دون أن تقرأ عقداً واحداً**. والصفحة تكتب هذه القاعدة للمشغّل، والصف يقول «محجوب — بيانات مكتب». وفلتر العمليات **مبنيّ ممّا يحويه السجل فعلاً** لا من قائمة ثابتة تنجرف.

- 26 اختباراً جديداً (`phase3.mjs` **200/200**) · `verify.mjs` 41/41 · `typecheck` و`build` نظيفان.
- الملفات: `supabase/migrations/{0052,0053}` · `app/platform/{health,alerts,audit}/page.tsx` · `app/platform/page.tsx` · `app/api/cron/*/route.ts` · `lib/platform.ts` · `components/platform/platform-sidebar.tsx`

### ميزة — Sprint T‑3: مركز الاشتراكات + مركز الفوترة (هجرة `0051`)

**كتالوج الخطط صار قابلاً للتحرير من اللوحة.** الهجرة `0036` عاملت الأسعار والحدود **بيانات** لا بنية («تعديلها لا يحتاج هجرة»)، لكن لم يكن ثمّة طريق محروس لتغييرها — فصار كل تغيير سعر هجرةً عملياً. `operator_upsert_plan` تُغلق ذلك بالعقد المعتاد: محروسة بالمشغّل، مُتحقَّق منها، **ومُدقَّقة كعملية منصة بلا منشأة مالكة**. وخفض حدّ لا يمسّ صفّاً قائماً — سقوف الخطط تمنع **الإنشاء الجديد** فقط (ق‑هـ) — والإيراد الماضي محميّ لأن `subscription_event` يلتقط السعر لحظتها (`0048`). **الاختبار يثبت أن إعادة التسعير لا تعيد كتابة ما سُجِّل.**

**مركز الاشتراكات:** التجارب (المنتهية خلال ٧/٣٠ يوماً، و**«منقضية بلا قرار»** — انتهى تاريخها ولم يُبتّ فيها) · التجديدات المستحقة · التجديد التلقائي مفعّل/متوقف · **«نشط بلا بطاقة محفوظة»** وهي أكثر قائمة قابلة للتصرّف: اشتراك نشط بلا بطاقة **لن يُجدِّد نفسه وسينقضي بصمت** ما لم يتصل به أحد · الموقوف/المتأخر/الملغى.

**مركز الفوترة:** كل المدفوعات عبر المنصة (بحث بالمكتب أو بمرجع البوابة + فلترة بالحالة + ترقيم) · نسبة نجاح العمليات · **«معلّقة > ٢٤ ساعة»** (بدأت ولم يصل إشعار) · المستردّات · **أسباب الفشل مجمّعة** — تُقرأ من مواضعها المعتادة في جسم الإشعار، **وحين لا يذكر أيٌّ منها سبباً يُكتب ذلك بدل اختلاق سبب**.

> **نسبة النجاح `null` لا ١٠٠٪ حين لا محاولات.** النسبة فوق صفر محاولات ليست حقيقة، والبطاقة تعرض «غير مُقاس بعد».

**ثلاثة بنود من البرومت لم تُبنَ — بقرار، وبلا أزرار وهمية:**
1. **الخصومات/الكوبونات:** لا يوجد نموذج خصم في القاعدة أصلاً — رمز وقيمة ومدة وتتبّع استخدام نظامٌ قائم بذاته، ونصفه أسوأ من عدمه.
2. **تنفيذ الاسترداد:** الحالة تُعرض، لكن **إصدار** استرداد يحتاج واجهة Moyasar وإشعاراً. وضع الصف «مُستردّاً» في القاعدة دون استرداد فعلي **كذبة على دفاترنا**.
3. **🔴 فواتير ضريبية للمكاتب — فجوة حقيقية:** نسجّل ما **دفعه** المكتب لنا ولا **نُصدر** له فاتورة. وكمورّد سعودي علينا إصدار فاتورة ضريبية (ZATCA) عن رسوم الاشتراك. بنينا ذلك للمكاتب تجاه مستأجريهم ولم نبنِه لأنفسنا.

**وميزات كل خطة** أُجِّلت إلى T‑5 (إدارة الميزات) ليكون لها **موضع واحد لا موضعان**. الصفحتان تكتبان هذه القيود للمشغّل صراحةً.

- 21 اختباراً جديداً (`phase3.mjs` **175/175**) منها ٤ اختبارات عزل و٤ للتحقق من مدخلات الخطة · `typecheck` و`build` نظيفان.
- الملفات: `supabase/migrations/0051_platform_billing.sql` · `app/platform/{subscriptions,billing}/page.tsx` · `app/platform/actions.ts` · `components/platform/plan-editor.tsx` · `components/platform/platform-sidebar.tsx`

### ميزة — Sprint T‑2: إدارة المكاتب + صفحة المكتب الكاملة (Tenant 360) (هجرة `0050`)

**حالة اشتراك جديدة: «موقوف».** حتى الآن كان «أوقِف هذا المكتب» يُكتب `canceled` — وهي تقول **إن العميل غادر**. الحقيقتان مختلفتان، وخلطهما **يفسد حساب الفقد**: إيقافٌ لعدم السداد كان سيُحتسب عميلاً مفقوداً. لا شيء آخر تغيّر: `subscription_active()` تعدّد الحالات الحيّة صراحةً وتفشل مغلقةً على ما عداها، فـ«موقوف» يمنع الإنشاء الجديد فور وجوده بينما **تبقى البيانات القائمة مقروءة وقابلة للتعديل** (الميثاق ق‑هـ). الاختبار يثبت الثلاثة.

**الأذرع التشغيلية** خلف قائمة (⋮) في القائمة وفي صفحة المكتب: تمديد التجربة ١٤/٣٠ يوماً · نقل بين الخطط · إيقاف · إعادة تفعيل. **ما لا ينطبق لا يُعرض** — تمديد التجربة لا معنى له لاشتراك نشط، وعرض ذراع لا تنطبق دعوةٌ لنقرة تنتهي برسالة خطأ. و**الإيقاف يطلب سبباً ولا يُرسَل بدونه**: يقطع عميلاً يدفع، والسبب هو ما يقرأه مشغّل لاحق في سجل التدقيق ليفهم لماذا.

`operator_extend_trial` **تحسب من اليوم لا من نهاية تجربة انقضت**: تمديد تجربة انتهت قبل شهر بـ١٤ يوماً من تاريخها القديم يمنح المكتب تجربة **منتهية أصلاً**.

**Tenant 360** — نداء واحد يعطي المكتب كاملاً: الاشتراك · الاستهلاك مقابل سقف الخطة · **المحفظة أعداداً** (عقارات · وحدات ونسبة الإشغال · عقود · ملّاك · مستأجرون) · ما دفعه **لنا** · البطاقة المحفوظة · فريق المكتب · النشاط · مسار الاشتراك · سجل المدفوعات.

> **توضيح فرضه هذا السبرنت وأثبته في [ADR‑0006](docs/adr/0006-platform-console-access.md):** **فريق المكتب** (الهويات التي تسجّل الدخول عندنا، بدورها وآخر دخول) **بيانات علاقة لا بيانات مكتب** — الدعم لا يستطيع مساعدة مكتب دون معرفة بمن يتصل. أما **عملاء المكتب** (المُلّاك والمستأجرون وعقودهم) فيبقون **أعداداً ولا شيء غيرها**، ولا يظهر **ريال واحد من مال المكتب** في هذه الصفحة. الصفحة تكتب ذلك صراحةً للمشغّل.

**«Reset Limits» في البرومت لا ينطبق على معمارنا** ولم أبنِ زرّاً وهمياً له: الحدود مشتقّة من الخطة، والاستهلاك عدّ حيّ للعقارات/الوحدات/الأعضاء — لا توجد عدّادات استهلاك تُصفَّر. البديل الحقيقي (لو أردته) **تجاوز حدود لكل مكتب** وهو ميزة جديدة لا «تصفير».

- 16 اختباراً جديداً (`phase3.mjs` **154/154**) منها ٣ اختبارات عزل، واختبار يثبت أن الإيقاف **يُسجَّل حالةً مستقلة لا إلغاءً**.
- الملفات: `supabase/migrations/0050_platform_tenant_360.sql` · `app/platform/tenants/[orgId]/page.tsx` · `app/platform/actions.ts` · `components/platform/tenant-actions.tsx` · `lib/platform.ts` · `lib/labels.ts`

### ميزة — Sprint T‑1: مساحة `/platform` المستقلة + اللوحة التنفيذية (هجرة `0049`)

**المساحة.** `app/platform/` بـ layout وقائمة جانبية ومكوّنات وإجراءات خاصة بها — لا تستورد شيئاً من لوحة المكتب ولا تقرأ ترويسة المنشأة النشطة. القائمة **داكنة عمداً** حتى لا تختلط بلوحة المكتب: أحد هذه الشاشات يغيّر فوترة عميل. الوحدات غير المبنية **مُدرَجة ومعلَّمة «قريباً»** لا مخفيّة — الغائب يُقرأ «ضائع»، والمعلَّم يُقرأ «لم يحن بعد».

**`/operator` حُذف نهائياً** ونُقلت شاشتاه إلى `/platform/tenants` و`/platform/tenants/[orgId]`. سطحان للإدارة العليا كارثة صيانة وأمان؛ لا يبقى إلا واحد.

> **تصحيح للبرومت:** «Middleware مستقل» **غير ممكن في Next.js** — ملف واحد لكل تطبيق. البديل المكافئ (والأقوى) مُطبَّق: الـ middleware يقرّر «مسجَّل دخول أم لا» فقط، و**بوّابة `is_platform_operator()` في `app/platform/layout.tsx`** تقرّر من يرى المساحة، **وكل دالة منصة تعيد الفحص في SQL**. وغير المشغّل يحصل على `notFound()` لا رفضاً — اللوحة لا تُعلن وجودها لمن لا شأن له بها.

**اللوحة التنفيذية** — أربع دوال قراءة في `0049`، كلها بعقد ADR‑0006:
- **MRR يحسب `active` فقط.** التجربة لا تدفع، و«الممنوح» منحة — ضمّ أيّهما للإيراد المتكرّر تضخيم له. و`past_due` متعاقد لكنه لا يُحصّل، فيُعرض **«إيراد معرّض للخطر»** بدل ضمّه بصمت أو إسقاطه بصمت. الاختبارات تثبت القاعدة **بالحركة**: إضافة اشتراك من كل نوع على نفس الخطة المسعّرة ومراقبة ما يفعله MRR.
- **`trend_since` يتجاهل البذرة المُستنتَجة** من `0048` (المؤرّخة بتاريخ إنشاء كل اشتراك) — وإلا ادّعت اللوحة أشهراً من الاتجاه لم تُسجَّل قط. واللوحة تكتب صراحةً متى بدأ التسجيل.
- **الأشهر الفارغة تُرجَع أصفاراً لا تُحذف**: فجوة يُغلقها الرسم البياني تدّعي أن الإيراد استمر ولم يستمر.
- **توزيع الخطط يُرجع كل خطة** بما فيها التي لا أحد عليها — الشريحة الفارغة هي نفسها الجواب على «هل يشتري أحد المؤسسية؟».
- **أعلى العملاء** ترتيب بما **دُفع فعلاً** لا بما فُوتر.

**الرسوم البيانية SVG تُرسَم على الخادم** — بلا مكتبة وبلا JS على العميل. الوقت يجري يساراً←يميناً رغم RTL: الأشهر خط أعداد، وكل أداة مالية يستخدمها القارئ ترسمها هكذا، فقلب هذا الرسم وحده هو المفاجئ. وبطاقة المؤشر تعرض **«غير مُقاس بعد»** لا صفراً حين لا تتوفّر البيانات — الصفر ادّعاء قياس.

- 13 اختباراً جديداً (`phase3.mjs` **138/138**) منها **٤ اختبارات عزل** للدوال الأربع · `verify.mjs` 41/41 · `build` نجح · حالات تحميل/فراغ/خطأ ووضع داكن وRTL مغطّاة.
- الملفات: `supabase/migrations/0049_platform_kpis.sql` · `app/platform/{layout,page,loading,actions}` · `app/platform/tenants/…` · `components/platform/{platform-sidebar,stat-card,bar-chart,subscription-drawer}.tsx` · `middleware.ts` · **حُذف** `app/operator/**` و`components/operator-edit-drawer.tsx`

### ميزة — Sprint T‑0: أساس منصة الإدارة العليا (هجرة `0048`) + [ADR‑0006](docs/adr/0006-platform-console-access.md)

أول سبرنت في مسار لوحة الإدارة العليا. **لا لوحة بعد** — هذا ما تقف عليه، وثلاث ثغرات كانت تمنع بناءها بصدق:

1. **فجوة التاريخ.** `org_subscription` يُكتب فوق نفسه، فالفقد (Churn) والنمو ومسار الإيراد **لم تكن قابلة للحساب — البيانات لم تُوجد قط**. الجديد `app.subscription_event` سجلّ **ملحق فقط** يكتبه **مُشغِّل على الجدول**، فيلتقط كل تغيير أيّاً كان مصدره (دالة المشغّل · محرّك التجديد · webhook · SQL يدوي) بلا موضع نداء يجب تذكّر تعديله. وسعر الخطة **يُلتقط لحظتها** حتى لا يُعيد تسعيرٌ لاحق كتابةَ إيراد الماضي.
2. **فجوة التدقيق** (الميثاق §١١ بند ٥). `operator_set_subscription` تغيّر خطة عميل أو توقف حسابه ولم تكن **تترك أثراً**. صارت تكتب `platform.subscription_update` بالحالة **قبل** التغيير. مركز التدقيق بلا هذا كان سيكون مسرحاً.
3. **فجوة التوسّع.** `operator_list_orgs()` كانت تُعيد **كل** المنشآت، لكل واحدة ثلاث عدّات. عند ألف مكتب = ثلاثة آلاف استعلام عدّ في كل فتح للصفحة. حلّت محلّها `app.platform_list_orgs(...)`: بحث وفلترة وترقيم في نداء واحد، وتحمل **حدود الخطة** ليُرسم الاستهلاك مقابلها. وتمرير `p_org` يعطي صفّاً واحداً، فصفحة المنشأة تقرأ من **نفس الدالة** ولا تنشأ نسختان من «كيف يبدو المكتب».

**عيب كامن كشفه العمل:** `write_audit` كانت تأخذ `membership_id` من `current_membership_id()` المرتبطة بترويسة **المنشأة النشطة** لا بالمنشأة المُدقَّقة. لعمليات المكتب تتطابقان دائماً فلم يظهر الخطأ؛ لكن عملية منصة على منشأة أخرى — أو `org.create` نفسها — كانت تختم **عضوية في منشأة مختلفة**، فتُقرأ كأن تلك العضوية هي التي تصرّفت. الآن تُسجَّل العضوية فقط إن كانت في المنشأة المُدقَّقة نفسها، وإلا `NULL` — وهو الجواب الصادق وعلامة عملية المنصة.

**القرارات المعتمدة (ADR‑0006):** «أعداد لا صفوف» — المنصة ترى بياناتها كاملةً وبيانات المكتب **أعداداً فقط** · **لا انتحال شخصية** (PDPL + يجعل اختراق حساب مشغّل اختراقاً للمنصة) · لا شاشة لإضافة مشغّل · كل دالة منصة: DEFINER + `FORBIDDEN` أولاً + اختبار عزل.

**العرض:** بحث خادمي + فلاتر حالة + ترقيم (نفس نمط قوائم النظام) · شارات الحالة بالعربية · **مؤشّر استهلاك** جديد (`components/usage-meter.tsx`) يرسم الاستخدام مقابل سقف الخطة ويتلوّن كهرماناً عند ٨٠٪ وأحمر عند التجاوز — الرقم وحده لا يقول إن المكتب على وشك الاصطدام بالسقف · **مسار الاشتراك** خط زمني في صفحة المنشأة، ووقائع ما قبل بدء التسجيل تُوسم صراحةً «مُستنتَج».

- 12 اختباراً جديداً (`phase3.mjs` **122/122**) منها ثلاثة اختبارات عزل تثبت رفض غير المشغّل · `verify.mjs` 41/41 · `build` نجح.
- الملفات: `supabase/migrations/0048_platform_foundation.sql` · `docs/adr/0006-platform-console-access.md` · `lib/platform.ts` · `components/usage-meter.tsx` · `app/operator/page.tsx` · `app/operator/[orgId]/page.tsx` · `lib/labels.ts` · `components/{list-toolbar,pagination,operator-edit-drawer}.tsx` · `SCHEMA.md`

### إصلاح — ثلاث ثغرات في مسار الاستيراد نفسه (هجرة `0047`)

استكمالاً لـ `0046` الذي أصلح القارئَين المشتركَين، بقيت ثلاث ثغرات في `import_validate`:

1. **«نسبة الضريبة»** كانت تُحوَّل مباشرة `::numeric` — فعلامة `%` **تُسقط الدفعة كاملة** (نفس عطل 0046 طبقةً أعلى). **والأخطر:** القالب يوثّق الكسر `0.15`، لكن الناس يكتبون `15` — وكان `15` يمرّ من التحويل **فيضرب الاستحقاق في ١٥**. فاتورة مضخّمة ١٠٠ ضعف بصمت أثقل من الانهيار. الآن `0.15` و`15` و`15%` و`٪١٥` كلها تعني ١٥٪، وما فوق ١٠٠٪ يُرفض بخطأ في صفه.
2. **«المساحة»** لم تكن تُتحقَّق **إطلاقاً**: `import_validate` يخزّن النص الخام و`import_commit` يحوّله `::numeric`. فقيمة مثل `120 م²` تمرّ من التحقق، ويُخبَر المستخدم أن الصف **صحيح**، ويضغط «اعتماد» — فينهار التحويل داخل `import_commit` **الذي يعمل في معاملة واحدة بالتصميم**، فتُلغى الدفعة بأكملها ولا يُحفظ شيء. كل حقل يحوّله `import_commit` يجب إثبات قابليته للقراءة أثناء التحقق؛ كانت هذه آخر ثغرة.
3. **لا شيء كان يحدّ من أثر أي ثقب قراءة.** صفوف الاستيراد مُدخَل غير موثوق، وقد أفلت منها اثنان بالفعل — فصار جسم الحلقة يحوّل أي خطأ تحويل متبقٍّ إلى **خطأ في صفه** بدل إسقاط الدفعة.

- توحيد المعرفة: قاعدة «اقرأ رقماً كتبه إنسان» صارت في دالة واحدة `normalize_decimal`، و`normalize_amount_halalas` صار تعبيراً عنها بعقده نفسه.
- **تحقّقتُ صراحةً** أن `when data_exception` يلتقط فعلاً رمزاً من فئته (22P02) — وإلا كانت الشبكة زينة.
- 7 اختبارات جديدة (`verify.mjs` 41/41) · `phase3.mjs` 109/109 · `build` نجح · الدليل حُدّث بالصيغ المقبولة.
- الملفات: `supabase/migrations/0047_import_validate_hardening.sql` · `docs/user-guide/09-import.md` · `supabase/tests/local/verify.mjs`

### إصلاح — خلية تاريخ واحدة كانت تُسقط دفعة الاستيراد كاملة (هجرة `0046`)

- **البلاغ:** فشل استيراد العقود بالخطأ `date/time field value out of range: "2026/16/06"`. **سبب الملف:** التواريخ مكتوبة **سنة‑يوم‑شهر** (`2026-16-06`)، والقالب يتوقّع `YYYY-MM-DD` — فقُرئ `16` كشهر. الصحيح `2026-06-16`.
- **العيب الذي كشفه:** `app.normalize_date` تستدعي `to_date` التي **ترمي استثناءً** عند قيمة خارج المدى، فيخرج الاستثناء من حلقة `app.import_validate` قبل أن تصل إلى مسار «خطأ لكل صف» الموجود أصلاً — فتضيع نتيجة التحقق من **كل** الصفوف السليمة، ويرى المستخدم رسالة قاعدة بيانات إنجليزية بلا رقم صف.
- **نفس الشكل في المبالغ:** التنظيف يُبقي `-` و`.` في مواضعهما، فقيمة مثل `1.2.3` تصل إلى `::numeric` وترمي كذلك.
- **الإصلاح:** الدالتان تُعيدان `null` عند تعذّر القراءة — وهو ما يعامله كل مستدعٍ أصلاً بأنه «غير صالح، أبلغ المستخدم بالحقل». لا تغيير لأي مُدخل كان يُقرأ بنجاح.
- **كشفه الاختبار أيضاً:** `1,200.50 ر.س` **لم يكن يعمل قط** (نقطة «ر.س» تلتصق بالرقم) — كان ينهار، والآن يُقرأ ١٢٠٠٫٥٠ ريال بعد إزالة النقاط الطرفية فقط. النقطة الوسطى تبقى كما هي حتى يظل `1.2.3` **مرفوضاً** لا مُخمَّناً — المبالغ لا تُقرأ بالتخمين.
- **الواجهة والدليل:** صيغة التاريخ صارت مكتوبة تحت أزرار القوالب في شاشة الاستيراد وفي دليل المستخدم مع مثال صريح.
- 6 اختبارات تراجعية جديدة في `verify.mjs` (36/36) · `phase3.mjs` 109/109 · `build` نجح.
- الملفات: `supabase/migrations/0046_import_parse_hardening.sql` · `app/app/import/page.tsx` · `docs/user-guide/09-import.md` · `supabase/tests/local/verify.mjs`

### ميزة — Sprint S: شبكتا بطاقات الملّاك والمستأجرين (واجهة فقط، بلا هجرة)
تعميم نمط البطاقات على آخر شاشتَي كيانات.
- **الملّاك** (`components/owner-card.tsx` + `owners-grid.tsx`): تبويبات [الكل · أفراد · شركات · المنشأة] · البطاقة تعرض الاسم بأيقونة + شارة النوع + قائمة (⋮) · كبسولات (الهوية · الجوال · **الآيبان مُقنَّعاً** بآخر 4 أرقام مع اسم البنك) · **عدد العقارات في محفظته** · تذييل «كشف الحساب» + دعوة بوابة المالك (لغير الذاتي).
- **المستأجرون** (`components/tenant-card.tsx` + `tenants-grid.tsx`): تبويبات [الكل · أفراد · مؤسسات فردية · شركات] (من `tenant_type`) · البطاقة تعرض الاسم بأيقونة تتبدّل حسب النوع (فرد/منشأة) + شارة + قائمة (⋮) · كبسولات الهوية والجوال · **عدد العقود النشطة وقائمة الوحدات المستأجَرة** · تذييل «عرض التفاصيل» + دعوة بوابة المستأجر. **استُبدل الجدول بالشبكة.**
- **تنظيف مشترك:** استُخرج شريط التبويب المكرّر إلى `components/status-tabs.tsx` واحد تستخدمه الشبكتان (بدل نسخه مرة ثالثة).
- **أمان العرض:** الآيبان **لا يُعرض كاملاً** في القائمة — آخر 4 أرقام فقط؛ القيمة الكاملة تبقى في صفحة المالك حيث تعديلها مضبوط.
- `build` **exit 0** (حجم `/app/tenants` 3.04 → **4.62 kB**). بلا تغيير على الصلاحيات/RLS · لا هجرة.

### ميزة — Sprint R: شبكة بطاقات الوحدات الذكية (واجهة فقط، بلا هجرة)
إعادة تصميم شاشة «الوحدات» بنفس روح صفحة العقود.
- **الشبكة والفلترة** (`components/units-grid.tsx`): شبكة متجاوبة (عمودان md · ثلاثة lg) + **شريط تبويب حالة** بعدّادات. **تعديل مقصود على الطلب:** التبويبات **خمسة** [الكل · الشاغرة · المؤجرة · تحت الصيانة · **أخرى**] لا ثلاثة — لأن `unit_status` في القاعدة **ستّ قيم** (يضاف: محجوزة/غير صالحة للتأجير/خارج الخدمة)، وثلاثة تبويبات كانت **ستُخفي وحدات** من الشاشة.
- **بطاقة الوحدة** (`components/unit-card.tsx`): إطار يتوهّج بلون الهوية عند المرور. **الرأس:** «وحدة {الرقم}» بخط بارز + شارة حالة ملوّنة (مؤجرة أخضر · شاغرة أزرق · صيانة برتقالي) + قائمة (⋮). **الجسم:** اسم العقار بأيقونة مبنى + **كبسولات المواصفات** (تصنيف العقار · الدور · المساحة · الغرف). **الإشغال:** المؤجّرة تعرض **اسم المستأجر والإيجار السنوي** (من العقد النشط)، والشاغرة تعرض «شاغرة — جاهزة للتأجير» بلون إيجابي. **التذييل:** «عرض التفاصيل» + **«+ إنشاء عقد»** للشاغرة فقط + زر التعديل (بصلاحية `manage_data`).
- **🔗 «إنشاء عقد» يعمل فعلياً:** الرابط `/app/contracts?add=1&unit=<id>` يفتح درج العقد **والوحدة محدَّدة مسبقاً** (`ContractForm` اكتسب `defaultUnitId`) — لا يترك المستخدم يبحث عنها يدوياً.
- **تعديلان على الطلب (بقرارك):** «نوع الاستخدام تجاري/سكني» **غير موجود على الوحدة** في المخطط → الكبسولة تعرض **تصنيف العقار** (`property_kind`) · «القيمة الإيجارية المستهدفة» **غير موجودة** → نص «جاهزة للتأجير» بدلها (بلا هجرة). · زر التبديل للجدول مستبعَد (كما في العقود).
- `build` **exit 0**. بلا تغيير على الصلاحيات/RLS · لا هجرة.

### ميزة — Sprint Q: شبكة بطاقات العقود الذكية + صفحة عقد قابلة للطباعة (واجهة فقط، بلا هجرة)
إعادة تصميم شاشة «عقود التأجير» من قائمة عمودية بسيطة إلى **شبكة بطاقات** بروح SaaS حديثة.
- **الشبكة والفلترة** (`components/contracts-grid.tsx`): شبكة متجاوبة (**عمودان md · ثلاثة lg**) + **شريط تبويب حالة** [الكل · نشط · **ينتهي قريباً** · منتهي] مع عدّاد لكل تبويب، فوق فلتر النص الفوري القائم (إعادة استخدام `FilterableCards` لا تكرار). تعريف «ينتهي قريباً» = **عقد نشط ينتهي خلال 60 يوماً** — نفس تعريف لوحة المؤشرات فيتّسق السطحان.
- **بطاقة العقد** (`components/contract-card.tsx`): إطار يتوهّج بلون الهوية عند المرور (`hover:border-brand`). **الرأس:** رقم العقد + **زر نسخ** (يعرض Toast تأكيد) · شارة حالة ملوّنة (أخضر/برتقالي/أحمر) + **قائمة خيارات (⋮)**. **الجسم:** اسم المستأجر بخط بارز مع أيقونة + كبسولة «العقار • وحدة». **المالي والزمني:** الإيجار السنوي بخط بارز + **شريط تقدّم يحسب نسبة المنقضي من مدة العقد تلقائياً** (بلون يتبع الحالة). **التذييل:** «عرض العقد» + «طباعة».
- **🆕 صفحة عقد قابلة للطباعة** `/app/contracts/[id]/print`: بنفس نمط السندات والفواتير (ترويسة المنشأة بالسجل والرقم الضريبي · أطراف العقد والعين المؤجرة · المدة والقيمة مع **تفقيط** المبلغ · بيانات منصة إيجار إن وُجدت · حقول توقيع). منها يحفظ المستخدم PDF عبر المتصفح. رابط الطباعة مضاف أيضاً في صفحة تفاصيل العقد.
- **خارج النطاق بقرارك:** زر التبديل بين الشبكة والجدول (Table View) — الشبكة فقط.
- **ملاحظة تنفيذية:** قائمة (⋮) تحوي **روابط تنقّل فقط** (عرض/طباعة/العقار/المستأجر) ولا تحوي إجراءات خطرة (تفعيل/إنهاء/تجديد) — فتلك تبقى في صفحة التفاصيل بتأكيداتها.
- `build` **exit 0**. بلا تغيير على الاستعلامات الحسّاسة/الصلاحيات/RLS · لا هجرة.

### إصلاح — مراجعة كود (clean-code-guard): عيب سلوكي + ربط مكوّنَين ميّتين + تنظيف
مراجعة منهجية للشيفرة الجديدة (سبرنتات N+1…P) وفق Clean Code/SOLID/DRY وأنماط فشل الذكاء الاصطناعي الـ14. **لا ثغرات أمنية ولا ابتلاع أخطاء ولا قيم وهمية.**
- **🐞 إصلاح فعلي — درج الإضافة كان يُعيد فتح نفسه بعد الحفظ الناجح** (`components/form-drawer.tsx`): الأثر كان مربوطاً بـ`searchParams`، وبما أنّ `createProperty` ينادي `revalidatePath` ثم يُرجع نجاحاً، تتغيّر هوية `searchParams` فيُعاد تشغيل الأثر بينما `?add=1` ما زال في الرابط → يُعاد فتح الدرج فور إغلاقه. الحل: حارس **تشغيل‑مرة‑واحدة** (`useRef`). يمسّ مسار زر (+) في الشريط الجانبي.
- **ربط Toast (كان ميّتاً):** رُكِّب `ToastProvider` في تخطيط التطبيق، وصارت النماذج الأربعة (عقار/وحدة/مستأجر/مالك) تعرض **رسالة نجاح لحظية** عند الحفظ بدل الإغلاق الصامت.
- **ربط Skeleton (كان ميّتاً):** مكوّن `ListSkeleton` جديد + `loading.tsx` لصفحات **العقارات والعقود والمستأجرين** — هيكل تحميل أثناء التنقّل بدل شاشة فارغة.
- **تنظيف:** توحيد مُساعد `first` من `lib/rows` بدل **4 نسخ محلية مكرّرة** (`firstOf`) في صفحات العقارات/الوحدات/المستأجرين/العقود · إزالة سلسلة اختيارية زائدة في `drawer.tsx` · إعادة تسمية `hi` → `highlightedIndex` في `combobox.tsx`.
- `build` **exit 0**. بلا تغيير على المخطط/الاستعلامات/الصلاحيات · لا هجرة.

### ميزة — Sprint P: توحيد ترقيم العقود + قسم «بيانات منصة إيجار» (هجرة `0045`)
**المشكلة:** كان الرقم يُولَّد فقط إذا تُرك الحقل فارغاً، وبصيغة `CT-<طابع زمني>` — فاختلطت أرقام يدوية بأرقام عشوائية الطول ولا تسلسل بينها.
- **ترقيم إلزامي وموحّد:** حُذف حقل الإدخال اليدوي **تماماً** (من نموذج العقد الجديد، ومن تعديل المسودة، ومن نموذج التجديد). صار التوليد **في قاعدة البيانات** (المكان الوحيد الذي يضمن الذرّية) بصيغة **`CT-YYYY-NNNNN`** — نفس العدّاد المتسلسل بلا فجوات لكل منشأة/سنة المستخدَم في السندات والفواتير والتوريدات (`next_counter`). **قرارك:** العقود القائمة تبقى بأرقامها كما هي. رقم صريح يُمرَّر يُحترَم، فيبقى ترقيم التجديد `<الأصل>-R<السنة>` كما هو.
- **قسم «بيانات منصة إيجار» (اختياري بالكامل):** قائمة منطوية أسفل تفاصيل العقد في **نموذج الإنشاء وتعديل المسودة**، وتُعرض للقراءة في تفاصيل العقد عند تعبئتها. الحقول: **رقم العقد في منصة إيجار** (كان موجوداً في المخطط منذ 0007 وأُظهر الآن) · **الوسيط العقاري**: اسم المكتب/رقم المكتب/ممثل المكتب · **سؤال البنود الإضافية** (نعم/لا عبر Radio، ويقبل «بلا إجابة»).
- **هجرة `0045`:** 4 أعمدة اختيارية (`ejar_broker_office`/`_number`/`_representative`/`ejar_has_extra_terms`) + مُشغِّل `tg_assign_contract_no`. **عرضية بحتة — لا تقود RLS ولا الضريبة ولا أي منطق.** ملاحظة تصميمية: حقول الوسيط **خارج** مجموعة تجميد العقد فتبقى قابلة للتحديث بعد التفعيل، بينما `ejar_contract_number` داخلها (يُجمَّد مع الحقول القانونية).
- **التحقق PG17:** **`verify.mjs` ٣١/٣١ + `phase3.mjs` ١٠٩/١٠٩** (أُضيف ٦: صيغة `CT-YYYY-NNNNN` · التسلسل يزيد واحداً · احترام الرقم الصريح · تخزين حقول إيجار · اختياريتها · قابلية تعديل حقول الوسيط) · `schema_all` (٤٥) · `build` **exit 0**.
- **أثر البيانات:** لا شيء — أعمدة اختيارية + مُشغِّل يعمل على الإدراج الجديد فقط. **⏳ يتطلّب تطبيقك:** هجرة **`0045`**.

## 2026-07-29

### تحسين — Sprint O-4: صفحة تفاصيل العقار الموحّدة بتبويبات (واجهة فقط، بلا هجرة) — إتمام مبادرة إعادة التخطيط
- **رأس موحّد:** بطاقة العقار (الاسم · الكود · التصنيف · المدينة/الحي · الصك) + **نموذج تغيير المالك انتقل داخل قسم قابل للطي** (مع التحذير والتأكيد كما هو) بدل أن يشغل الرأس.
- **٤ بطاقات إحصاء للعقار:** إجمالي الوحدات · مؤجرة · شاغرة · عقود نشطة.
- **تبويبات:** **[الوحدات]** — عدّاد + زر **«إضافة وحدة جديدة»** يفتح درجاً (بدل نموذج ظاهر دائماً) + جدول وحدات هذا العقار بشارات حالة · **[العقود]** — **جديد:** جدول عقود هذا العقار فقط (رقم/وحدة/مستأجر/حالة/فترة/إيجار + رابط العقد)، عبر `contract.property_id` المُفهرَس.
- **مؤجَّل بقرارك:** تبويب المرفقات + رمز QR/الملف العام (يحتاجان Storage وسبرنت أمني).
- **بلا تغيير على الاستعلامات الأخرى/الصلاحيات/RLS · لا هجرة.** `build` **exit 0**.
- **🎉 اكتملت مبادرة إعادة التخطيط (O-1…O-4):** شريط جانبي مُجمَّع · نماذج في أدراج · معالج إضافة عقار + Slide-Over للمالك · تفاصيل عقار بتبويبات.

### تحسين — Sprint O-3: معالج إضافة عقار (3 خطوات) + Slide-Over لإضافة المالك (واجهة فقط، بلا هجرة)
- **معالج (Wizard) لإضافة العقار:** نموذج العقار أُعيد بناؤه بثلاث خطوات مع مؤشّر خطوات قابل للنقر: **1) البيانات الأساسية** (الاسم/الكود/علاقة العقار/التصنيف/المالك) · **2) الصك والمساحة** (نوع العقار والإشغال، المدينة والحي، بيانات الصك، العدّادات، الوحدات المخطّطة) · **3) الصور والملفات** (لوحة «قريباً» — تحتاج Supabase Storage غير المبني بعد).
- **تفصيلة تقنية مهمة:** الخطوات الثلاث داخل **نموذج واحد**، والخطوات غير النشطة تُخفى بـ CSS (**لا تُفكَّك من DOM**) فيبقى الإرسال الواحد حاملاً كل الحقول؛ و`required` مربوط بالخطوة النشطة حتى لا يحاول المتصفح التحقق من حقل مخفي غير قابل للتركيز. زر «إضافة العقار» متاح في كل الخطوات (الخطوتان 2 و3 اختياريتان).
- **Slide-Over لإضافة المالك:** في `LandlordPicker` تحوّل نموذج «مالك جديد» المضمّن إلى **درج منزلق متداخل** فوق درج العقار (`z-[60]`، عبر خاصية `zClass` جديدة في `Drawer`) — يُضيف المالك ويختاره ويعود للنموذج دون فقدان المدخلات.
- **تحصين `Drawer`:** صار يُرندَر عبر **Portal إلى `body`** — فلا يمكن أن يتموضع لوح `fixed` بالنسبة لأبٍ يحمل `transform` (يُزيل مخاطرة تداخل الأدراج جذرياً لا اختباراً فقط).
- **بلا تغيير على `createProperty`/`/api/owners/quick`/الصلاحيات/RLS · لا هجرة.**
- **التحقق الحيّ:** `build` **exit 0** · على الخطوة 1: كتلة الخطوة 2 تحمل `hidden` ومع ذلك **`FormData` يحوي `deed_type` و`water_meter`** (19 مفتاحاً) — إثبات آلية الإرسال الواحد · «التالي» يُظهر الخطوة 2 و**`required` ينطفئ تلقائياً** على الحقل المخفي · تبديل العلاقة إلى «إدارة أملاك» يُظهر منتقي المالك، و«أضِف مالكاً جديداً» يفتح **درجاً متداخلاً `z-[60]` أبوه `BODY`** (Portal مؤكَّد) مع قفل التمرير · صفر أخطاء طرفية.

### تحسين — Sprint O-2: تطهير نماذج الإضافة إلى Drawers (واجهة فقط، بلا هجرة)
تنظيف أسطح القوائم: نُقلت نماذج «الإضافة» الظاهرة دائماً أعلى الصفحات إلى **درج منزلق** يفتحه زر بارز.
- **`components/form-drawer.tsx` (جديد):** زر «+ إضافة» يفتح النموذج في Drawer. **إغلاق تلقائي عند النجاح** عبر Context (`useFormDrawerClose`) — النماذج الأربعة (العقار/الوحدة/المستأجر/المالك) تستدعيه في `state.ok`. **فتح تلقائي عند `?add=1`** — فربط زرّي (+) في الشريط الجانبي بفتح الدرج مباشرةً.
- **5 صفحات طُهِّرت:** العقارات · الوحدات · المستأجرون · الملّاك · العقود — أُزيل قسم النموذج الدائم، وأُضيف زر «+ إضافة X» في رأس الصفحة (مقيَّد بـ`manage_data` حيث ينطبق). صفحات الشروط (أضِف عقاراً/مستأجراً أولاً) انتقلت داخل الدرج.
- **التحقق:** `tsc` + `build` **exit 0** · فحص حيّ محلي معزول: الدرج يفتح بالزر، الـ Context متوفّر للنموذج، **الإغلاق التلقائي عند النجاح** يعمل (قفل التمرير يعود)، و**`?add=1` يفتح الدرج تلقائياً**؛ صفر أخطاء طرفية. **بلا تغيير على الإجراءات/الصلاحيات/RLS · لا هجرة.**
- **التالي:** O-3 Wizard إضافة عقار (3 خطوات) + Slide-Over لإضافة المالك · O-4 تبويبات تفاصيل العقار.

### تحسين — Sprint O-1: شريط جانبي هيكلي بدل التنقّل العلوي (واجهة فقط، بلا هجرة)
مواءمة تخطيط النظام مع الأنظمة العقارية الاحترافية (نمط موجود) — أكبر تغيير بصري.
- **`components/app-sidebar.tsx` (جديد):** شريط جانبي RTL مثبَّت على اليمين، **مُجمَّع بأقسام**: الرئيسية · **إدارة العقارات** (العقارات/الوحدات/عقود التأجير/الملّاك/المستأجرون) · **المالية والتحصيل** (الفواتير/السندات/الاشتراك) · **الصيانة والطلبات** (معطّل «قريباً» — الوحدة غير مبنيّة بعد) · **الإدارة** (الاستيراد/الفريق/الإشعارات). أيقونات SVG لكل عنصر، إبراز الرابط النشط، شارة الإشعارات، **زر (+) سريع** بجانب العقارات وعقود التأجير.
- **متجاوب:** على الحاسوب شريط ثابت (المحتوى مُزاح `md:pr-64`)؛ على الجوال **شريط علوي + همبرغر يفتح درجاً منزلقاً** بنفس القائمة (حفاظاً على إصلاح Sprint M). `no-print` على الشريط (لا يظهر في السندات/الفواتير المطبوعة).
- **`app/app/layout.tsx`:** أُعيد هيكلته لاستخدام الشريط الجانبي؛ شاشة «بلا منشأة نشطة» تبقى بشريط علوي بسيط. حُذف `components/app-nav.tsx` القديم (ميّت).
- **التحقق:** `tsc` + `build` **exit 0** · فحص حيّ محلي معزول: الحاسوب (لا تمرير أفقي · 4 مجموعات · «قريباً» · زرّا +) · الجوال (الشريط مخفي · الهمبرغر يفتح الدرج بكل المجموعات + خروج · قفل تمرير) · صفر أخطاء طرفية. **بلا هجرة/تغيير منطق.**
- **ملاحظة:** زرّا (+) يوجّهان حالياً لصفحة القسم؛ يُحوَّلان لفتح Drawer الإضافة مباشرةً في O-2. **التالي:** O-2 نقل نماذج الإضافة إلى Drawers · O-3 Wizard إضافة عقار · O-4 تبويبات تفاصيل العقار.

### تحسين — Sprint N+5: لوحة المشغّل — تعديل بالسطر + فلترة فورية (واجهة فقط، بلا هجرة) — إتمام مبادرة إعادة التصميم
البند الأخير: لوحة مشغّل المنصّة (Super-Admin).
- **قائمة `/operator`:** أُعيد بناؤها بـ **فلترة فورية** (بحث بالاسم/الخطة/الحالة) + **`Badge`** ملوّن للحالة + فلاش نجاح/خطأ.
- **تعديل بالسطر (in-line):** لكل صف زر «تعديل» يفتح **درجاً منزلقاً** (`components/operator-edit-drawer.tsx`) بنموذج إدارة الاشتراك (الخطة/الحالة/التجربة/الفترة/ملاحظة) — دون مغادرة القائمة. رابط «التفاصيل» يبقى للانتقال لسجل المدفوعات.
- **`operatorSetSubscription`** اكتسب حقل `back` اختيارياً (مُحقَّق ضد open-redirect عبر `safeReturnTo`) فيرجع التعديل من القائمة إلى `/operator?ok=1` (يُحدَّث السجل)؛ صفحة التفصيل بلا تغيير (الافتراضي `/operator/<org>`).
- **التحقق:** `tsc` + `build` **exit 0**. مكوّنات FilterableTable/Drawer/نموذج‑بـ‑Server‑Action مثبتة في N+2/N+4. **بلا تغيير على RPC/RLS/بوابة المشغّل (`is_platform_operator`) · لا هجرة.**
- **🎉 اكتملت مبادرة إعادة تصميم النظام (N+1…N+5):** نظام مكوّنات موحّد · لوحة مكتب · فلترة كل القوائم · بوابتان بالتبويبات · Combobox+Drawers في النماذج · لوحة مشغّل بالتعديل‑بالسطر.

### تحسين — Sprint N+4: قوائم بحث فوري (Combobox) في النماذج + Drawer تحرير سريع (واجهة فقط، بلا هجرة)
تبسيط إدخال البيانات مع كثرة العناصر.
- **Combobox في النماذج:** استُبدلت منتقيات الكيانات في نموذج العقد (**الوحدة** + **المستأجر**) ونموذج الوحدة (**العقار**) من `<select>` عادي إلى **قائمة بحث فوري**. يُصدر الـ Combobox `hidden input` بنفس الاسم فتقرأه Server Actions دون أي تغيير في الخادم.
- **Drawer تحرير الوحدة:** في `/app/units` استُبدل موسّع `<details>` بزر «تعديل» يفتح **درجاً منزلقاً** (`components/unit-edit-drawer.tsx`) بنموذج التعديل؛ `updateUnit` يُعيد التوجيه إلى `/app/units` فيُحدَّث بعد الحفظ ويُغلق الدرج.
- **التحقق:** `tsc` + `build` **exit 0** · فحص حيّ محلي معزول: اختيار من Combobox داخل نموذج ثم إرسال → **FormData يحمل القيمة الصحيحة** (`unit_id=u2`)؛ Drawer يفتح مع قفل التمرير؛ صفر أخطاء طرفية. **بلا تغيير على الاستعلامات/الصلاحيات/RLS · لا هجرة.**
- **التالي:** N+5 لوحة المشغّل (تعديل بالسطر).

### تحسين — Sprint N+3: إعادة تصميم البوابتين بالتبويبات (واجهة فقط، بلا هجرة)
تحويل صفحتَي البوابة الطويلتين (تمرير) إلى واجهة **تبويبات سلسة** + بطاقات عصرية، مع الحفاظ التام على منطق البيانات والعزل بالهوية (`my_owner_links`/`my_tenant_links`).
- **بوابة المالك** (`app/portal/[ownerId]`): رأس + بطاقات ملخّص الفترة (`Card`) + منتقي المدى الزمني، ثم **تبويبات**: كشف الحساب · التوريدات · عقاراتي.
- **بوابة المستأجر** (`app/portal/tenant/[tenantId]`): رأس + بطاقة «إجمالي المتبقّي» (أحمر عند وجود مديونية)، ثم **تبويبات**: العقود والاستحقاقات (بطاقات + جداول + `Badge` للحالة) · دفعاتي (مع رابط السند القابل للطباعة).
- استخدام مكوّنات `Tabs`/`Card`/`Badge` من N+1 (محتوى التبويبات يُرندَر خادمياً ويُمرَّر كـ slots)، لوحة Slate. **بلا تغيير على RPC/RLS/عزل الهوية.** `tsc` + `build` **exit 0**. لا هجرة.

### تحسين — Sprint N+2b: الفلترة الفورية على القوائم المالية (واجهة فقط، بلا هجرة)
إكمال تعميم التصفية اللحظية على بقية الجداول:
- **العقود** (`FilterableCards` — بطاقات): تصفية برقم العقد/العقار/الوحدة/المستأجر/الحالة.
- **الفواتير** و**سندات القبض** (`FilterableTable` — جداول): تصفية بالرقم/المشتري أو المستلَم منه/التاريخ/النوع/الحالة/الطريقة.
- نفس المكوّنات المُتحقَّق منها في N+2 (الروابط تبقى تعمل عبر slots)، بلا تغيير على الاستعلامات/الصلاحيات/RLS. `tsc` + `build` **exit 0**. **الآن كل قوائم النظام تملك تصفية فورية.** لا هجرة.

### تحسين — Sprint N+2: إعادة تصميم لوحة المكتب + فلترة الجداول الفورية (واجهة فقط، بلا هجرة)
تبنّي نظام مكوّنات N+1 في أول سطح فعلي، وإضافة تصفية لحظية فوق القوائم.
- **لوحة المكتب** (`components/dashboard.tsx`): أُعيد بناؤها ببطاقات `Card`/`Badge` + لوحة Slate — بطاقات KPI (الإشغال بشريط + تحصيل الشهر + المتأخرات + المستحق)، **رسم مقارنة تحصيل الشهر الحالي مقابل الماضي** (شريطان SVG)، وقسم «نشاط المحفظة» **قابل للطي**. **منطق جلب البيانات لم يتغيّر** (نفس الاستعلامات + RLS).
- **فلترة فورية** (`components/filterable-list.tsx`، جديد): `FilterableTable` + `FilterableCards` — تصفية client-side لحظية فوق صفوف الصفحة المحمّلة (تُكمّل البحث الخادمي عبر كامل البيانات) مع عدّاد «عرض N من M». الخلايا/البطاقات تُرندَر خادمياً وتُمرَّر كـ slots فتبقى الروابط ونماذج Server Actions تعمل عبر حدّ RSC.
- **طُبِّقت على 4 قوائم:** العقارات + المستأجرون (جداول) · الوحدات + الملّاك (بطاقات). **لا تغيير على الاستعلامات/الصلاحيات/RLS.**
- **التحقق:** `tsc` + `build` **exit 0** · فحص حيّ محلي معزول (صفحة مؤقتة، حُذفت): الجدول «تجاري»→صف واحد، البطاقات «جدة»→بطاقة واحدة، عدّاد «عرض 1 من 3»، صفر أخطاء طرفية. **لا هجرة.**
- **التالي:** N+2b (الفلترة على القوائم المالية: العقود/الفواتير/السندات) · N+3 البوابتان · N+4 Drawers+Combobox في النماذج · N+5 لوحة المشغّل.

### بنية — Sprint N+1: نظام مكوّنات الواجهة `components/ui/` (أساس التصميم الموحّد، إضافي بالكامل)
أول لبنة في مبادرة توحيد تصميم كل الشاشات: مكتبة مكوّنات محلية موحّدة بالأخضر الزمردي + Slate + Tajawal، **بلا أي تبعية خارجية** (لا shadcn/Radix/Framer) و**بلا لمس أي صفحة قائمة** (التبنّي يبدأ في السبرنتات التالية).
- **8 مكوّنات في `components/ui/`:** `Button` (5 متغيّرات) · `Card` + رأس/عنوان/جسم · `Badge` (6 ألوان حالة) · `Skeleton` · `Tabs` (تبويبات متاحة + تنقّل لوحة مفاتيح) · `Drawer` (لوح ينزلق من اليمين RTL + غطاء + Esc + قفل تمرير + إدارة تركيز) · `Combobox` (قائمة بحث فوري متوافقة مع النماذج عبر hidden input) · `Toast` (مزوّد + `useToast`). + `lib/cx.ts` (دمج أصناف) + keyframes حركة في `tailwind.config` (fade-in/slide-in).
- **التحقق:** `tsc` + `build` **exit 0**. **فحص بصري حيّ محلي** (صفحة معاينة مؤقتة غير مرفوعة، حُذفت): Combobox يفلتر فوراً («تجاري»→نتيجة واحدة)، Tabs تبدّل، Drawer يفتح/يُغلق بـ Esc ويقفل التمرير ويحوي combobox متداخلاً، Toast يظهر — صفر أخطاء طرفية. **لا تغيير على أي صفحة قائمة · لا هجرة.**
- **التالي (سبرنتات مستقلة):** N+2 لوحة المكتب + فلترة فورية · N+3 البوابتان (تبويبات) · N+4 Drawers + Combobox في النماذج (عبر Server Actions) · N+5 لوحة المشغّل (تعديل بالسطر).

### ميزة — Sprint N: صفحة الهبوط التسويقية + الخصوصية والشروط + خط Tajawal (واجهة فقط، بلا هجرة)
استبدال شاشة فحص الاتصال في `/` بصفحة هبوط تسويقية عصرية، وإغلاق فجوة PDPL بصفحتَي محتوى قانونيتَين. **أسس تصميمية معتمدة:** الأخضر الزمردي القائم (لا Sky الأزرق) · Tailwind + مكوّنات محلية (لا shadcn) · خط **Tajawal** عبر `next/font` (self-hosted، بلا طلب خارجي).
- **`app/page.tsx`** أُعيد بناؤها (شاشة الفحص انتقلت إلى `/api/health` JSON): هيدر بتنقّل + قائمة جوال · قسم بطل بعنوان وزرّين (تجربة مجاناً → `/login` · طلب عرض → `mailto`) + **Mockup لوحة KPI مبنيّ بـ CSS** (لا صورة خارجية) · **6 بطاقات مميزات** (كلها حقيقية ومبنيّة) بأيقونات SVG مضمّنة · قسم الحلول (مكاتب/ملّاك/شركات) · **بطاقات أسعار بالأرقام الحقيقية** (الأساسية 99 · الاحترافية 299 · المؤسسية حسب الطلب) مع **تبديل شهري/سنوي (−20%)** وإبراز «الأكثر طلباً» · **أكورديون أسئلة** (ZATCA · التجربة · الأمان · PDPL) · تذييل بروابط عاملة.
- **مكوّنات جديدة:** `components/landing/landing-nav.tsx` + `pricing.tsx` + `faq.tsx` (عميلة للتفاعل).
- **`app/privacy/page.tsx` + `app/terms/page.tsx`:** نسختان أوّليتان عربيتان متوافقتان مع مبادئ PDPL (يُنصح بمراجعة قانونية) — تُغلقان فجوة إطلاق مهمة، وروابط التذييل تعمل فعلياً.
- **`app/layout.tsx` + `tailwind.config`:** إضافة Tajawal على كامل الموقع + سلسلة احتياطية.
- **صدق تسويقي:** لا إحصاءات وهمية · ZATCA = «المرحلة الأولى» · الأسعار حقيقية من القاعدة · الفوترة الشهرية (السنوي عرض تسويقي عبر المبيعات).
- **التحقق:** `tsc` + `build` **exit 0** · فحص حيّ محلي: لا تمرير أفقي على 1280px و375px · Tajawal مُطبَّق · التبديل السعري والأكورديون وقائمة الجوال تعمل · لا أخطاء طرفية (ترطيب). **لا هجرة/تغيير قاعدة.**

## 2026-07-28

### تحسين — Sprint M: إصلاح تنقّل الجوال (واجهة فقط، بلا هجرة)
معالجة العيب الوحيد في تجاوب الجوال المرصود بمقارنة Mogod: الشريط العلوي كان يحوي **12 رابطاً** بلا التفاف أو قائمة مطويّة فيتجاوز عرض الشاشة على الجوال ويزاحم زر «خروج».
- **مكوّن جديد `components/app-nav.tsx` (عميل):** على الحاسوب (md+) يبقى الشريط الأفقي كما هو؛ على الجوال (<md) يظهر **زر قائمة (همبرغر)** يفتح **درجاً منسدلاً** بالروابط مُجمَّعة في أقسام (**المحفظة / المالية / الإدارة**) على نمط Mogod، مع **إبراز الرابط النشط** (`usePathname`) وشارة الإشعارات، وزر «خروج» في تذييل الدرج. يُغلق الدرج تلقائياً عند اختيار رابط.
- **`app/app/layout.tsx`:** استُبدل شريط التنقّل المضمّن بـ`<AppNav>` (عند وجود منشأة نشطة)؛ في حالة عدم وجود منشأة يبقى زر «خروج» ظاهراً كما كان.
- **لا تغيير على المخطط/المنطق/الصلاحيات** — الروابط نفسها تُعرض للجميع كما قبل. مجموعات اختبار قاعدة البيانات غير متأثرة (لم يُمسّ SQL).
- التحقق: **`tsc` + `build` كلاهما exit 0** (جُمّعت كل صفحات `/app`). الموقع الحيّ يُنشَر تلقائياً على Vercel — تحقّق من هاتفك بعد دقيقة‑دقيقتين.
- **أثر البيانات:** لا شيء. **لا يتطلّب أي هجرة.**

## 2026-07-25

### ميزة — Sprint L: مواءمة Mogod (الطبقة A — إضافات آمنة)
مواءمة مع منصّة Mogod دون إعادة تصميم. قرارات المستخدم: لا فروع، لا صفحة عامة، نوع التعاقد **عرضي**، ومنشأة واحدة لكل مستخدم.
- **هجرة `0044`:** أعمدة اختيارية على `property` (**عرضية فقط**، لا تقود منطقاً): `holding_type` (مملوك/إدارة أملاك/استثمار، CHECK، مبذور من `owner.is_self`)، `property_code`، `property_type`، `occupancy_type` (عوائل/عزّاب، CHECK)، `deed_type`/`deed_date`، `water_meter`/`electricity_meter`، `planned_residential_units`/`planned_commercial_units`. **منشأة واحدة لكل مستخدم:** `create_organization` يرفض الإنشاء إن كان المستخدم يملك منشأة (`OWN_ORG_EXISTS`) — لا يمنع قبول الدعوات، متوافق رجعياً.
- **التطبيق:** نموذج «إضافة عقار» مُوسَّع بأقسام قابلة للطي + منتقي علاقة العقار (مملوك→المالك الذاتي؛ إدارة/استثمار→مالك خارجي مطلوب). **`LandlordPicker`**: بحث بالاسم/الهوية + **إنشاء مالك داخل الشاشة** عبر `/api/owners/quick`. **لوحة العقارات:** بطاقات KPI للوحدات (إجمالي/شاغرة/مؤجرة/غير مكتملة) + **جدول** (تصنيف/كود/علاقة/عدد وحدات/شاغرة) + **حذف soft** بتأكيد. **Paywall Modal** («ترقية مطلوبة» → اشترك الآن) عند بلوغ حدّ الخطة. صفحة الرئيسية تُخفي «إنشاء منشأة أخرى» لمن يملك منشأة + ترجمة `OWN_ORG_EXISTS`.
- **خارج النطاق (بقرارك):** الفروع، الصفحة العامة + QR + تحليلات الزوّار، المرفقات، PDF كامل، أي منطق يُقاد بنوع العقار/التعاقد.
- التحقق PG17: **`verify.mjs` ٣١/٣١ + `phase3.mjs` ١٠٣/١٠٣** (أُضيف ٥: حقول العقار، CHECK لـ holding/occupancy، `create_organization` يقبل الأولى ويرفض الثانية) · `0001`–`0044` نظيف · `schema_all` (٤٤) · `tsc`+`build` نظيفان.
- **أثر البيانات:** لا شيء — أعمدة اختيارية + بذر holding_type + حارس على الإنشاء الجديد فقط. **⏳ يتطلّب تطبيقك:** هجرة **`0044`**.

### تحسين — Sprint K: تحسينات تجربة المستخدم (٦ بنود من اختبار المستخدمين)
1. **إضافة عقار بلا مالك:** نموذج العقار يعرض تلميحاً «إذا لم تكن أنت المالك، أضِف مالكاً جديداً من هنا» + رابط لصفحة الملّاك.
2. **طرق الدفع القياسية:** قائمة تسجيل الدفعة صارت **نقداً / حوالة بنكية / منصة إيجار**. **هجرة `0043`** تضيف قيمة `ejar` إلى enum طرق الدفع (`ALTER TYPE ADD VALUE`، آمن، بلا فقدان بيانات).
3. **إضافة وحدة من القائمة الرئيسية:** صفحة **`/app/units`** جديدة (منتقي عقار + نموذج إضافة + قائمة الوحدات) + رابط «الوحدات» في الشريط. الوحدة تبقى مرتبطة بعقارها عند الإنشاء (`UnitForm` وسّعناها لتقبل منتقي عقار).
4. **تغيير مالك العقار:** موجود ويعمل؛ أضفنا **رسالة تحذير + تأكيد** (`ConfirmButton`) قبل الحفظ توضّح أثر التغيير على العقود والالتزامات.
5. **اسم المحل التجاري في العقد:** كان مضافاً في Sprint J (إنشاء)، والآن متاح أيضاً في **تعديل المسودة**.
6. **تعديل البيانات (السبب + الإصلاح):** **السبب** لم يكن قيداً في قاعدة البيانات — الـ RLS يسمح بالتعديل (`manage_data`)، وحارس تجميد العقد يُجمّد **العقد النشط فقط** لا المسودة — بل **لم تكن هناك واجهة تعديل أصلاً** (كانت شاشات إنشاء فقط). **الإصلاح:** أضفنا واجهات + إجراءات تعديل: `updateUnit` (تعديل الوحدات من صفحة الوحدات)، صفحة **`/app/tenants/[id]`** لتعديل المستأجر (بيانات المنشأة والنوع)، و**تعديل مسودة العقد** على صفحة العقد (`updateDraftContract` مقيّد بـ`status='draft'`). **بعد التفعيل تبقى بنود العقد مجمّدة كما هو مقصود.**
- التحقق PG17: **`verify.mjs` ٣١/٣١ + `phase3.mjs` ٩٨/٩٨** (أُضيف ٥: `ejar` مقبول، المسودة قابلة للتعديل، النشط يبقى مجمّداً، الوحدة/المستأجر قابلان للتعديل) · `0001`–`0043` نظيف · `schema_all` (٤٣) · `tsc`+`build` **exit 0**.
- **أثر البيانات:** لا شيء — `0043` إضافة قيمة enum فقط؛ كل الباقي واجهات/إجراءات تعديل تحترم الـ RLS وحارس التجميد. **⏳ يتطلّب تطبيقك:** هجرة **`0043`**.

### ميزة — Sprint J: نموذج المستأجر والمنشآت التجارية (إضافي، متوافق تماماً)
إثراء نموذج المستأجر دون إعادة تصميم: أنواع مستأجر (فرد/مؤسسة فردية/شركة)، بيانات المنشأة، واسم المحل/الممثل على مستوى العقد. **إضافي بالكامل — لا فقدان بيانات، العقود الحالية سليمة.**
- **هجرة `0042`:** `tenant.tenant_type` (text+CHECK: individual/sole_establishment/company، افتراضي individual، **مبذور من `tenant_kind`**) — **لم نُعدّل enum `legal_kind`** تفادياً لعطل `ALTER TYPE ADD VALUE` داخل transaction، مع إبقاء `tenant_kind` للتوافق. أعمدة منشأة على `party` (`vat_number`/`unified_number`/`cr_expiry` بجانب `cr_number`). أعمدة على `contract`: `trade_name` (اسم المحل — لكل عقد، فتملك نفس المنشأة عدة عقود بأسماء محلات مختلفة) + `representative_name`/`_capacity`/`_id`/`_phone`. الأعمدة الجديدة خارج مجموعة تجميد العقد (`tg_contract_immutable`) فتبقى قابلة للتحرير.
- **الواجهة:** شاشة إضافة مستأجر **مبسّطة** (اسم/نوع/جوال) + قسم **«تفاصيل إضافية»** قابل للطي (يظهر بيانات المنشأة عند اختيار مؤسسة/شركة) · `createTenant` يكتب النوع وبيانات المنشأة (`legal_kind` يُشتقّ للتوافق) · نموذج العقد اكتسب قسم «اسم المحل التجاري وممثل المنشأة» (اختياري) · تفاصيل العقد تعرضهما.
- **نُفِّذ بطريقة مختلفة عن الحرفي:** أنواع المستأجر عبر `text+CHECK` لا توسيع enum (حماية الـ one-shot apply)؛ «اسم المنشأة الرسمي» = `party.display_name` (لا حقل مكرّر)؛ بيانات المنشأة على `party` واسم المحل/الممثل على `contract` (أنسب للمعمارية القائمة).
- التحقق PG17: **`verify.mjs` ٣١/٣١ + `phase3.mjs` ٩٣/٩٣** (أُضيف ٦: بيانات المنشأة على party، `tenant_type` يثبت/افتراضي/يقبل sole_establishment/يرفض غير الصالح، اسم المحل+الممثل على العقد) · `0001`–`0042` نظيف · `schema_all` (٤٢) · `tsc`+`build` **exit 0**.
- **أثر البيانات:** لا شيء — أعمدة اختيارية/مبذورة؛ الصفوف والعقود الحالية كما هي. **⏳ يتطلّب تطبيقك:** هجرة **`0042`** فقط.

### ميزة — Sprint I: مصفوفة الأدوار والصلاحيات الكاملة — نطاق مُجمَّد
تجاوز قاعدة «المطّلع فقط» (0033) بمصفوفة قدرات لكل دور، مفروضة على RLS ومنعكسة في الواجهات. المصفوفة معتمدة كما اقتُرحت.
- **هجرة `0041`:** ٥ قدرات (`view`/`manage_data`/`manage_finance`/`manage_team`/`manage_billing`) + جدول `role_capability` مبذور بالمصفوفة (owner/admin=الكل · manager=بيانات+مالية · accountant=مالية · staff=بيانات · viewer=قراءة) + `has_capability(org,cap)` + `current_capabilities(org)`. **استُبدلت سياسات `0033` الكتابية العامة** بسياسات RLS **تقييدية لكل قدرة**: جداول البيانات (+`charge`) → `manage_data`؛ الجداول المالية (`payment`/`payment_allocation`/`invoice`/`invoice_line`/`owner_remittance`) → `manage_finance`؛ الفريق/الفوترة تبقى owner/admin.
- **الواجهة:** `lib/capabilities.ts` + إخفاء الأزرار حسب القدرة — صفحة العقارات (إضافة عقار=بيانات)، تفاصيل العقد (تسجيل دفعة/إصدار فاتورة=مالية؛ تفعيل/تجديد/تعديل/إنهاء=بيانات)، صفحة الاشتراك (الدفع/التجديد=فوترة)، وجدول **مرجعي للأدوار** في صفحة الفريق. **الـ RLS هو الحاكم؛ الواجهة تحسين تجربة.**
- التحقق PG17: **`verify.mjs` ٣١/٣١ + `phase3.mjs` ٨٧/٨٧** (أُضيف ١٢ لكل دور: staff يكتب البيانات ويُمنع من المالية، accountant يعكسه، manager كلاهما، viewer لا شيء، `has_capability`/`current_capabilities`، الفريق/الفوترة owner/admin فقط) · `0001`–`0041` نظيف · `schema_all` (٤١) · `tsc`+`build` **exit 0**.
- الوثائق: **ADR-0005** + `04-roles.md` (المصفوفة) + SCHEMA + الذاكرة.
- **⏳ يتطلّب تطبيقك:** هجرة **`0041`** فقط.

### ميزة — Sprint H: التجديد المتكرّر الآلي (توكنة + خصم دوري + Dunning) — نطاق مُجمَّد
استدامة الفوترة بلا تدخّل شهري. قرارات مُعتمدة: حفظ البطاقة عند أول دفعة (on-session) · Dunning يوم ٠/+٢/+٥ ثم `past_due` (قفل، البيانات تبقى) · تنبيهات داخل التطبيق + بريد · إقرار دعم Moyasar للـoff-session.
- **هجرة `0040`:** `org_payment_method` (**رمز token مرجعي — لا بيانات بطاقة**، RLS قراءة للمدير، بطاقة نشطة واحدة/منشأة) + حقول `org_subscription` (`auto_renew`/`payment_method_id`/`dunning_attempts`/`next_charge_at`) + `subscription_payment` (`initiated_by`/`attempt`). دوال: `set_auto_renew`/`remove_payment_method` (مدير) · `save_payment_method`/`claim_due_renewals` (حجز ذرّي `FOR UPDATE SKIP LOCKED` + فتح نيّة `auto`)/`record_dunning_failure` (جدول ٠/+٢/+٥ ثم `past_due` + تنبيه)/`enqueue_notification_email` (**service_role حصراً**). تحديث `apply_subscription_payment` (تصفير Dunning عند أي نجاح) و`subscription_summary` (auto_renew + بطاقة).
- **التطبيق:** `moyasar.chargeToken` (Payments API، `source=token`) + `extractCardToken` · خانة «فعّل التجديد التلقائي» (مفعّلة افتراضياً) في الدفع؛ الـwebhook يحفظ الرمز عند النجاح إن طُلب · صفحة الاشتراك: قسم التجديد التلقائي (بطاقة •••• + تشغيل/إيقاف + إزالة) · مجدول **Vercel Cron يومي** `/api/cron/renew-subscriptions` (claim→charge→apply/dunning) + إدخال cron في `vercel.json`.
- **مؤجَّل بالتجميد:** proration · بطاقات متعدّدة · Apple Pay المتكرّر · الاسترداد الآلي · Dunning عبر SMS.
- التحقق PG17: **`verify.mjs` ٣١/٣١ + `phase3.mjs` ٧٥/٧٥** (أُضيف ١٢: التوكنة تُفعّل التجديد، `set_auto_renew` يتطلّب بطاقة، `claim_due_renewals` يحجز المستحقّ فقط ولا يُكرّر، `apply` يُجدّد ويصفّر Dunning، دورة Dunning الكاملة → `past_due`، عزل RLS للبطاقات) · `0001`–`0040` نظيف · `schema_all` (٤٠) · `tsc`+`build` **exit 0**.
- **⏳ يتطلّب تطبيقك:** هجرة **`0040`** فقط (لا أسرار جديدة). *التحقّق الحيّ للخصم off-session يعتمد دعم Moyasar للدفع المتكرّر المُوكَّن (مُقَرّ).*

### ميزة — Sprint G: دفع الاشتراك الآلي (Moyasar) + واجهة مشغّل المنصّة — نطاق مُجمَّد
إغلاق دائرة الفوترة: يدفع المكتب عبر صفحة Moyasar المستضافة فيُفعَّل اشتراكه فوراً، + واجهة مشغّل لإدارة الاشتراكات يدوياً. قرارات مُعتمدة: Moyasar · دفعة لكل فترة · جدول `platform_operator` · تأجيل فاتورة ZATCA لرسوم الـSaaS.
- **هجرة `0039`:** جدول `subscription_payment` (RLS قراءة للمدير، `gateway_payment_id` فريد، تتبّع status/period/raw). `create_subscription_payment(org,plan)` (مقيّد بالمدير، يرفض الخطط غير العامة/بلا سعر). `apply_subscription_payment`/`mark_subscription_payment_failed` (**service_role حصراً، idempotent**): عند الدفع يُفعّل `org_subscription` (`active` + الخطة + `current_period_end = max(now, current) + شهر`)، والتطبيق مرّتين = تفعيل واحد. جدول `platform_operator` + `is_platform_operator()` + `operator_list_orgs`/`operator_set_subscription`/`operator_list_payments` (مقيّدة بالمشغّل، فوق كل المنشآت).
- **G-1 الدفع:** طبقة `lib/payments/moyasar.ts` (Invoices API، Basic auth، **صفحة مستضافة — لا نلمس البطاقة**) + `startSubscriptionCheckout` (نيّة → فاتورة Moyasar → تحويل) + أزرار «اشترك/تجديد» في `/app/subscription` + webhook `/api/payments/webhook` (تحقّق `secret_token`، service_role، `apply`/`mark`، idempotent، مطابقة عبر `metadata.payment_intent`).
- **G-2 المشغّل:** صفحتا `/operator` (كل المنشآت + اشتراك + استخدام) و`/operator/[orgId]` (تعديل الاشتراك عبر `operator_set_subscription` + سجل المدفوعات)، محميّتان بـ`is_platform_operator` (غير المشغّل → 404) + `/operator` أُضيف لحماية Middleware.
- **مؤجَّل بالتجميد:** الدفع المتكرّر الآلي (توكنة/تجديد ذاتي/dunning) · proration · فاتورة ZATCA لرسوم الاشتراك · الاسترداد الآلي · قنوات SMS.
- التحقق PG17: **`verify.mjs` ٣١/٣١ + `phase3.mjs` ٦٣/٦٣** (أُضيف ١٦: نيّة الدفع مقيّدة بالمدير + رفض غير القابل للشراء، `apply` يُفعّل ويمدّد و**idempotent**، mark failed، عزل RLS للمدفوعات، بوّابة `is_platform_operator` وعمل/رفض `operator_*` والتطبيق الفعلي للتعديل) · `0001`–`0039` نظيف · `schema_all` (٣٩) · `tsc` نظيف · `build` مُجمَّع بنجاح (كل مسارات الدفع/المشغّل).
- **⏳ يتطلّب تطبيقك:** هجرة **`0039`** + متغيّرات Moyasar في Vercel + ضبط webhook + بذر هويتك في `platform_operator`.

### ميزة — Sprint F: إرسال البريد (إشعارات + اكتمال المصادقة) — نطاق مُجمَّد
تفعيل إرسال البريد الحقيقي فوق **Resend** (مزوّد واحد): إشعارات الاستحقاقات للمكاتب (ADR-0002) + تأكيد البريد واستعادة كلمة المرور.
- **هجرة `0038` (صندوق إرسال الإشعارات):** `notification_channel`/`delivery_status` enums + جدول `notification_delivery` (append-only، `org_id` لـ RLS قراءة الأعضاء، فهرس فريد `(notification_id,channel,target)` = **idempotency**، حقول تتبّع: `provider_message_id`/`provider_response`/`attempts`/`next_attempt_at`/`last_error`/`last_attempt_at`). دوال: `enqueue_email_deliveries(org)` (جلسة المستخدم، `has_org_access`، صفّ لكل إشعار غير مقروء × عضو نشط له بريد) + `claim_email_deliveries`/`mark_email_delivery_sent`/`_failed` (**service_role حصراً**؛ `claim` يحجز ذرّياً بـ`FOR UPDATE SKIP LOCKED` فلا إرسال مزدوج). **إعادة المحاولة:** ٣ محاولات، ١→٥→٣٠ دقيقة، ثم `failed`.
- **F-2 خط الإشعارات:** `lib/email/provider.ts` (طبقة Resend واحدة عبر `fetch`) + `lib/supabase/admin.ts` (عميل service_role، للـ drainer فقط) + مسار **Vercel Cron** `/api/cron/drain-notifications` (حارس `CRON_SECRET`، claim→send→mark) + `vercel.json` (كل ٥ دقائق) + قالب عربي بسيط (Plain+HTML). الاستدعاء `enqueue` مربوط بعد `generate_notifications` في الرئيسية وصفحة الإشعارات (idempotent، يتدهور قبل `0038`).
- **F-1 اكتمال المصادقة:** مسار عام **`/auth/callback`** (يتعامل مع `code` وأيضاً `token_hash`+`type` لكل تدفقات Supabase: تأكيد/استعادة/رابط سحري/دعوة/تغيير بريد) + **`/forgot-password`** (`resetPasswordForEmail`) + **`/auth/reset`** (`updateUser`) + رابط «نسيت كلمة المرور» وزر «إعادة إرسال التأكيد» في `/login` + قراءة `?error/?notice`. (Middleware بلا تغيير — هذه المسارات عامة أصلاً.)
- **مؤجَّل بالتجميد:** قناة SMS، تفضيلات القنوات لكل مستخدم، واتساب، التجميع (digest)، قوالب HTML غنية.
- التحقق PG17: **`verify.mjs` ٣١/٣١ + `phase3.mjs` ٤٧/٤٧** (أُضيف ١١: enqueue لكل عضو له بريد فقط، idempotency، FORBIDDEN لغير العضو، عزل RLS، دورة claim→mark_sent، عدم إعادة الحجز، تحوّل `failed` بعد ٣ محاولات) · تحميل `0001`–`0038` نظيفاً · `schema_all` (٣٨) · `tsc`+`build` نظيفان.
- **⏳ يتطلّب تطبيقك:** هجرة **`0038`** + متغيرات بيئة Vercel (`SUPABASE_SERVICE_ROLE_KEY`، `EMAIL_FROM`، `CRON_SECRET`). ويمكنك الآن **تفعيل Confirm Email** (مسار الاستدعاء جاهز). *ملاحظة تشغيلية: دورية Vercel Cron تعتمد خطتك — Pro تُشغّلها كل ٥ دقائق، Hobby ~يومياً؛ التصميم لا يتأثّر (backoff يعتمد `next_attempt_at`) لكن زمن الوصول يتأثّر.*

### ميزة — Sprint E: الدخول بالبريد الإلكتروني (Email + Password) — نطاق مُجمَّد
إتاحة إنشاء حساب ودخول بالبريد وكلمة المرور بلا تبعية إرسال (SMTP)، مصمَّماً ليُركَّب عليه تأكيد البريد/الاستعادة/إشعارات البريد لاحقاً دون كسر (الخيار «أ» المُعتمَد).
- **هجرة `0037`:** `identity.phone_e164` صار **اختيارياً** (فحص الصيغة يبقى ساري المفعول عند وجود جوال — الـ CHECK يُمرّر NULL أصلاً) + قيد `identity_contact_present` (**جوال أو بريد على الأقل**) + تحديث المُشغّل `handle_new_auth_user()` ليُنشئ هوية **لمستخدم البريد أيضاً** (هاتف NULL) ويقرأ `full_name` من `raw_user_meta_data`. **Future-proof:** نقطة التزويد الوحيدة لم تتغيّر، فتفعيل تأكيد البريد لاحقاً تبديل إعداد لا تعديل كود.
- **التطبيق:** `/login` أصبح **بريد + كلمة مرور** (تبويبا دخول/حساب جديد + اسم اختياري) عبر `emailAuth` (`signUp`/`signInWithPassword`) مع `safeReturnTo`. **حاسم للمرونة:** إجراء التسجيل يتفرّع على وجود `data.session` — موجودة (التأكيد مُطفأ) → دخول مباشر؛ فارغة (عند تفعيله لاحقاً) → «تحقّق من بريدك» بدل الانهيار. `lib/auth-errors.ts` يترجم أخطاء GoTrue للعربية. **مسار OTP الجوال بقي في الكود خامِلاً** (غير مُبرَز) لسبرنت الجوال.
- **قناة بريد الإشعارات (تصميماً):** **[ADR-0002](docs/adr/0002-notification-delivery-channels.md)** — نموذج صندوق إرسال متعدّد القنوات (`in_app/email/sms`) يُضاف لاحقاً كطبقة *إضافية* فوق جدول `notification` الحالي دون تعديله (المحتوى محايد للقناة أصلاً). **لا كود إرسال بُني.**
- **مؤجَّل بالتجميد:** تأكيد البريد · استعادة كلمة المرور · SMTP · أي إرسال فعلي · OTP الجوال كمسار فعّال.
- التحقق PG17: **`verify.mjs` ٣١/٣١ + `phase3.mjs` ٣٦/٣٦** (أُضيف: هوية ببريد فقط تُقبل، بلا تواصل تُرفض، صيغة الجوال تُفحص عند وجوده، **`create_organization` ينجح لهوية بريدية بلا جوال + يوفّر اشتراك التجربة**) · تحميل `0001`–`0037` نظيفاً · `schema_all` (٣٧) · `tsc`+`build` نظيفان.
- **⏳ يتطلّب تطبيقك:** هجرة **`0037`** + **ضبط لوحة Supabase** (تفعيل Email، إطفاء Confirm Email). بدونهما لا يعمل المسار البريدي.

### ميزة — Sprint D: نموذج الاشتراك (SaaS Subscription) — نطاق مُجمَّد، فوترة يدوية
تفعيل الاشتراك الذي يحكم النمو: خطط بحدود، تجربة ٣٠ يوماً ثم قفل، ومنع صارم عند تجاوز الحدود (مع بقاء البيانات القائمة قابلة للقراءة/التعديل). قرارات المنتج مُعتمدة (ق-أ..هـ): حدود افتراضية، فوترة يدوية، تجربة ٣٠ يوماً بحدود **الأساسية** ثم قفل تام، منع صارم عند التجاوز، وإدارة عبر SQL مع لَبِنتَي تسويق يدويتين (تمديد التجربة + منح `comped`).
- **هجرة `0036`:**
  - `plan` (كتالوج) + بذور ٣ خطط: **الأساسية** (٢٥ عقاراً/١٥٠ وحدة/٣ أعضاء — ٩٩ ر.س)، **الاحترافية** (١٠٠/٥٠٠/١٠ — ٢٩٩ ر.س)، **المؤسسية** (غير محدود — حسب الطلب). الحدود **بيانات** قابلة للتعديل بلا هجرة.
  - نوع `subscription_status` (`trialing/active/past_due/canceled/comped`) + جدول `org_subscription` (سجل لكل منشأة، RLS: قراءة للأعضاء فقط، بلا كتابة عبر الواجهة — الإدارة عبر SQL/DEFINER).
  - دوال `subscription_active` / `plan_limit` / `usage_count` (DEFINER) + `subscription_summary(org)` (jsonb لصفحة الاستخدام، محكوم بالعضوية).
  - **الفرض عند الإنشاء** عبر `BEFORE INSERT trigger` على `property/unit/membership/contract`: قفل الحيوية (تجربة/اشتراك منتهٍ وغير ممنوح → `SUBSCRIPTION_EXPIRED`) + سقف الموارد المعدودة (`PLAN_LIMIT_EXCEEDED`)، مع استثناء إعادة تفعيل العضو (ON CONFLICT).
  - `create_organization` صار يُنشئ **تجربة ٣٠ يوماً على الأساسية** قبل عضوية المالك؛ و**Backfill**: كل منشأة قائمة تُمنح `comped` (grandfathering) كي لا تُقفل بيانات حيّة.
- **التطبيق:** صفحة **«الاشتراك والاستخدام»** (`/app/subscription`: الخطة، الحالة، تواريخ التجربة/التجديد، وأشرطة الاستهلاك مقابل الحدود) + رابط في الشريط + **شريط تنبيه** (قفل أحمر عند الانتهاء / عدّاد تجربة أصفر في الأسبوع الأخير) — كلاهما يتدهور برشاقة قبل `0036`. **ترجمة الأخطاء** (`PLAN_LIMIT_EXCEEDED`/`SUBSCRIPTION_EXPIRED`) إلى عربية واضحة في مسارات إنشاء العقار/الوحدة/العقد/التجديد وقبول الدعوة.
- **مؤجَّل بالتجميد:** بوابة دفع آلية/اشتراك ذاتي، واجهة مشغّل المنصّة الكاملة، أتمتة `past_due`، وفوترة رسوم الـ SaaS نفسها.
- التحقق PG17: **`verify.mjs` ٣١/٣١ + `phase3.mjs` ٣٢/٣٢** (أُضيف: قفل التجربة المنتهية، تمديد التجربة يُعيد الإتاحة، سقف الخطة يمنع، `comped` يتجاوز الانتهاء، `create_organization` يُنشئ تجربة، عزل RLS للاشتراك) · تحميل `0001`–`0036` نظيفاً · `tsc`+`build` نظيفان · `schema_all.sql` مُعاد توليده (٣٦ هجرة).
- **⏳ يتطلّب تطبيقك:** هجرة **`0036`** على قاعدة Supabase. (التطبيق يعمل قبلها.)

## 2026-07-24

### ميزة — Sprint C: Beta Readiness (نطاق مُجمَّد: بحث/ترقيم + إشعارات داخل التطبيق)
- **C-1 بحث + ترقيم القوائم:** صفحات العقارات/المستأجرين/الملّاك/العقود/الفواتير/السندات صارت **مبحوثة ومُرقَّمة صفحات** (٢٠/صفحة) عبر Server Actions (بحث `ilike` + `range` + عدّ). مكوّنات مشتركة `ListToolbar` + `Pagination` + `lib/list-params.ts` (مع تهريب محارف `ilike`). حالات «لا نتائج» مراعية للبحث.
- **هجرة `0035`:** فهارس **pg_trgm GIN trigram** على أعمدة البحث (اسم العقار/الطرف، رقم العقد/الفاتورة/السند) — لضمان سرعة البحث مع نمو السجلات (بناءً على ملاحظة الأداء).
- **C-2 إشعارات داخل التطبيق:** **هجرة `0034`** — جدول `notification` (org-scoped + RLS بنطاق العقار) + `generate_notifications` (استحقاقات قريبة خلال ٧ أيام / متأخرة / عقود تنتهي خلال ٣٠ يوماً بلا تجديد — DEFINER محكوم بالهوية، idempotent بفهرس إلغاء تكرار) + `mark_notifications_read`. **صندوق إشعارات** `/app/notifications` + **شارة عدد غير المقروء** في الشريط (تتدهور لصفر قبل الهجرة) + توليد تلقائي عند فتح الرئيسية. **التسليم الخارجي (SMS/واتساب) مؤجَّل** كما اتُّفق.
- **مؤجَّل بالتجميد:** نموذج الاشتراك (C-3، قرار منتجي)، وحماية نشر Vercel (C-4، إجراء المستخدم).
- التحقق PG17: **`verify.mjs` ٣١/٣١ + `phase3.mjs` ٢٥/٢٥** (أُضيف عزل الإشعارات: عضو يرى إشعارات منشأته، السياق المُزوَّر/الأجنبي لا يرى شيئاً، `generate` FORBIDDEN لغير العضو) · تحميل الهجرات `0001`–`0035` نظيفاً · `tsc`+`build` نظيفان.
- **⏳ يتطلّب تطبيقك:** هجرتا **`0034`** و**`0035`** (التطبيق يعمل قبلهما — البحث/الشارة يتدهوران برشاقة).

### بنية/بيانات — Sprint B: Technical Debt Remediation (نطاق مُجمَّد)
معالجة الدَّين الهندسي الذي حوّله الميثاق لقواعد. ٥ بنود:
- **إزالة كود OTP الميت** — هجرة **`0032`**: إسقاط نظام OTP/الجلسات المخصّص المهجور (`otp_challenge`/`auth_attempt`/`sms_outbox`/`session`/`auth_method` + ٥ دوال + `auth_method_type`)؛ `identity` يبقى. المصادقة عبر Supabase Auth منذ 0017 (هـ-٣٥/مر-١٩).
- **فرض «المطّلع» للقراءة فقط** — هجرة **`0033`**: دالة `is_org_writer()` + سياسات RLS **تقييدية** (INSERT/UPDATE/DELETE) على جداول المحفظة والمالية تمنع `role='viewer'` من الكتابة (SELECT سليم). تُغلق الثغرة الأمنية (هـ-١٦/مر-١٤). *(المصفوفة الكاملة للأدوار مؤجَّلة كقرار منتجي.)*
- **CI** — `.github/workflows/ci.yml`: typecheck + build + توليد `schema_all` والتأكد من عدم انحرافه + سويت اختبارات PostgreSQL 17، على كل دفع/PR (هـ-٣٩).
- **اختبارات مُلتزَمة للمرحلة الثالثة** — `supabase/tests/local/phase3.mjs`: عزل «المطّلع»، عزل البوابات بالهوية (مالك/مستأجر أجنبي → FORBIDDEN)، صحّة مالية، تجديد. موصولة بـ`npm run verify` (هـ-٣٦/٣٧).
- **أتمتة `schema_all.sql`** — `supabase/build-schema.mjs` + `npm run build:schema` يعيد توليده من الهجرات (هـ-٤/مر-١٨).
- التحقق على PostgreSQL 17: **`verify.mjs` ٣١/٣١** (حُذفت اختبارات OTP الخمسة) + **`phase3.mjs` ٢١/٢١**؛ تحميل الهجرات `0001`–`0033` نظيفاً؛ `tsc`+`build` نظيفان.
- **⏳ يتطلّب تطبيقك:** هجرتا **`0032`** و**`0033`** على قاعدة Supabase (SQL Editor). التطبيق يعمل قبلهما (لا يعتمد عليهما)؛ تطبيقهما يُزيل الكود الميت ويجعل «المطّلع» للقراءة فقط فعلياً.

### بنية/توثيق — Sprint A: Platform Stabilization (نطاق مُجمَّد، بلا هجرات/منطق)
أول سبرنت تنفيذي بعد اعتماد الميثاق — محاذاة توثيق ونظافة مستودع واتساق UI فقط (٨ بنود مُجمَّدة):
- **README** أُعيدت كتابته ليعكس الواقع (٣١ هجرة، تطبيق حيّ، وثائق التأسيس) بدل «١٦ هجرة / لا واجهة» (مر-١/٢).
- **SCHEMA.md** صُحّحت حالته ووُثّقت الهجرات `0017`–`0031` في خريطة الملفات، مع الإشارة إلى أن `0017` يُلغي عملياً OTP المخصّص المهجور (مر-٣/٨).
- **roadmap** أُعيدت كتابته: المُنفَّذ في «متاح الآن»، والقادم وفق الميثاق (مر-٦/٩).
- **`schema_all.sql`** صُحّحت ترويسته النصّية (يُصان بالإلحاق؛ الأتمتة لـ Sprint B) (مر-١٨ جزء).
- **`.gitignore`** يغطّي ملفات Ruflo (`CLAUDE.md`, `.claude-flow/`, `.swarm/`, `.mcp.json`, `ruvector.db`) دون تعديل `CLAUDE.md` (مر-٤/٢٠).
- حُذف ملف القمامة المُلتزَم `supabase/tests/local/({wch` (مر-٢١).
- ترويسة بوابة المستأجر «عقار · بوابة المالك» → **«عقار · البوابة»** محايدة (مر-٢٢).
- نصّ الصفحة الرئيسية «المرحلة الثانية…» → **«منصّة إدارة الأملاك.»** (مر-٢٣ جزء).
- **مُؤجَّل بقاعدة التجميد (لم يُنفَّذ):** `deployment-checklist` (مر-٧)، مُولِّد `schema_all` الآلي، إعادة تصميم الرئيسية، وكل بنود Sprint B (إزالة OTP، فرض الأدوار، CI، اختبارات).
- التحقق (DoD): `tsc` + `build` نظيفان · صفر تغيير سلوك (نصوص UI فقط).

### قرار — اعتماد ميثاق المشروع v1.0 ووثائق التأسيس (بلا كود)
- **مرحلة مواءمة** من ٧ مراحل (رؤية · مبادئ منتج · مبادئ هندسية · تعريف مجال · تعريف مستخدمين · مراجعة توثيق · ميثاق) — حُفظت في [`docs/foundation/`](docs/foundation/) بالعربية، و**تصبح المرجع الرسمي الأعلى للمشروع**.
- **قرارات تنفيذية:** ق١ الشريحة الأساسية **مُغلق** (مكاتب إدارة الأملاك في السعودية أساسياً · الملّاك المباشرون ثانوياً بنفس النموذج، تجربة مبسّطة). نموذج العمل = **SaaS Subscription**. التكامل الحكومي (Ejar/ZATCA) جزء من الرؤية عند توفّر المتطلبات. مزوّد SMS نُقل لقرار تقني في [ADR-0001](docs/adr/0001-sms-provider.md).
- **الميثاق يثبّت:** تعريف الإنجاز (Definition of Done)، معايير التطوير/التوثيق، تسلسل «مصدر الحقيقة»، واستراتيجية الإصدار (MVP/Beta/v1.0/v2.0). المراجعة النقدية رصدت ٢٣ بند انجراف/دَين (مر-١..٢٣) تحوّل جزءٌ منها إلى قواعد هندسية مُلزِمة (هـ-٤/٢٠/٢٢/٣٤/٣٥/٣٦/٣٩).
- لا تعديل كود في هذه المرحلة؛ التنفيذ يبدأ ضمن خارطة الطريق وفق الميثاق.

### ميزة — تحسينات الدخول: العودة بعد تسجيل الدخول (بلا هجرة)
- **المشكلة:** فتح رابط عميق أو **رابط دعوة/بوابة** بدون تسجيل دخول كان يحوّل إلى `/login` ثم يُنزل المستخدم في `/app` — فيضيع المقصد (خاصة روابط الدعوة `‎/app/join?token=` و`‎/portal/join?token=`).
- **الحل:** حماية مسارات `‎/app` و`‎/portal` في **middleware**؛ الزائر غير المسجَّل يُحوَّل إلى `‎/login?returnTo=…` حاملاً وجهته، وبعد إدخال الرمز يعود **إلى نفس الصفحة** (فيقبل الدعوة/يربط البوابة مباشرة). ومن كان مسجّلاً وفتح `‎/login` يُمرَّر فوراً لوجهته.
- **الأمان:** `returnTo` يُنقّى عبر `lib/return-to.ts` — يُقبل **فقط** مسار داخلي يبدأ بشرطة واحدة؛ تُرفض الروابط الخارجية و«//host» وخدعة الشرطة العكسية و`javascript:` والمحارف المتحكَّمة (منع Open-Redirect). التحقق يجري في الوسيط وعند الدخول معاً.
- **بنية:** `‎/login` صار مكوّن خادم يقرأ `returnTo` ويمرّره لنموذج العميل (`login-form.tsx`) كحقل مخفي في الخطوتين؛ `verifyOtp` يعيد التوجيه للوجهة المُتحقَّقة (أو `‎/app`).
- التحقق: اختبار وحدة **15/15** لمنقّي `returnTo` (يقبل الداخلي، يرفض كل نواقل التحويل الخارجي) + typecheck/build نظيفان.

---

## 2026-07-23

### ميزة — تجديد العقود عبر عقد لاحق (تمديد)
- **الفكرة:** العقد بعد تفعيله «مجمّد» قانونياً (لا يمكن تعديل تاريخ نهايته). لذلك يُنمذَج التمديد كـ**عقد لاحق (تجديد)** لا كتعديل: عقد جديد بنفس الوحدة والمستأجر والعقار، مرتبط بالعقد السابق عبر `renewed_from_contract_id`. العقد السابق لا يُمسّ إطلاقاً.
- **الإنشاء:** من صفحة عقد **نشط أو منتهٍ** يظهر قسم **«تجديد العقد»** — يقترح بدايةً في اليوم التالي للنهاية ومدة سنة والإيجار الحالي (كلها قابلة للتعديل)، ويُنشئ **مسودة** تجديد. رقم العقد يُشتق تلقائياً (`رقم العقد-Rسنة`) أو يُدخل يدوياً.
- **التفعيل:** «تفعيل التجديد» يُنهي العقد السابق ذرّياً (`نشط ← منتهٍ`، مع إلغاء استحقاقاته المستقبلية **غير المدفوعة فقط** من تاريخ بدء الجديد لمنع الازدواج) ثم يُعيد استخدام `activate_contract` لتوليد جدول الجديد وإبقاء الوحدة «مؤجرة». ترتيب الخطوات يضمن قيد «عقد نشط واحد لكل وحدة».
- **الحماية:** يُمنع تجديد عقد غير نشط/منتهٍ، ويُمنع تجديد ثانٍ من نفس المصدر، وترفض دالة التفعيل ما ليس تجديداً. صفحة العقد تعرض سلسلة النسب (مجدَّد من… / جُدِّد بعقد لاحق…).
- **بيانات** — هجرة `0031`: عمود `contract.renewed_from_contract_id` + دالتا `renew_contract` و`activate_renewal` (كلاهما SECURITY INVOKER، يحترمان RLS ونطاق العقار).
- التحقق: اختبار **26/26** على PostgreSQL 17 (الحماية، نسخ الحقول والترقيم، إحالة السابق لمنتهٍ، تفعيل الجديد بجدوله، عقد نشط واحد للوحدة، التجديد المبكر: إبقاء استحقاقات ما قبل البداية والمدفوعة وإلغاء غير المدفوعة اللاحقة) + تحميل كامل المخطط `0001`–`0031` نظيفاً + typecheck/build نظيفان.

---

## 2026-07-22

### ميزة — مستندات قابلة للطباعة في البوابة (خدمة ذاتية)
- **المستأجر:** من قائمة دفعاته يفتح **سند قبض قابل للطباعة** لكل دفعة (ترويسة المنشأة، رقم/تاريخ، المبلغ رقماً و**كتابةً**، الطريقة، «وذلك عن» بالتخصيصات).
- **المالك:** زر **كشف حساب قابل للطباعة** لفترة (لكل عقار: المُحصَّل/الأتعاب/الصافي + التوريدات + المتبقّي)، و**سند صرف قابل للطباعة** لكل توريد.
- **بيانات** — هجرة `0030`: دوال **SECURITY DEFINER محكومة بالهوية**: `tenant_portal_receipt(+_lines)` (تفاصيل السند وتخصيصاته للمستأجر صاحبه فقط) و`owner_portal_org` (ترويسة المنشأة للمالك). كشف/سند المالك يعيدان استخدام دوال 0028. RLS المكتب لم يُمسّ.
- التحقق: اختبار مخصّص **10/10** (بيانات السند صحيحة والتخصيصات، ترويسة المنشأة، عزل: غير صاحب السند/المالك الأجنبي FORBIDDEN) + السويت الرئيسي 36/36 + typecheck/build نظيفان.
- ⚠️ **يتطلب تطبيق هجرة `0030`**.

### ميزة — بوابة المستأجر (دخول ذاتي للمستأجر)
- **للمكتب:** في جدول **المستأجرون** زر **«رابط البوابة»** لكل مستأجر يُنشئ رابط دعوة (مرة واحدة، ٣٠ يوماً).
- **للمستأجر:** `/portal/tenant/[id]` — يرى **عقوده** وجدول **الاستحقاقات** لكل عقد (مبلغ/مسدّد/متبقّي/الحالة) و**إجمالي المتبقّي عليه** و**دفعاته** (بأرقام السندات) — للقراءة فقط. يدعم أكثر من مكتب.
- **`/portal`** موحّدة الآن: تعرض ملفاتك **كمالك وكمستأجر** معاً؛ إن كان لديك ملف واحد فقط تُفتح مباشرة.
- **بيانات** — هجرة `0029`: نظير بوابة المالك — `create_tenant_invitation`، ودوال قراءة **SECURITY DEFINER محكومة بـ`party.identity_id = auth.uid()`** (`tenant_is_mine`, `my_tenant_links`, `tenant_portal_contracts/charges/payments`). و**قبول موحّد** `accept_portal_invitation` يخدم دعوات المالك والمستأجر معاً. RLS المكتب لم يُمسّ.
- التحقق: اختبار مخصّص **15/15** (الربط عبر القبول الموحّد، القراءات المحكومة، عزل مستأجر آخر FORBIDDEN، أحادية الرمز، حجب غير المدير) + السويت الرئيسي 36/36 + typecheck/build نظيفان.
- ⚠️ **يتطلب تطبيق هجرة `0029`**.

### ميزة — بوابة المالك (دخول ذاتي للمالك)
- **للمكتب:** في صفحة المالك زر **«إنشاء رابط بوابة المالك»** يُنشئ رابط دعوة (يظهر مرة واحدة، صالح ٣٠ يوماً) يُرسل للمالك.
- **للمالك:** `/portal` — بعد قبول الرابط وتسجيل الدخول، يرى **كشف حسابه** لفترة (المُحصَّل/الأتعاب/الصافي/المتبقّي لكل عقار)، **توريداته**، و**عقاراته** — للقراءة فقط. يدعم الملكية عبر أكثر من مكتب.
- **بيانات** — هجرة `0028`: المالك ليس عضواً في المكتب؛ يُربط `party` بهوية دخول (`create_owner_invitation`/`accept_owner_invitation` عبر حارس ربط الطرف الموجود)، ويقرأ بياناته حصراً عبر دوال **SECURITY DEFINER محكومة بـ`party.identity_id = auth.uid()`** (`my_owner_links`, `owner_portal_statement/properties/remittances`). **RLS المكتب لم يُمسّ إطلاقاً** — البوابة سطح منفصل. إعادة استخدام أنيق: دالة الكشف تستدعي `owner_statement` داخل سياق definer (تتجاوز RLS لذلك المالك فقط) بلا تكرار.
- الأمان: رمز الدعوة يُهاش (sha256) ويُستخدم مرة واحدة؛ مالك لا يرى مالكاً آخر (FORBIDDEN)؛ الدعوة للمدراء فقط؛ الطرف المرتبط بحساب آخر محميّ.
- التحقق: اختبار مخصّص **16/16** (الربط، القراءات المحكومة، عزل المالك الآخر، أحادية الرمز، حجب غير المدير) + السويت الرئيسي 36/36 + typecheck/build نظيفان.
- ⚠️ **يتطلب تطبيق هجرة `0028`**.

### ميزة — ملاحق العقود (تعديل الإيجار / الإنهاء المبكر)
- **من صفحة العقد النشط** قسم **«ملاحق العقد»**:
  - **تعديل الإيجار:** إيجار سنوي جديد + تاريخ سريان + سبب → يُعيد تسعير الاستحقاقات **المستقبلية غير المدفوعة** فقط بالسعر الجديد (تُترك المدفوعة والماضية كما هي).
  - **الإنهاء المبكر:** تاريخ + سبب → يجعل العقد «مُنهى» ويلغي الاستحقاقات المستقبلية غير المدفوعة ويُحرّر الوحدة (شاغرة).
- **سجلّ الملاحق** (نسخة، نوع، التغيير من←إلى، تاريخ السريان، السبب) على صفحة العقد، وبانر واضح للعقد المُنهى.
- **بيانات** — هجرة `0027`: دالتان ذرّيتان `amend_contract_rent` و`amend_contract_terminate` (SECURITY INVOKER) تحترمان **ثبات العقد بعد التفعيل**: لا تُعدَّل حقول العقد المجمّدة، بل يُسجَّل ملحق موثّق في `contract_amendment` (بترقيم نسخ) ويُطبَّق الأثر على الاستحقاقات/الحالة/الوحدة. إعادة التسعير تمسّ فقط الاستحقاقات بلا أي دفعة (تفادياً لتيتيم المخصّصات). التمديد مؤجّل (يتعارض مع مبدأ ثبات تاريخ النهاية).
- التحقق: اختبار مخصّص **20/20** (إعادة تسعير المستقبلية بالضريبة الصحيحة، عدم مسّ المدفوعة، لا تكرار، السكني بضريبة 0، الإنهاء يحرّر الوحدة ويلغي المستقبلية ويُبقي الماضية، الحراسات) + السويت الرئيسي 36/36 + typecheck/build نظيفان.
- ⚠️ **يتطلب تطبيق هجرة `0027`**.

### ميزة — نطاقات العقارات للأعضاء (بلا هجرة)
- **`/app/team/[member]/scope`**: يحدّد المدير ما إذا كان العضو يرى **كل العقارات** أو **عقارات محدّدة فقط** (باختيار العقارات).
- عمود **النطاق** في جدول الأعضاء يعرض الحالة ويربط بصفحة التحديد.
- يعتمد كلياً على الأساس الموجود في القاعدة: `membership.scope_all` + `membership_property_scope` + `has_property_access` + سياسات RLS — فلا هجرة جديدة. RLS يفرض العزل تلقائياً على كل جداول المحفظة (العقارات/الوحدات/العقود/الاستحقاقات…).
- التحقق: اختبار مخصّص **8/8** (عضو بلا نطاق لا يرى عقاراً؛ منح عقار يفتحه فقط؛ `scope_all` يفتح الكل؛ سحب المنح يُعيد الحجب؛ المدير يرى الكل) + السويت الرئيسي 36/36 + typecheck/build نظيفان.

### ميزة — دعوة الأعضاء والصلاحيات (فريق المكتب)
- **`/app/team`** (للمدراء): **دعوة عضو** بجوال/بريد ودور (مدير محفظة/محاسب/موظف/مطّلع/مدير) — يُنشأ **رابط انضمام** يُنسخ ويُرسل (صالح ١٤ يوماً، يُستخدم مرة واحدة، لا يظهر مرة أخرى). قائمة **الأعضاء** (جوال، دور، حالة) مع **تغيير الدور** و**إيقاف/تفعيل**، وقائمة **الدعوات المعلّقة** مع الإلغاء. غير المدراء يرون دورهم فقط.
- **`/app/join?token=…`**: صفحة قبول الدعوة (زر واحد) — تُنشئ عضويتك بالدور المحدّد.
- **بيانات** — هجرة `0026`: دالة `org_members()` (قائمة أعضاء المنشأة مع الجوال، للمدراء فقط — لأن جدول الهوية self-only، فهي المنفذ المصرّح) و`create_invitation()` (تُنشئ الدعوة وتُعيد الرمز الخام **مرة واحدة**؛ يُخزَّن هاشه فقط sha256). القبول عبر `accept_invitation` الموجودة. تغيير الدور/الحالة وإلغاء الدعوة تحديثات محكومة بـ RLS للمدراء.
- الأمان: الرمز يُولَّد ويُهاش في الخادم ولا يُخزَّن خاماً؛ يُستخدم مرة واحدة؛ غير المدير لا يرى القائمة ولا يدعو؛ **المالك الوحيد محميّ** من الإيقاف/الإنزال.
- التحقق: اختبار مخصّص **16/16** (توليد/هاش الرمز، عدم تخزينه خاماً، القائمة، القبول، منع إعادة الاستخدام، حجب غير المدير) + السويت الرئيسي 36/36 + typecheck/build نظيفان.
- ⚠️ **يتطلب تطبيق هجرة `0026`**.

### بنية — تنظيف (بلا تغيير سلوك)
- توحيد دوال مساعدة مكرّرة: `first` (فكّ علاقات Supabase) في [`lib/rows.ts`](lib/rows.ts)، و`isoDaysAgo` في [`lib/dates.ts`](lib/dates.ts)، ونقل `DOC_KIND_AR` إلى [`lib/labels.ts`](lib/labels.ts). (مراجعة clean-code-guard.)

### ميزة — توريد المالك وكشف الحساب القابل للطباعة
- **تسجيل توريد للمالك** من صفحة المالك: المبلغ (يُقترح تلقائياً = المتبقّي للمالك)، الطريقة، التاريخ، المرجع، مع الفترة تلقائياً من الكشف. يُصدَر **سند صرف مرقّم** `RM-YYYY-NNNNN`.
- **ملخّص الفترة** على صفحة المالك: صافي الفترة · المورّد في الفترة · المتبقّي للمالك، وجدول سجلّ التوريدات مع رابط سند الصرف لكل توريد.
- **كشف حساب قابل للطباعة** `/app/owners/[id]/statement`: ترويسة المنشأة، بيانات المالك وآيبانه، الفترة، جدول العقارات (المُحصَّل/الأتعاب/الصافي/المتبقّي) بإجمالياته، التوريدات خلال الفترة، وخلاصة (صافي الفترة − المورّد = المتبقّي للمالك).
- **سند صرف قابل للطباعة** `/app/owners/[id]/remittance/[id]`: للمالك، بالمبلغ رقماً و**كتابةً (تفقيط)**، الطريقة والمرجع والفترة، وخانتا التوقيع.
- **بيانات** — هجرة `0025`: جدول `owner_remittance` بترقيم متسلسل بلا فجوات لكل (منشأة، سنة) `RM-YYYY-NNNNN` (trigger)، RLS بنطاق المنشأة. التوريد يوثّق ما دُفِع فعلاً للمالك؛ والمستحق يُحسب من `owner_statement`.
- التحقق: اختبار مخصّص **7/7** (الترقيم، الاستقلال بين المنشآت، إعادة الضبط السنوي، منع المبلغ غير الموجب، مجموع توريدات الفترة) + السويت الرئيسي 36/36 + typecheck/build نظيفان.
- ⚠️ **يتطلب تطبيق هجرة `0025`**.

### ميزة — الإشعار الدائن والمدين (تصحيح الفواتير)
- **إشعار دائن** (إلغاء فاتورة): يُصدَر من صفحة الفاتورة بسبب مطلوب، يعكس بنودها كاملةً ويجعلها «ملغاة» (ويحرّر استحقاقها لإعادة الإصدار).
- **إشعار مدين** (إضافة مبلغ): يضيف مبلغاً على الفاتورة (وصف + مبلغ + سبب)، ويرث نسبة ضريبتها، دون تغيير حالتها.
- **صفحة الفاتourة** تعرض الآن حالتها (سارية/ملغاة) والإشعارات المرتبطة بها؛ وصفحة الإشعار تعرض الإشارة للفاتورة الأصلية والسبب. كلاهما قابل للطباعة بنفس تنسيق الفاتورة (مع رمز QR للمسجّل ضريبياً).
- **بيانات** — هجرة `0024`: توسعة `invoice` بـ`doc_kind` (فاتورة/إشعار دائن/إشعار مدين) و`ref_invoice_id` و`reason`، ودالتا `issue_credit_note` و`issue_debit_note` (SECURITY INVOKER، مع الحراسات: لا إشعار على إشعار، لا إلغاء لفاتورة ملغاة، السبب مطلوب). ترقيم مستقل بلا فجوات لكل (منشأة، سنة): `CN-YYYY-NNNNN` و`DN-YYYY-NNNNN` — وسلسلة الفواتير لا تتأثّر. الإشعارات بلا `charge_id` فلا تمسّ فهرس «فاتورة واحدة لكل استحقاق».
- التحقق: اختبار مخصّص **19/19** (الترقيم، عكس البنود، إلغاء الأصل وتحرير استحقاقه، الحراسات، حساب ضريبة الإشعار المدين) + السويت الرئيسي 36/36 + typecheck/build نظيفان.
- ⚠️ **يتطلب تطبيق هجرة `0024`**.

### ميزة — الفاتورة الضريبية (ZATCA — المرحلة الأولى)
- **إصدار فاتورة لكل استحقاق** من صفحة العقد (زر «إصدار» بجانب كل استحقاق؛ ومن أصدر تظهر بجانبه رقم فاتورته).
- **`/app/invoices`**: قائمة الفواتير (رقم، تاريخ، مشترٍ، ضريبة، إجمالي، نوع) مع رابط لكل فاتورة.
- **`/app/invoices/[id]`**: فاتورة قابلة للطباعة (عربي RTL): بيانات المورّد (اسم + س.ت + الرقم الضريبي)، بيانات المشتري، بنود الفاتورة، الإجماليات (غير شامل/الضريبة/الإجمالي)، المبلغ **كتابةً**، و**رمز QR** بالحقول الخمسة (اسم البائع، الرقم الضريبي، التاريخ، الإجمالي، الضريبة) بترميز TLV/Base64.
- **مرونة التسجيل الضريبي:** المورّد على فاتورة الإيجار هو **المالك** (المكتب وكيل)، أو **المنشأة** للعقارات الذاتية. إن كان له رقم ضريبي → **فاتورة ضريبية مبسطة + QR**؛ وإلا → **فاتورة عادية** بلا ضريبة/QR. **سكني معفى (0%) مع بيان الإعفاء**، **تجاري 15%**. أُضيف حقل الرقم الضريبي/السجل للمالك (نموذج في صفحة المالك).
- **بيانات** — هجرة `0023`: جدولا `invoice` و`invoice_line`، ودالة `issue_invoice(charge)` (لقطة ثابتة للمورّد/المشتري، بنود من الاستحقاق، إجماليات، نوع الفاتورة، ومنع تكرار الفاتورة لنفس الاستحقاق)، وترقيم متسلسل بلا فجوات لكل (منشأة، سنة) `INV-YYYY-NNNNN`. الفاتورة توثّق التوريد وهي **مستقلّة عن السداد** (تُصدَر بكامل المبلغ بصرف النظر عن الدفع الجزئي).
- أداة `lib/zatca.ts` لبناء حمولة QR، و`qrcode` لتوليد الرمز.
- التحقق: اختبار الإصدار **20/20** (VAT 15%/الإعفاء السكني، اللقطة، الترقيم، منع التكرار، فاتورة عادية مقابل ضريبية، المورّد الذاتي = المنشأة) + اختبار QR TLV 6/6 (فكّ الترميز يطابق الحقول، العربية محفوظة) + السويت الرئيسي 36/36 + typecheck/build نظيفان.
- ⚠️ **يتطلب تطبيق هجرة `0023`** لتفعيل الفواتير. الفاتورة الضريبية بالمرحلة الأولى؛ ربط المرحلة الثانية (المقاصة/الإبلاغ والتوقيع التشفيري) يحتاج تسجيل المنشأة في «فاتورة» لاحقاً.

### ميزة — سند القبض (إثبات التحصيل، قابل للطباعة)
- **لكل دفعة مستلمة سند قبض مرقّم** — إثبات استلام المبلغ (خاصة للكاش، وربط التحويل البنكي بالعقد/الاستحقاق).
- **`/app/receipts`**: قائمة كل السندات (رقم السند، التاريخ، المستلم منه، المبلغ، الطريقة) مع رابط لكل سند.
- **`/app/receipts/[id]`**: سند قبض قابل للطباعة (A4، عربي RTL): اسم المنشأة + س.ت/الرقم الضريبي، رقم السند وتاريخه، المستلم منه وهويته، المبلغ رقماً و**كتابةً (تفقيط)**، طريقة الدفع والمرجع، وبيان «وذلك عن» (العقد/الاستحقاقات المسدَّدة)، وخانتا المستلم والختم. زر طباعة يخفي واجهة التطبيق.
- **صفحة العقد**: قسم جديد «الدفعات المستلمة» يربط كل دفعة بسندها.
- **بيانات** — هجرة `0022`: عدّاد مستندات لكل منشأة (`org_counter` + `next_counter`) وترقيم **متسلسل بلا فجوات لكل (منشأة، سنة)** يُسند تلقائياً على كل دفعة (trigger) بصيغة `RV-YYYY-NNNNN`، مع تعبئة رجعية للدفعات الموجودة. السند يوثّق **الاستلام** وهو مستقلّ عن الفاتورة الضريبية (توثّق التوريد+الضريبة).
- أداة **التفقيط** العربية (`lib/tafqit.ts`) لتحويل المبلغ إلى كلمات على السند.
- التحقق: اختبار مخصّص للترقيم 9/9 (تسلسل لكل منشأة، إعادة ضبط سنوي، منع التكرار، التعبئة الرجعية) + السويت الرئيسي 36/36 (لا انحدار) + typecheck/build نظيفان.
- ⚠️ **يتطلب تطبيق هجرة `0022`** لتظهر أرقام السندات وتُطبع.

### ميزة — لوحة المؤشرات (الصفحة الرئيسية للمكتب)
- **الصفحة الرئيسية `/app`** أصبحت **لوحة مؤشرات** تظهر عند كل دخول:
  - **نسبة الإشغال** (مؤجرة/إجمالي الوحدات) بشريط تقدّم + عدد الشاغرة.
  - **تحصيل الشهر** (ر.س) مع اتجاه مقارنة بالشهر الماضي (▲/▼).
  - **المتأخرات** (ر.س) وعدد الاستحقاقات المتأخرة (بلون تنبيه عند وجودها).
  - **إجمالي المستحق** غير المسدَّد على المستأجرين.
  - شريط نشاط: **العقود النشطة**، **ما ينتهي خلال 60 يوماً** (رادار التجديد)، عدد العقارات، عدد المستأجرين — كلها روابط سريعة.
- المؤشرات العددية (الإشغال/العقود/العقارات) تُحسب عبر **استعلامات عدّ خفيفة** (بلا نقل صفوف) وتعمل فوراً. البطاقات المالية تعتمد على دالة القاعدة.
- **بيانات** — هجرة `0021`: دالة `dashboard_finance()` تُرجع صفاً واحداً (المستحق، المتأخرات، عدد المتأخرات، تحصيل الشهر، تحصيل الشهر السابق) — حدود الشهر بتوقيت الرياض. SECURITY INVOKER (يسري RLS ونطاق المنشأة النشطة).
- تدرّج آمن: إن لم تُطبَّق `0021` تظهر البطاقات المالية برسالة «طبّق هجرة 0021» بدل الخطأ، وباقي اللوحة يعمل.
- التحقق: اختبار مخصّص للدالة 7/7 (المستحق 9,000,000؛ متأخرات موجودة؛ تحصيل الشهر 3,000,000؛ فصل الشهر السابق 500,000) + typecheck/build نظيفان.
- ⚠️ **يتطلب تطبيق هجرة `0021`** لتظهر بطاقتا التحصيل والمتأخرات.

### ميزة — الملّاك وكشوف الحسابات
- **الملّاك** `/app/owners`: قائمة + إضافة مالك (اسم، نوع، هوية/سجل، جوال، آيبان، بنك).
- **ربط العقار بمالك:** اختيار المالك عند إنشاء العقار، وإعادة تعيينه من صفحة العقار (بدل المالك الذاتي الافتراضي).
- **تفاصيل المالك** `/app/owners/[id]`: عقاراته، **نسبة أتعاب الإدارة** (تُحفظ كاتفاقية إدارة)، و**كشف الحساب** لفترة: المُحصَّل − الأتعاب = الصافي للمالك، مع المتبقّي على المستأجرين.
- **بيانات** — هجرة `0020`: دالة `owner_statement(owner, from, to)` تجمع (لكل عقار) المُحصَّل خلال الفترة، والأتعاب (من اتفاقية النسبة)، والصافي، والمتبقّي. SECURITY INVOKER (يسري RLS ونطاق العقارات).
- التحقق: اختبار مخصّص (تحصيل 3,000,000 → أتعاب 5% = 150,000 → صافي 2,850,000 → متبقٍّ 9,000,000؛ خارج الفترة = 0) + typecheck/build نظيفان.
- ⚠️ **يتطلب تطبيق هجرة `0020`** لتظهر كشوف الحساب.

### ميزة — استيراد Excel (رفع ← تحقّق ← معاينة ← اعتماد ← تراجع)
- **`/app/import`**: تنزيل قوالب عربية جاهزة (عقارات/وحدات/ملّاك/مستأجرون/عقود/استحقاقات)، ورفع الملف بعد تعبئته.
- تحليل الملف داخل خادم Next (SheetJS)، وتخزين الصفوف، والتحقق عبر `import_validate` في القاعدة.
- **`/app/import/[batchId]`**: معاينة النتيجة (صحيحة/أخطاء)، **تقرير أخطاء لكل صف** (الصف + الحقل + السبب)، واعتماد الصفوف الصحيحة (`import_commit`)، والتراجع عن الدفعة كاملة (`import_revert`).
- يستخدم آلية الاستيراد الموجودة في القاعدة (الهجرة `0016`) — **لا يحتاج أي هجرة جديدة**. لا يُحفظ شيء قبل «اعتماد».
- التحقق: typecheck + build نظيفان.

### ميزة — المستأجرون والعقود والحالة المالية
- **المستأجرون** `/app/tenants`: قائمة + إضافة مستأجر (اسم، نوع، هوية/إقامة، جوال، بريد).
- **العقود** `/app/contracts`: إنشاء عقد كمسودة (وحدة + مستأجر + مدة + إيجار سنوي + دورية + تأمين + رسوم).
- **تفاصيل العقد** `/app/contracts/[id]`: تفعيل العقد، وجدول الاستحقاقات، وتسجيل الدفعات، والحالة المالية المشتقّة (مدفوع/متأخر/متبقّي).
- **بيانات** — هجرة `0019`: دالتان ذرّيتان في القاعدة:
  - `activate_contract` — يحوّل المسودة إلى نشط، ويولّد جدول الاستحقاقات حسب الدورية (تقسيم متساوٍ مع الباقي على آخر دفعة)، والضريبة (سكني 0%، تجاري 15%)، ويجعل الوحدة «مؤجرة».
  - `record_charge_payment` — ينشئ دفعة ويطابقها على الاستحقاق (بحد أقصى المتبقّي؛ الزائد رصيد للمستأجر).
- التحقق: 36/36 (لا انحدار) + اختبار مخصّص للدالتين (توليد الاستحقاقات، الضريبة، التفعيل، الدفعات الجزئية/الزائدة) + typecheck/build نظيفان.
- ⚠️ **يتطلب تطبيق هجرة `0019` على القاعدة الحيّة** ليعمل التفعيل وتسجيل الدفعات (إضافة/عرض المستأجرين والعقود تعمل بدونها).

### ميزة — أول شاشات سجل الحقيقة: العقارات والوحدات
- **العقارات** `/app/properties`: عرض قائمة عقارات المنشأة النشطة وإضافة عقار (اسم، نوع، مدينة، حي، رقم صك). المالك الافتراضي = المالك الذاتي للمنشأة.
- **تفاصيل العقار** `/app/properties/[id]`: بيانات العقار + جدول وحداته + إضافة وحدة (رقم، حالة، دور، مساحة، غرف، دورات مياه).
- شريط تنقّل موحّد للتطبيق (الرئيسية · العقارات) واسم المنشأة النشطة، مع حماية المسارات (تحويل لغير المسجّل).
- كل القراءة/الكتابة تمرّ عبر RLS بسياق `x-active-org` — لا تظهر إلا بيانات المنشأة النشطة.
- التحقق: typecheck + build نظيفان.

### بنية — النشر على Vercel وتشغيل البيئة الحيّة
- ضبط `NEXT_PUBLIC_SUPABASE_URL` و`NEXT_PUBLIC_SUPABASE_ANON_KEY` في مشروع Vercel `aqar` (production + preview) وإطلاق نشر جديد.
- **الرابط الحيّ: https://aqar-eta.vercel.app** — فحص `/api/health` يُرجع `ok:true` (الاتصال بـ Supabase سليم، RLS تعمل).
- تعطيل حماية النشر (Vercel Authentication) مؤقتاً ليكون الرابط قابلاً للعرض/المشاركة. **يُنصح بإعادة تفعيلها قبل إدخال بيانات حقيقية** (Vercel ← المشروع ← Settings ← Deployment Protection).
- تطبيق الهجرتين `0017` و`0018` على القاعدة، وتفعيل مزوّد الجوال + رقم تجريبي — أصبح تسجيل الدخول يعمل على الرابط الحيّ.

### توثيق — دليل المستخدم وسجل التحديثات
- إضافة [`CHANGELOG.md`](CHANGELOG.md) (هذا الملف) — يُحدَّث مع كل تغيير.
- إضافة دليل استخدام عربي للمستخدمين في [`docs/user-guide/`](docs/user-guide/).
- إضافة قائمة مهام النشر لفريق التطوير في [`docs/deployment-checklist.md`](docs/deployment-checklist.md).

### ميزة — المرحلة 2ب: تسجيل الدخول بالجوال (OTP) والمنشآت — `436f23a`
- شاشة `/login`: إدخال رقم الجوال ← استقبال رمز التحقق ← الدخول (عبر Supabase Auth، واجهة عربية RTL).
- شاشة `/app` المحمية: قائمة منشآتك، إنشاء منشأة جديدة، التبديل بين المنشآت، تسجيل الخروج.
- **بيانات** — هجرة `0017`: ربط الهوية بحساب المصادقة (إنشاء ملف الهوية تلقائياً عند أول دخول).
- **بيانات** — هجرة `0018`: تمكين المستخدم من رؤية منشآته (لعمل المبدّل) دون المساس بعزل البيانات.
- **قرار** — اعتماد **Supabase Auth** لتسجيل الدخول بالجوال (بدل نظام OTP المخصّص)، مع إمكانية توصيل مزوّد رسائل سعودي لاحقاً عبر SMS Hook.
- التحقق: بناء نظيف، `/login` تعمل، `/app` تحوّل لغير المسجّل، فحص الاتصال `ok:true`، واختبارات القاعدة 36/36 + 3/3.

### ميزة — المرحلة 2: إعداد تطبيق Next.js وربطه بـ Supabase — `ee45e39`
- مشروع Next.js 15 (App Router) + TypeScript + Tailwind، واجهة **عربية RTL**.
- عملاء Supabase (خادم/متصفح) مع تمرير سياق المنظمة النشطة عبر ترويسة `x-active-org`.
- صفحة رئيسية بفحص اتصال حيّ + مسار `/api/health`.
- **بنية** — التحقق الحيّ: الاتصال بـ Supabase يعمل، ومخطط `app` مكشوف، وRLS تحجب الزائر المجهول (`ok:true`).

### بيانات — تطبيق طبقة البيانات على Supabase والتحقق
- تطبيق كل الهجرات (`schema_all.sql`) على قاعدة Supabase عبر SQL Editor.
- التحقق: 29 جدولاً + 4 عروض (views) + RLS مفعّلة على 29 جدولاً + صفر حقول عشرية (float).
- **إصلاح** — جعل هجرة `0003` آمنة على Supabase (عدم استبدال دوال `auth.uid()` المملوكة للنظام).

### بنية — نقل المشروع إلى GitHub — `ead3d16` / `40b7ca1`
- نقل كامل المشروع إلى مجلد `aqar/` ورفعه إلى مستودع GitHub (فرع `main`).
- إضافة `.gitignore`، `.env.example`، و`supabase/schema_all.sql` (ملف مجمّع للتطبيق دفعة واحدة).

### قرار / بيانات — المرحلة 1: طبقة البيانات (سجل الحقيقة) — **مُعتمدة**
- 18 هجرة SQL: المخطط الكامل، سياسات RLS لكل جدول، دوال أمان، المصادقة، العروض المالية، وآلية استيراد Excel.
- مبادئ ملزمة: عزل متعدد المستأجرين، الحالة المالية مشتقّة لا مُخزّنة، المبالغ بالهللات (صحيحة)، ثبات العقد بعد التفعيل، جاهزية ضريبية (ZATCA) على كل استحقاق، سجل تدقيق غير قابل للتعديل.
- اختبارات آلية: 36/36 فحص سلوكي + 3/3 ملفات pgTAP على PostgreSQL 17.
- التوثيق الكامل في [`SCHEMA.md`](SCHEMA.md).

### توثيق — تحليل المنافسين
- دراسة تفصيلية لـ Diiwan وRawaf Amlak وAmlak One مع الأدلة في [`analysis/`](analysis/) والتقرير في [`analysis/REPORT.md`](analysis/REPORT.md).

---

## كيف نُحدّث هذا الملف | How we maintain this
عند كل تغيير: أضِف مدخلاً تحت تاريخ اليوم بأعلى القسم، اذكر النوع، ووصفاً موجزاً، ومعرّف الـ commit إن وُجد. حدِّث قسم "بانتظار فريق التطوير" عند إنجاز أي بند.
