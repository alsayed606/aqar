# ٦) تقرير مراجعة التوثيق — Documentation Review Report
*عقار (Aqar) · وثيقة تأسيسية ٦ من ٧ · مُعتمدة · رصد فقط (لا تعديل)*

> كل بند له مُعرّف (مر-NN)، دليل، وشدّة. **الإجراءات تُنفَّذ ضمن خارطة الطريق لا الآن.** الخلاصة: طبقة البيانات والتطبيق متقدّمان على مستندات الحالة العليا؛ الانجراف مركَّز في `README`/`roadmap`/`deployment-checklist` واكتمال `SCHEMA.md`، ودليل المستخدم دقيق إلى حدّ كبير.

## أ) تعارضات التوثيق ↔ الواقع
| # | التعارض | الدليل | الشدّة |
|---|---|---|---|
| مر-١ | `README` يقول «١٦ هجرة» والفعلي ٣١ | `README.md:12` | حرج |
| مر-٢ | `README` «لا واجهة ولا كود بعد» والتطبيق كامل حيّ | `README.md:24-25` مقابل `app/**` | حرج |
| مر-٣ | سطر حالة `SCHEMA.md` «لا واجهة قبل موافقتك» | `SCHEMA.md:4` | عالٍ |
| مر-٤ | جذر `CLAUDE.md` يصف منهجية Swarm ليست منهجية البناء → يضلّل مطوّراً جديداً | `CLAUDE.md` | عالٍ |
| مر-٥ | سطر تقنيات `README` يعِد ZATCA-2/PDPL/Moyasar غير المُنفَّذة | `README.md:47` | متوسط |

## ب) مستندات قديمة
| # | المستند | الدليل | الشدّة |
|---|---|---|---|
| مر-٦ | `05-roadmap.md` يضع ميزات منجزة في «القادمة/لاحقاً» (تجديد، دعوة أعضاء، بوابتان) | `05-roadmap.md:24-37` | عالٍ |
| مر-٧ | `deployment-checklist.md` متجمّد عند «طبّق 0017/0018» | الملف كامل | متوسط |
| مر-٨ | خريطة `SCHEMA.md` تُوثّق 0001–0016 فقط؛ ١٥ هجرة غير موثّقة | `SCHEMA.md:13-30` | عالٍ |

## ج) معلومات مكرّرة
- مر-٩ «ZATCA-2» مذكورة مرّتين في `05-roadmap.md`. — متوسط
- مر-١٠ قائمة الهجرات موزّعة على ٣ مصادر متضاربة (README/SCHEMA/CHANGELOG). — متوسط
- مر-١١ قسم «خطوات التشغيل» في `CHANGELOG` يكرّر `deployment-checklist`. — منخفض

## د) توثيق ناقص
- مر-١٢ لا توثيق لطبقة التطبيق/المعمار (RSC/Server Actions/`x-active-org`/البوابات). — عالٍ
- مر-١٣ لا توثيق لاستراتيجية الاختبار ولا إفصاح بأن اختبارات المرحلة الثالثة غير مُلتزَمة. — عالٍ
- مر-١٤ لا توثيق لواقع فرض الأدوار («المطّلع» يكتب). — عالٍ
- مر-١٥ لا مستندات تشغيل/نسخ احتياطي/مراقبة/PDPL ولا ADRs. — متوسط
- مر-١٦ الكيان `document` والـ OTP الميت غير موثّقَين كذلك. — منخفض

## هـ) متطلبات متضاربة
- مر-١٧ `README`/`SCHEMA` يعلنان «طبقة بيانات فقط» بينما التطبيق حيّ — تضارب في مرحلة المشروع. — عالٍ
- مر-١٨ ترويسة `schema_all.sql` «AUTO-GENERATED» بينما الواقع إلحاق يدوي. — متوسط

## و) انجراف الكود/القاعدة
- مر-١٩ كود ميت: OTP المخصّص (0004/0014) موثّق كآلية المصادقة بينما الحيّ Supabase Auth. — عالٍ
- مر-٢٠ ملفات Ruflo في الجذر غير مُتجاهَلة (`ruvector.db` ١.٥ ميغا وغيرها). — عالٍ
- مر-٢١ ملف قمامة مُلتزَم `supabase/tests/local/({wch`. — منخفض
- مر-٢٢ ترويسة بوابة المستأجر تعرض «بوابة المالك» (`app/portal/layout.tsx:20`). — منخفض
- مر-٢٣ الصفحة الرئيسية `/` لوحة فحص تقنية تعرض حالة/رابط Supabase للعموم. — متوسط

## ز) تسلسل «مصدر الحقيقة» المعتمد
الكود+الهجرات (تقني) ← الميثاق (منتجي) ← `CHANGELOG` (زمني) ← `SCHEMA.md`+دليل المستخدم (تفصيلي) ← `README`/`roadmap`/`checklist` (ملخّصات تتبع ما سبق).

## ح) خريطة الإجراءات (ضمن خارطة الطريق)
- **حرج:** تصحيح `README` (مر-١/٢)، `SCHEMA.md` الحالة (مر-٣).
- **عالٍ:** استبدال `roadmap` (مر-٦)، توثيق 0017–0031 (مر-٨)، معالجة تضليل `CLAUDE.md` + تجاهل Ruflo (مر-٤/٢٠)، توثيق الأدوار (مر-١٤) والاختبار (مر-١٣) والمعمار (مر-١٢)، إزالة OTP الميت (مر-١٩).
- **متوسط/منخفض:** `deployment-checklist`، أتمتة `schema_all` (مر-١٨)، حذف ملف القمامة (مر-٢١)، ترويسة البوابة (مر-٢٢)، الصفحة الرئيسية (مر-٢٣).
